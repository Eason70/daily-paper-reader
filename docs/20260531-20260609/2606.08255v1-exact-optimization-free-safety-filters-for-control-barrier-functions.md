---
title: Exact Optimization-Free Safety Filters for Control Barrier Functions
title_zh: 控制障碍函数的精确无优化安全滤波器
authors: Ankit Goel
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08255v1"
tags: ["query:safe-rl-cbf"]
score: 9.0
evidence: 提出控制屏障函数精确无优化安全滤波器
tldr: 针对控制仿射系统的安全滤波器通常依赖二次规划求解，在嵌入式或高频率应用中计算负担大。本文研究无需优化即可精确计算欧几里得投影的条件，通过分析被标称输入违反的仿射不等式，计算最小范数矫正使其满足等式约束。主要结果给出该矫正与可行集上精确投影一致的结构条件，利用Gram矩阵通过不等式法线交互解释。最后提供在线验证程序，确保优化自由更新的精确性。该方法降低了计算负担，适用于资源受限系统。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08255v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 702, \"height\": 576, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08255v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 658, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08255v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 588, \"height\": 505, \"label\": \"Figure\"}]"
motivation: 基于控制屏障函数的安全滤波器通常需在线求解二次规划，在嵌入式或高频率应用中计算负担过大。
method: 提出免优化安全滤波器，通过计算标称输入所违反仿射不等式的最小范数矫正，并推导其与精确欧几里得投影一致的结构条件。
result: 给出了矫正等价于可行集投影的充要条件，通过Gram矩阵和法线交互解释，并提供在线验证流程。
conclusion: 实现了无需优化即可精确计算安全滤波器输出，显著降低计算复杂度，适用于资源受限系统。
---

## 摘要
对于控制仿射系统，标准和高阶控制障碍函数条件在控制输入中是仿射的，通常通过基于二次规划的安全滤波器来强制执行。尽管这些优化问题是凸的，但在嵌入式、高速率或资源受限的实现中可能不受欢迎。本文研究了何时可以在不求解二次规划的情况下精确计算相应的欧几里得投影。给定一个标称控制输入，我们形成被该输入违反的仿射不等式集合，并计算强制执行这些不等式为等式的最小范数修正。该修正不必等于完全可行集上的精确欧几里得投影。主要结果给出了该修正与可行集上欧几里得投影一致的结构条件。这些条件通过仿射不等式法线之间的相互作用来解释，并使用格拉姆矩阵表示。最后，给出了一种在线认证程序，用于确定无优化更新是否精确。

## Abstract
For control-affine systems, standard and high-order control barrier function conditions are affine in the control input and are commonly enforced through quadratic-program-based safety filters. Although convex, these optimization problems may be undesirable in embedded, high-rate, or resource-limited implementations. This letter studies when the corresponding Euclidean projection can be computed exactly without solving a quadratic program. Given a nominal control input, we form the set of affine inequalities violated by that input and compute the minimum-norm correction that enforces those inequalities with equality. This correction need not equal the exact Euclidean projection onto the full feasible set. The main result gives structural conditions under which it coincides with the Euclidean projection onto the feasible set. These conditions are interpreted through interactions between affine-inequality normals and are expressed using a Gram matrix. Finally, an online certification procedure is given for determining whether the optimization-free update is exact.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：在控制仿射系统中，控制障碍函数（CBF）和高阶CBF（HOCBF）导出的安全条件在控制输入中是仿射的，通常通过求解二次规划（CBF-QP）实现安全滤波器，以最小程度修改标称控制输入。
- **核心问题**：尽管CBF-QP是凸优化问题，但在嵌入式、高更新率或资源受限的系统中，每一时间步求解QP可能带来不可接受的计算负担。因此，需要在不调用数值求解器的情况下，获得与QP解（即欧几里得投影）完全相同的结果。
- **整体含义**：本文研究“无优化安全滤波器”的精确性条件，即仅根据当前被违反的约束计算修正，并推导该修正等于可行集上欧几里得投影的充要条件，从而在理论上保证可跳过QP求解，同时保持滤波后的最优性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：给定标称控制输入 \(u_{\text{nom}}\)，首先找出所有被违反的仿射不等式（\(A u \leq b\) 中残差为正的约束），仅对这些约束进行最小范数修正使其满足等式，得到一个候选输出 \(u_V\)。然后给出判断 \(u_V\) 是否等于精确欧几里得投影 \(u^*\) 的条件。
- **关键技术细节**：
  - **问题形式化**：可行集 \(\mathcal{U} = \{u \in \mathbb{R}^m : A u \leq b\}\)，\(A \in \mathbb{R}^{q \times m}\)。定义违反集 \(\mathcal{V} = \{i : (A u_{\text{nom}} - b)_i > 0\}\)。假设 \(A_\mathcal{V}\) 行满秩，定义违反集投影：
    \[
    u_\mathcal{V} = u_{\text{nom}} - A_\mathcal{V}^T (A_\mathcal{V} A_\mathcal{V}^T)^{-1} (A_\mathcal{V} u_{\text{nom}} - b_\mathcal{V})
    \]
  - **精确性充要条件（定理1）**：\(u_\mathcal{V} = u^*\) 当且仅当：
    1. 原始可行性：\(A u_\mathcal{V} \leq b\)；
    2. 对偶可行性：\(\lambda_\mathcal{V} = (A_\mathcal{V} A_\mathcal{V}^T)^{-1}(A_\mathcal{V} u_{\text{nom}} - b_\mathcal{V}) \geq 0\)（分量非负）。
  - **结构充分条件（定理2）**：若 \(A_\mathcal{V}\) 行满秩，且满足：
    \[
    (A_\mathcal{V} A_\mathcal{V}^T)^{-1} \geq 0, \quad A_{\mathcal{V}^c} A_\mathcal{V}^T (A_\mathcal{V} A_\mathcal{V}^T)^{-1} \geq 0,
    \]
    则 \(u_\mathcal{V} = u^*\)。该条件可基于行向量之间的Gram矩阵 \(G = A A^T\) 表达为：
    \[
    G_{\mathcal{V},\mathcal{V}}^{-1} \geq 0, \quad G_{\mathcal{V}^c,\mathcal{V}} G_{\mathcal{V},\mathcal{V}}^{-1} \geq 0.
    \]
  - **几何特例**：
    - 当违反约束的法向量相互正交（Corollary III.1）时，精确性自动成立。
    - 当仅有一个违反约束且所有其他约束与该约束的法向量点积非负（Corollary III.2）时，精确性成立。
  - **在线认证算法（Algorithm 1）**：每一步计算残差、确定 \(\mathcal{V}\)，计算 \(u_\mathcal{V}\) 和 \(\lambda_\mathcal{V}\)，然后检查原始可行性和对偶可行性。若通过，则直接输出 \(u_{\text{safe}} = u_\mathcal{V}\)；否则回退到标准CBF-QP或其他方案。

## 3. 实验设计：数据集/场景、基准、对比方法

- **本论文为纯理论推导，未进行任何数值实验或仿真验证。** 文中未提及数据集、仿真场景、基准方法或对比实验。所有结论均基于数学证明和几何图示（3张示意图）。

## 4. 资源与算力

- **文中未提及任何计算资源、GPU型号、训练时长或算力消耗。** 由于论文性质为理论分析，不涉及大规模计算。

## 5. 实验数量与充分性

- **无实验。** 因此不涉及实验数量、消融实验等。论文的充分性体现在数学定理的严格证明和条件推导上，而非通过实验验证。

## 6. 论文的主要结论与发现

- **主要结论**：
  1. 提出了一个可在线检查的精确性充要条件（定理1），用于判断基于违反集的最小范数修正是否等于可行集上的欧几里得投影。
  2. 推导了更易检验的结构充分条件（定理2），该条件只依赖于约束法向量的Gram矩阵的子块满足逆正性和非负性。
  3. 给出了两个几何直观的特例（正交约束、单个违反约束且交互非负），在这些情况下无优化校正自动精确。
  4. 设计了一个完整的无优化安全滤波器在线算法（Algorithm 1），通过一次检查即可在满足条件时避免求解QP，否则回退到QP。

## 7. 优点：方法或实验设计上的亮点

- **理论创新性强**：首次系统研究了CBF安全滤波器中无优化投影的精确性条件，将问题归结为Gram矩阵的逆正性，为资源受限系统提供了理论支撑。
- **算法实用性高**：所提在线认证算法（Algorithm 1）计算量极低（仅需矩阵乘法和少量比较），且可直接嵌入现有CBF框架；在不满足条件时仍可回退到标准QP，保证了安全性。
- **几何解释直观**：通过法向量交互的图示（Fig.1-3）清晰展示了条件（正交、正交互、负交互）在控制空间中的几何含义，便于理解。

## 8. 不足与局限

- **缺乏实验验证**：论文未提供任何数值仿真或实际系统案例，因此无法评估所提算法在典型场景（如机器人控制、自动驾驶）中的实际表现和计算收益。条件在实际动态系统中是否频繁满足仍存疑。
- **假设较强**：条件要求 \(A_\mathcal{V}\) 行满秩（避免奇异）、Gram子块逆正且与其他子块交互非负。这些条件在约束数量多、状态高度变化的场景中可能难以保证，导致算法频繁回退到QP。
- **未考虑高维或非线性扩展**：仅处理控制仿射系统下的线性仿射CBF约束；对于非仿射系统或高阶CBF带来的非线性约束，方法不直接适用。
- **未对比现有近似方法**：虽然指出“无需优化”，但未与已有的低复杂度近似滤波器（如基于松弛的QP、预测校正方法）进行理论或计算复杂度对比。

（完）

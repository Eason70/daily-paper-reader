---
title: An Adjoint-based Neural Regulator for Real-Time Optimal Control with State Constraints
title_zh: 一种基于伴随的神经调节器，用于具有状态约束的实时最优控制
authors: "Isaiah A. Agboola, Yuxin Tong, Uduak Inyang-Udoh"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16303v1"
tags: ["query:safe-rl-cbf"]
score: 9.0
evidence: 使用控制屏障函数进行安全约束最优控制的神经调节器
tldr: 针对非线性系统实时约束最优控制问题，提出基于伴随的神经调节器（ANR）。该方法学习伴随策略而非直接近似控制律，通过哈密顿量保证最优性，运行时用凸投影满足约束。在独轮车模型上，ANR性能与非线性MPC相当但计算成本降低两个数量级，且跨场景泛化性远超强化学习方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16303v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1837, \"height\": 444, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16303v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 891, \"height\": 326, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16303v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 876, \"height\": 1036, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16303v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 846, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16303v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 901, \"height\": 651, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16303v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 574, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16303v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 447, \"label\": \"Table\"}]"
motivation: 现有实时最优控制方法（如MPC）计算成本高，强化学习泛化性差，需一种高效且安全的方法处理非线性系统状态约束。
method: 基于庞特里亚金最小值原理，学习伴随策略编码最优性，运行时通过凸投影强制执行器约束和控制障碍函数约束。
result: 在独轮车模型上，ANR性能与非线性MPC相当，计算成本降低两个数量级，跨场景表现近不变，显著优于强化学习。
conclusion: ANR实现了高效、安全、泛化性强的实时约束最优控制，适用于计算资源受限的安全关键系统。
---

## 摘要
本文介绍了一种基于学习的控制框架，用于具有安全保证的非线性系统的实时约束最优控制，该框架基于庞特里亚金最小值原理。该方法学习一个神经共态（伴随）策略，通过系统哈密顿量编码最优性，而不是直接近似控制律。可行性通过一个高效的凸投影在运行时单独执行，该投影包含了执行器限制以及以控制屏障函数形式表示的安全约束。我们将此框架称为基于伴随的神经调节器（ANR），因为它产生了一个满足约束的控制器，同时保留了由学习的伴随编码的最优性结构。我们使用独轮车模型在非线性约束控制任务上展示了所提出框架的有效性。ANR实现了与非线性和模型预测控制相当的性能，而计算成本降低了两个数量级以上，并且在未见场景中表现出近乎不变的性能，因此在训练分布之外的区域显著优于强化学习方法。

## Abstract
This paper introduces a learning-based control framework for real-time constrained optimal control of nonlinear systems with safety guarantees based on the Pontryagin's Minimum Principle. The approach learns a neural co-state (adjoint) policy that encodes optimality through the system Hamiltonian, rather than directly approximating a control law. Feasibility is enforced separately at runtime through an efficient convex projection that incorporates actuator limits and safety constraints expressed as control barrier functions. We refer to this framework as an adjoint-based neural regulator (ANR) as it yields a controller that satisfies constraints while retaining the optimality structure encoded by the learned adjoint. We demonstrate the effectiveness of the proposed framework on nonlinear constrained control tasks using a unicycle model. The ANR achieves performance at par with nonlinear model predictive control at more than two orders of magnitude lower computational cost, while exhibiting near-invariant performance across unseen scenarios, thus, significantly outperforming reinforcement learning methods in out-of-training-distribution regimes.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：非线性系统的实时约束最优控制面临计算效率与安全保证之间的两难。传统非线性模型预测控制（NMPC）精度高但在线求解非线性规划成本高（可达数百毫秒），难以用于毫秒级动态场景；强化学习（RL）虽推理速度快，但泛化性差、需要大量奖励工程，且无法严格保证约束满足。
- **研究动机**：结合NMPC的最优性结构与RL的快速推理优势，同时通过控制屏障函数（CBF）在运行时强制执行安全和输入约束，提出一种新的学习控制框架。
- **整体含义**：本文提出的基于伴随的神经调节器（ANR）能够在保持与NMPC相当性能的前提下，将计算成本降低两个数量级以上，并在训练分布外场景中表现出近乎不变的性能，显著优于RL。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：基于庞特里亚金最小值原理（PMP），学习一个共态（伴随）神经网络（CoNN）来预测有限时域的共态轨迹，而非直接近似控制律。在线部署时，利用第一个预测共态执行带约束的哈密顿最小化，同时通过CBF二次规划（CBF-QP）强制执行器和安全约束。
- **关键技术细节**：
  - **CoNN架构**：全连接前馈神经网络，输入当前状态，输出长度为 \( n_p \) 的共态序列 \(\hat{\Lambda}_i = \{\hat{\lambda}_{0|i}, \hat{\lambda}_{1|i}, \dots, \hat{\lambda}_{n_p-1|i}\}\)。
  - **离线训练**：
    - 对每个训练状态 \( x_i \)，通过CoNN预测共态序列。
    - 对每个时间步，通过解析哈密顿最小化（对于二次成本）得到最优控制：\(\hat{u}^*_{k|i} = -\frac{1}{2}R^{-1}g^T(x_{k|i})\hat{\lambda}_{k|i}\)。
    - 利用动力学积分产生状态和输入轨迹。
    - 训练损失由阶段成本、终端成本和共态正则化（L1范数）三部分组成，通过反向传播整个展开轨迹优化CoNN参数。
  - **在线部署**：
    - 使用当前状态 \( x_k \) 通过CoNN预测共态序列，取第一个共态 \(\hat{\lambda}_{0|k}\)。
    - 求解安全感知的约束哈密顿最小化问题（式18）：\(\min_u [u^T R u + \hat{\lambda}_{0|k}^T g(x_k) u]\)，约束包括输入集 \(\mathcal{U}\) 和基于高阶CBF（HOCBF）的安全条件（式18c）。
    - 该优化为凸二次规划，求解高效。
- **公式与算法流程**：文中给出了PMP最优性条件、NMPC公式、SAC算法目标函数、CBF不等式、HOCBF表达式以及ANR的损失函数和在线优化问题，但本总结以文字说明为主。

## 3. 实验设计：数据集/场景、基准、对比方法
- **场景**：使用独轮车模型（动力学见式19），状态为 \([x_p, y_p, \theta, v]\)，控制为加速度 \(a\) 和角速度 \(\omega\)。任务为引导机器人到达目标点（零参考或非零参考），同时满足执行器限制（\(-1 \le a \le 1, -4 \le \omega \le 4\)）和圆形障碍物避碰。
- **数据集**：训练数据来自在状态空间 \([-2,2]\)（\(x, y, \theta, v\)）的网格上均匀采样的初始状态，每维10个采样点。无障碍物环境下训练CoNN。在线评估时包含障碍物和分布外初始状态。
- **基准与对比方法**：
  - **NMPC**：使用CasADi和ipopt求解器，完整非线性规划。
  - **RL**：基于软演员-评论家（SAC）算法，配备CBF安全滤波器（RL-CBF）。
- **实验组别**：分为无障碍物和有障碍物两大类，每类包含：
  - Case A：分布内评估（训练域内初始状态）
  - Case B：分布外评估（训练域外初始状态）
  - Case C：分布外评估且目标为非零参考（仅ANR vs NMPC）

## 4. 资源与算力
- 文中明确提及：所有仿真在一台配备Intel Core i9-14900K CPU和NVIDIA GeForce RTX 4080 SUPER GPU的台式计算机上进行。
- **但未说明**：CoNN和SAC的具体训练时长、GPU数量、batch size等细节，仅提及“离线训练”阶段。

## 5. 实验数量与充分性
- **实验数量**：
  - 无障碍物Case A：各100次试验，评估成功率和平均性能指标。
  - 无障碍物Case B/C：未明确试验次数，但展示了代表性的轨迹和表格数据。
  - 有障碍物Case A：100次试验（障碍物随机位置），统计成功率。
  - 有障碍物Case B：100次试验（分布外初始状态+随机障碍物），统计成功率。
  - 有障碍物Case C：未明确试验次数，有代表性轨迹和表。
- **充分性评价**：
  - 实验覆盖了分布内、分布外、不同参考、有无障碍物等多种场景，较为充分。
  - 对比了两种主流方法（NMPC和RL），且使用了多项指标（状态/控制MSD、绝对收敛误差、计算速度、成功率）。
  - **潜在不足**：RL在部分场景中未报告（如分布外非零参考场景），削弱了对比完整性。消融实验未单独做（例如移除CBF或不同正则化系数的影响）。仅使用独轮车模型，未扩展到高维系统或动态障碍物，泛化性验证有限。

## 6. 论文的主要结论与发现
- ANR在性能（收敛误差、状态/控制平滑度）上接近NMPC，而计算速度比NMPC快两个数量级以上（~1.2 ms vs 350-1200 ms），比RL略慢（~0.5 ms）但RL泛化性差。
- 在分布内场景，RL成功率仅30%-38%，而ANR达82%-100%。
- 在分布外场景（初始状态或参考目标超出训练域），RL彻底失败，ANR仍能保持较高成功率（98%）。
- ANR的泛化能力归因于其基于PMP的最优性结构，而非直接拟合控制动作。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：将伴随学习与CBF约束相结合，实现了“最优性+可行性”的解耦：离线学习最优性结构，在线用凸投影保证约束，避免了RL-CBF框架中“名义-投影”不匹配导致的保守/激进问题。
- **计算效率**：在线只需一次CoNN前向推理+一个小的凸QP求解，避免了反复非线性优化。
- **泛化性**：跨初始状态、目标参考、障碍物配置的泛化能力显著优于RL。
- **实验设计**：设计了多场景对比，包含分布内/外、有无障碍物、不同参考，评估指标全面（成功率、MSD、ACE、计算时间）。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制
- **实验覆盖**：仅在低维独轮车系统上验证，未在更高维系统（如四旋翼、机械臂）或动态障碍物场景测试，结论的普适性受限。
- **对比偏差**：RL-CBF仅在部分场景中运行，分布外非零参考场景未比较RL，可能弱化了对比的公平性。
- **离线训练依赖**：CoNN需要在无约束环境中预先训练，对于约束变化频繁的任务可能需要重新训练，适应性有限。
- **安全保证强度**：虽然CBF提供了一阶/二阶安全条件，但若HOCBF参数选择不当或系统动力学建模误差大，仍可能存在安全漏洞（文中未讨论鲁棒性）。
- **代码与可复现性**：论文称代码开源，但未详细说明超参数（如网络结构、学习率、训练轮数）设置，复现可能需额外工作。
- **未讨论**：与SQP-RTI等近似MPC方法的对比（仅在脚注中提及且未给出定量数据）。

（完）

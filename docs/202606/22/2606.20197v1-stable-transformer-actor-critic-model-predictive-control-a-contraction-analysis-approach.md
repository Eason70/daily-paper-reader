---
title: "Stable Transformer-Actor-Critic Model Predictive Control: A Contraction Analysis Approach"
title_zh: 稳定Transformer-Actor-Critic模型预测控制：一种收缩分析方法
authors: "Antonio Marino, Valerio Modugno, Marco Cognetti"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20197v1"
tags: ["query:safe-rl-cbf"]
score: 8.0
evidence: 使用变压器-演员-评论家MPC和收缩分析提供稳定鲁棒控制策略，与安全强化学习相关
tldr: Actor-Critic模型预测控制（MPC）在处理复杂非凸控制问题时表现优异，但序列预测模型的使用使得闭环稳定性难以保证。本文提出一种基于收缩分析的Transformer-Actor-Critic MPC架构，通过证明Transformer网络满足全局增量输入状态稳定性（δISS），并利用黎曼收缩理论刻画物理系统与预测网络间的互联动态，将理论界作为训练正则化项以产生可证鲁棒的策略。在非线性3D无人机目标到达和避障机动任务中验证了该方法的有效性。该框架首次为基于Transformer的Actor-Critic MPC提供了形式化的鲁棒性保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20197v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20197v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 919, \"label\": \"Figure\"}]"
motivation: 传统Actor-Critic MPC难以保证基于序列预测模型的闭环稳定性，需要形式化鲁棒性保证。
method: 提出Transformer-Actor-Critic MPC，基于收缩分析证明Transformer满足δISS，将理论界作为训练正则化项。
result: 在非线性3D无人机仿真中，目标到达与避障任务性能优于基线方法。
conclusion: 首次通过收缩分析为Transformer-Actor-Critic MPC提供形式化稳定性保障。
---

## 摘要
Actor-Critic模型预测控制有效解决了复杂的非凸控制问题，但保证这些流程中基于序列的学习模型的闭环稳定性仍然具有挑战性。本文提出了一种新颖的Transformer-Actor-Critic MPC架构，具有形式化的鲁棒性保证。首先，我们证明了Transformer网络可以满足全局增量输入到状态稳定性（δISS）。然后，我们利用黎曼收缩理论分析物理对象与预测神经网络之间的互联动态。最后，我们将这些理论界作为训练正则化项，以产生可证明鲁棒的策略。该框架在非线性3D无人机模型上进行了验证，执行目标到达和避障机动。

## Abstract
Actor-Critic Model Predictive Control (MPC) effectively addresses complex, non-convex control problems, but guaranteeing the closed-loop stability of sequence-based learning models within these pipelines remains challenging. This paper introduces a novel Transformer-Actor-Critic MPC architecture with formal robustness guarantees. First, we prove that Transformer networks can satisfy global incremental Input-to-State Stability ($δ$ISS). We then leverage Riemannian contraction theory to analyze the interconnected dynamics between the physical plant and the predictive neural network. Finally, we integrate these theoretical bounds as a training regularizer to yield a certifiably robust policy. The framework is validated on a nonlinear 3D drone model executing target-reaching and obstacle-avoidance maneuvers.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：Actor-Critic Model Predictive Control (MPC) 在处理复杂非凸控制问题时表现优异，但将序列预测模型（如 Transformer）集成到 MPC 中难以保证闭环稳定性。现有方法（如基于 MLP 的 Actor-Critic MPC）缺乏形式化的鲁棒性保证，尤其在存在时间相关干扰时表现不佳。
- **背景**：随着学习型控制的发展，混合优化-学习框架（如学习型MPC）日益重要。然而，序列模型（Transformer）在控制中的应用缺乏增量输入到状态稳定性（δISS）和收缩理论分析。现有的基于 ODE 的 Transformer 稳定性分析依赖限制性假设（如单位范数投影），未能解决反馈系统的基本鲁棒性问题。
- **整体含义**：本文旨在通过数学约束使 Transformer 满足全局 δISS，并利用黎曼收缩理论分析物理系统与预测网络的互联动态，从而为基于 Transformer 的 Actor-Critic MPC 提供首个形式化的鲁棒性保证。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **双线贡献**：
  1. 证明 Transformer 架构可以通过数学约束满足全局 δISS。
  2. 提出一种新的混合架构：Transformer-Actor-Critic MPC，将推导的 δISS Lipschitz 边界与物理系统的小增益条件耦合，确保闭环收缩稳定性。该理论界作为训练正则化项，产生可证明鲁棒的策略。

### 关键技术细节
- **系统模型**：离散时间非线性系统 \( x_{k+1} = \Phi(x_k, u_k) + d_k(x_k) \)，假设状态和控制有界。
- **Transformer 的 δISS 分析**：
  - 将 Transformer 块建模为离散动态系统（式11），包含残差连接、层归一化、多头注意力和 MLP。
  - 通过 Lipschitz 常数上界（如层归一化 Lipschitz 为 \( \|c\|_\infty / (\sigma_{\min} + \epsilon) \)，softmax 的 1/2-Lipschitz 等）推导出状态偏差递推关系（式14）。
  - 得出充分条件 \( A_\delta < 1 \)（式16），其中 \( A_\delta \) 包含 MLP 权重积、注意力结构参数等。
- **闭环稳定性**：
  - **收缩度量序列**：基于终端代价构造无穷时域 DARE 得到终端收缩度量 \( M_{T|k} \)，再逆推 DRE 得到时变度量序列 \( M_{i|k} \)，利用黎曼几何度量状态距离。
  - **互联动态**：将物理系统与 Transformer 耦合，得到联合状态误差方程（式31），提取小增益条件 \((1-\rho_{mpc})(1-A_\delta) - \gamma_z \gamma_x > 0\)（式32），保证互联系统收缩。
  - **训练正则化**：将上述小增益条件作为软约束加入训练损失，强制网络满足理论界。

## 3. 实验设计：数据集 / 场景、基准、对比方法

- **实验场景**：非线性 3D 无人机模型（13维状态，4维控制输入），执行目标到达和避障机动任务。
- **基准方法**：未正则化的“vanilla” baseline，按照 Romero et al. (2024) 的 Actor-Critic MPC 实现（使用 MLP 预测 MPC 代价参数）。
- **对比**：本文提出的稳定 Transformer-Actor-Critic MPC（正则化版本） vs. vanilla 网络（无收缩约束）。
- **实验设置**：控制时域 T=10，注入严重均匀观测噪声 \( n_k \sim U(-0.4,0.4)^{12} \) 和过程扰动 \( d_k \sim U(-0.5,0.5)^{12} \)，每个场景运行 10 个随机验证 episode。

## 4. 资源与算力

- 论文中未明确说明使用的 GPU 型号、数量、训练时长等算力信息。仅提及使用了可微 MPC 和 Transformer 网络，训练过程涉及梯度反向传播，但未提供具体计算资源细节。

## 5. 实验数量与充分性

- **实验数量**：一共两个主要场景（无避障和避障），每个场景运行 10 次随机 episode。此外，还对比了跟踪误差的均值和标准差、碰撞率、姿态跟踪误差。
- **充分性**：
  - 实验设计相对简洁，仅对比了是否施加正则化约束的版本，缺乏与其他方法（如其他序列模型或安全增益方法）的对比。
  - 没有进行消融实验（例如单独验证 δISS 约束的效果、不同扰动强度的影响等）。
  - 数据集单一（仅仿真无人机），规模有限。
  - 实验次数（10 次）尚可，但统计稳定性一般，未提供置信区间或更多分布特征。
- **客观性与公平性**：对比基线是标准的未正则化版本，测试条件一致（相同噪声和扰动分布），因此相对公平。然而，基线方法本身并非最先进的安全控制方法，且仅在一种模型上验证，泛化性有限。

## 6. 论文的主要结论与发现

- **稳定性保证**：证明了 Transformer 网络可以满足全局 δISS，并推导出互联闭环系统的收缩条件。
- **鲁棒性能**：在严重噪声和扰动下，提出的稳定网络在无避障和避障场景中均收敛到目标，且碰撞率 0%；而 vanilla 网络在避障场景碰撞率达 30%，并出现位置发散和飞行不稳定。
- **姿态跟踪**：稳定网络保持有界姿态误差（标准差约 0.12 rad），但 vanilla 网络在发散前瞬态姿态跟踪更紧，表明严格的全局收缩边界限制了瞬态机动灵活性。

## 7. 优点

- **理论深度**：首次将 Transformer 的 δISS 特性与黎曼收缩理论结合，为基于 Transformer 的 Actor-Critic MPC 提供了严格的闭环稳定性证明，这是现有工作缺乏的。
- **可实施性**：将理论条件（小增益不等式）转化为训练正则化项，可直接集成到标准学习框架中，无需离线验证或复杂约束处理。
- **实验验证**：在具有挑战性的高噪声 / 扰动条件下进行测试，验证了理论保证在实际中的有效性，特别是碰撞率显著降低。
- **通用性潜力**：方法不依赖于特定动力学模型，可用于其他非线性系统。

## 8. 不足与局限

- **理论保守性**：使用全局均匀收缩率，导致瞬态机动灵活性受限。论文也提及未来需探索状态依赖的收缩度量以缓解该问题。
- **实验覆盖不足**：仅在一个无人机模型上测试，缺乏不同系统（如机器人臂、汽车）的验证；未与其他安全控制方法（如 CBF-MPC、鲁棒 MPC）对比；无消融实验，不能量化各组件贡献。
- **算力需求未说明**：未提供训练时间、GPU 资源等，影响复现性评估。
- **假设限制**：许多理论依赖的假设（如强凸性、层归一化 Lipschitz 常数下界、输入序列有界等）可能在实际中不严格成立，需要小心验证。
- **泛化风险**：实验所用的噪声和扰动分布固定，未测试对其他类型干扰的鲁棒性。此外，仅 10 次 episode，统计置信度有限。

（完）

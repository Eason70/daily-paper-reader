---
title: Control Barrier Function only Formation Tracking in Multi-Agent Systems
title_zh: 多智能体系统中基于控制障碍函数的编队跟踪
authors: "S. Saharsh, Pushpak Jagtap"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25452v1"
tags: ["query:safe-rl-cbf"]
score: 9.0
evidence: 基于控制屏障函数的多智能体编队跟踪
tldr: 多智能体系统编队跟踪面临参数调节复杂和需额外标称控制器的问题。本文提出一种基于单个控制障碍函数约束的二次优化框架，利用邻居相对信息实现实时控制，无需手动调参。在移动编队仿真中验证了领导者-跟随者框架的有效性。该方法简化了异构非线性多智能体系统的编队跟踪控制，提升实用性与实时性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25452v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 520, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25452v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 851, \"height\": 798, \"label\": \"Figure\"}]"
motivation: 现有编队跟踪方法需要手动调节参数或依赖单独的标称控制器，缺乏实时自适应能力。
method: 将单个控制障碍函数约束嵌入二次规划，基于邻居相对信息设计领导者-跟随者实时控制器。
result: 移动编队仿真验证了所提框架能有效实现编队跟踪，无需参数调整或额外标称控制器。
conclusion: 该框架简化了异构非线性多智能体系统的编队跟踪控制，具有良好的实时性和适应性。
---

## 摘要
本文提出了一种针对具有非线性动力学的异构多智能体系统编队跟踪的实时控制框架。该方法在二次优化设置中构建了一个类控制障碍函数约束，以处理编队跟踪问题。控制器依赖邻居智能体的相对信息，无需手动参数调整或单独的名义编队控制器。通过移动编队的仿真验证了领导-跟随框架的有效性。

## Abstract
This paper presents a real-time control framework for formation tracking of heterogeneous multi-agent systems with non-linear dynamics. The proposed method formulates a single Control Barrier Function-like constraint within a quadratic optimization setting that addresses formation tracking. Relying on the relative information of neighboring agents, the controller is designed to operate without the need for manual parameter tuning or a separate nominal formation controller. The leader-follower framework is validated through simulations of moving formations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：异构非线性多智能体系统（MAS）在领导-跟随框架下的移动编队跟踪问题。现有方法通常需要手动调节控制器参数或依赖额外的标称编队控制器，这限制了系统的实时性和实用性。
- **整体含义**：本文旨在设计一种**无需手动调参、无需单独标称控制器**的分布式控制策略，仅利用邻居智能体的相对状态（如相对位置、方位向量）即可实现编队跟踪，提升部署的便捷性和对动态环境的适应能力。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将编队跟踪任务转化为**单个类控制障碍函数（reach-CBF）约束**，并嵌入二次规划（QP）优化框架中。通过该约束同时保证**安全集的前向不变性**和**目标集在有限时间内的可达性**，从而替代传统的控制李雅普诺夫函数（CLF）与CBF联合设计。
- **关键技术细节**：
  - **系统建模**：每个智能体建模为平面机器人，形如 $\dot{p}_i = v_i$, $\dot{v}_i = f_{v_i}(p_i,v_i) + h_{v_i}(p_i,v_i)u_i$，包含双积分器、差动驱动等非线性动力学。
  - **图通信框架**：采用无向图 $G$，定义方位拉普拉斯矩阵 $B$，并基于**无限小方位刚性**假设（$B_{ff}$非奇异）保证编队唯一性。
  - **局部子问题分解**：每个跟随者至少有2个参考节点，通过定义**方位导向目标点** $p_{iT}$（由两条非平行方位线的交点计算），将全局编队任务分解为局部子任务。
  - **reach-CBF设计**：构造关于速度方向与目标方向夹角的函数 $b_i(\hat{v}_i, \hat{p}_{iT}) = \langle \hat{v}_i, \hat{p}_{iT} \rangle - \cos(\theta_i^\circ)$，定义安全集 $X_i = \{ (\hat{v}_i, \hat{p}_{iT}) : b_i \ge 0 \}$ 和目标集 $R_i = \{ b_i \ge \mu_i \}$。推导出线性约束 $K_f + K_h^\top u_i \ge \mu_i / \tau_i$。
  - **QP控制器**：优化目标为最小化控制输入与标称速度跟踪器 $u_{iv}$ 的偏差，约束为上述reach-CBF约束。标称控制器 $u_{iv}$ 通过李雅普诺夫设计保证位置和速度误差渐近收敛。
- **算法流程**：每个跟随者实时测量邻居相对位置 → 计算 $p_{iT}$ → 求解QP（式(17)）得到 $u_i$ → 施加到系统。无需任何手动参数调整（仅需常量 $\gamma_i$, $\tau_i$ 等）。

### 3. 实验设计：使用的数据集/场景、基准、对比方法

- **场景**：2D平面中，8个智能体（2个领导者，6个跟随者），通信图包含12条边，跟随者动力学异构（3个双积分器，3个差动驱动/阿克曼驱动）。领导者沿预定直线路径移动（恒定速度）。
- **初始条件**：给出各个智能体的初始位置、速度、朝向。
- **基准/对比方法**：**未明确对比其他方法**。论文仅展示了所提方法的仿真结果，无基线算法或消融实验对比。
- **评价指标**：最终方位误差 $\epsilon_i = |N_i| - \sum \langle g_{ij}, g^d_{ij} \rangle$，所有跟随者误差数量级在 $10^{-4} \sim 10^{-5}$。

### 4. 资源与算力

- **未说明**：论文未提及任何GPU型号、数量、训练时长或算力资源。仿真时应为简单的数值模拟，计算开销主要集中在每个时间步求解2维QP上。

### 5. 实验数量与充分性

- **实验数量**：仅**一组仿真**（单场景、单次运行），未进行多次随机初始化、不同图拓扑、不同动力学参数、不同领导者轨迹或加入扰动/噪声的实验。
- **充分性判断**：不够充分。实验仅验证了所提方法的可行性，但缺乏与现有方法的定量比较、参数敏感性分析、鲁棒性测试以及规模化（更多智能体）验证。评价指标只有最终方位误差，未展示收敛时间、控制能耗、避碰性能等。因此实验的客观性和公平性有限，存在过拟合于特定设置的风险。

### 6. 论文的主要结论与发现

- 提出的**单个reach-CBF约束**嵌入QP框架，能够成功实现异构多智能体系统的移动编队跟踪，**无需手动参数调整或额外标称控制器**。
- 仿真中所有跟随者最终方位误差均低于 $2 \times 10^{-4}$，证明了方法的有效性。
- 控制设计巧妙地将**速度方向控制（通过CBF约束）** 与**速度大小控制（通过标称反馈）** 解耦，避免了竞争目标。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：提出reach-CBF概念，同时保证安全集的向前不变性和目标集在有限时间内可达，统一了编队跟踪和安全性（此处安全指任务一致性，非避碰）。
- **实用性强**：仅需相对方位信息，适用于GPS拒绝环境；无需标称控制器和手动调参，降低部署难度。
- **分布式执行**：每个智能体仅依赖局部邻居信息，可扩展性好。
- **理论严谨**：提供了基于李雅普诺夫的收敛性证明和基于reach-CBF的有限时间可达性证明。

### 8. 不足与局限

- **实验覆盖严重不足**：仅一组仿真，无与现有方法（如标准CLF-CBF、基于Lyapunov的编队控制）的对比，缺乏消融实验、不同图结构、不同动力学模型、噪声、通信时延等测试。
- **安全定义狭隘**：论文中的“安全”仅指速度方向与目标方向一致，未考虑**避碰**。对于多智能体系统，碰撞避免是核心安全需求，本文缺失。
- **假设较强**：需要无限小方位刚性（至少2个领导者且有非平行方位线）、需要$B_{ff}$非奇异、需要anti-alignment非退化（测度零情况）。实际系统中可能难以始终满足。
- **规模性未验证**：仅8个智能体，对于大规模群体（如数十个）的可伸缩性未知。
- **未考虑动态障碍物**：文中提到“未来工作”涉及加速障碍，但当前方案未处理环境障碍，限制了实际应用。

（完）

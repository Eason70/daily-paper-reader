---
title: Conformal Recovery-Deadline Certificates for Runtime Assurance of Adapting Controllers
title_zh: 适应控制器运行时保证的共形恢复截止时间证书
authors: Alireza Shojaei
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25371v1"
tags: ["query:ocean-robot"]
score: 9.0
evidence: 基于保形证书的运行时保证用于安全关键系统
tldr: 运行时保证RTA通过切换到安全控制器保护系统，但标准规则会错误抑制正在恢复的自适应控制器。本文提出共形恢复截止证书，利用分裂共形预测给出恢复时间的有限样本上界，允许延迟切换并由验证监视器后援。该证书有效区分有能力和无能力的控制器，在航天器和倒立摆上消除抑制病理。它是可靠性非对称设计的通用机制，分离了自治统计覆盖与安全验证后援。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25371v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1828, \"height\": 773, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25371v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 871, \"height\": 699, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25371v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1080, \"height\": 213, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25371v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1380, \"height\": 228, \"label\": \"Table\"}]"
motivation: 标准latching规则会抑制自适应控制器正常的恢复瞬态，需要一种能区分能力与无能控制器的新证书。
method: 提出分裂共形恢复截止证书，分布自由地给出恢复时间上界，带有统计覆盖保证，并由验证监视器在硬临界极限后援。
result: 在6-DOF航天器姿态控制与扭矩倒立摆上验证，证书有效消除抑制病理，正确区分适应性与发散性控制器。
conclusion: 该证书作为领域通用机制，实现了自治与安全的可靠性非对称分离，可扩展至其他安全关键系统。
---

## 摘要
运行时保证（RTA）通过在监测条件违反时从先进控制器切换到经过验证的安全控制器来保护安全关键系统。标准的锁存规则在首次违反安全集合时触发并滑行，对于发散控制器是正确的，但对于有能力的在线自适应控制器则是病态的。这种控制器在设计上在有限恢复瞬态期间是不安全的。它必须激励被控对象以识别故障，然后才能纠正故障，因此锁存屏蔽会在该瞬态触发并抑制本可以恢复的控制器。我们引入了共形恢复截止时间证书，这是一种分裂共形、无分布、有限样本的适应控制器恢复时间上界，该上界允许延迟回退并带有覆盖保证，并由硬临界极限处的验证监视器提供后备支持。认证的截止时间区分了有能力和无能力的控制器，在保持恢复器自主的同时捕捉发散器。该结构将自主性（由统计覆盖控制）与安全性（由验证后备支持）分离，作为可靠性非对称设计的一个实例。我们证明了边际覆盖、在已知故障分布偏移下恢复覆盖的加权扩展，以及分组条件蒙德里安覆盖。我们在两个不相关的Simplex试验台上演示了所有三种情况：一个六自由度航天器姿态控制器和一个扭矩控制倒立摆。两者都显示出相同的抑制病理和相同的治疗方法，使得该证书成为一种领域通用机制而非单一系统技巧。

## Abstract
Runtime assurance (RTA) protects a safety-critical system by switching from an advanced controller to a verified safe controller when a monitored condition is violated. The standard latching rule, which trips on the first breach of the safe set and then coasts, is correct for a diverging controller but pathological for a capable online-adapting one. Such a controller is unsafe by design during a bounded recovery transient. It must excite the plant to identify the fault before it can correct it, so a latching shield trips on that transient and suppresses a controller that would have recovered. We introduce the conformal recovery-deadline certificate, a split-conformal, distribution-free, finite-sample upper bound on the adapting controller's recovery time that licenses delayed fallback with a coverage guarantee, backstopped by a verified monitor at a hard critical limit. The certified deadline discriminates capable from incapable controllers, keeping the recoverer autonomous while catching the diverger. The construction separates autonomy, governed by statistical coverage, from safety, governed by the verified backstop, as an instance of reliability-asymmetric design. We prove marginal coverage, a weighted extension that restores coverage under a known fault-distribution shift, and group-conditional Mondrian coverage. We demonstrate all three on two unrelated Simplex testbeds: a 6-DOF spacecraft attitude controller and a torque-controlled inverted pendulum. Both show the same suppression pathology and the same cure, making the certificate a domain-general mechanism rather than a single-system trick.

---

## 论文详细总结（自动生成）

# 论文《Conformal Recovery-Deadline Certificates for Runtime Assurance of Adapting Controllers》详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：运行时保证（RTA）通过“锁存规则”保护安全关键系统：一旦安全集边界被违反，立即切换到已验证的安全控制器并永久锁定。对于固定控制器（发散的）这是正确的；但对于**在线自适应控制器**，这种规则是**病态的**。自适应控制器在故障识别阶段必须暂时离开安全集（“恢复瞬态”）以激励系统获取故障信息，而锁存屏蔽会在该瞬态触发并永久抑制本可以恢复的控制器，甚至剥夺其所需的激励信号，将潜在的自愈者转化为永久的安全保持。
- **整体含义**：解决该矛盾的关键不是改进瞬态监视器，而是**认证恢复时间**——区分有能力（有限恢复时间）与无能力（无限恢复时间）控制器的量。论文提出**共形恢复截止时间证书**，用分布自由、有限样本的统计方法给出恢复时间上界，允许延迟切换，同时由已验证的硬临界极限监视器后援安全性，实现**可靠性非对称设计**：自主性由统计覆盖保证，安全性由形式化验证保证，两者分离。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：用**分裂共形预测**在离线校准集上构造恢复时间τ的截断时间上界dα，并在运行时按照“状态未达硬临界且未超过截止时间”的规则保留自适应控制器。证书只依赖恢复时间排序分位数，无分布假设，且当恢复率不足时诚实地返回+∞，拒绝签发合理截止时间。
- **关键技术细节**：
  - **恢复时间定义**：τ = 最后离开安全集的时间步+1（若在全观测时长H内再未违反），若H时仍违反则τ=+∞。τ是轨迹级函数，非停止时间，但共形预测只需排序。
  - **截止时间构造**：校准集的n个τ排序，dα = τ_(⌈(1-α)(n+1)⌉)，若该索引>n则dα = +∞。
  - **运行时规则**：安全集S，硬临界Φc。自适应控制器保持直至：(a) e_t ≥ Φc (验证后援触发) 或 (b) t ≥ dα 且未恢复。任一条件满足即切换到安全控制器。
  - **理论保证**：
    - **边际覆盖**（定理1）：P(τ_{n+1} ≤ dα) ≥ 1-α，基于可交换性。
    - **加权覆盖**（定理2）：当部署分布Q≠校准分布P，且已知似然比w = dQ/dP时，重新加权校准样本后得到加权截止时间，恢复保证。对运行时固定截止时间建议用平均权重，经验验证。
    - **分组（Mondrian）覆盖**（定理3）：按故障类分组校准各得截止时间，保证每类内覆盖，样本不足时返回+∞。
  - **可靠性非对称设计**：统计误差只影响自主性（保留控制器的概率），安全性由验证后援独立保证，因此证书可安全用于安全关键回路。

## 3. 实验设计
- **场景1：6-DOF航天器姿态控制**（复杂、高保真）
  - 平台：Basilisk模拟器，反应轮故障（增益反转）。
  - 安全集：5°指向误差；硬临界Φc = 90°，用Kind 2验证监视器。
  - 自适应控制器：快速电机适应控制器（恢复率99.8%，中位恢复119步，p95=162步）。
  - 校准：300次随机校准/测试分裂，α∈{0.20, 0.10, 0.05}。
  - 对比基线：启发式 p95×1.3（无保证、不可调）。
- **场景2：扭矩控制倒立摆**（纯NumPy、确定性）
  - 故障：执行器增益/符号变化（反转导致诊断瞬态）。
  - 安全集5°，硬临界45°，模拟噪声速率传感。
  - 校准200个故障，测试分裂。
  - 对比基线：锁存Simplex、启发式 p95×1.3、CP-on-safety-value [8]、恢复截止证书（本文）。
  - 额外实验：已知分布偏移（校准30%反转，测试70%反转，似然比精确），测试加权恢复。Mondrian分组（良性类vs反转类）。
- **评估指标**：边际覆盖（多分裂平均值）、自主性保留（适应控制器未被打断的比例）、发散捕获率、平分捕获时间。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量或训练时长。航天器实验基于“Paper A’s reproduced manifest”（可能为仿真），倒立摆是“纯NumPy和确定性”实现。两域都涉及离线校准集（数百个故障样本），计算开销极低（仅排序分位数），无需大规模训练。未提及任何GPU资源。

## 5. 实验数量与充分性
- **数量**：
  - 域1：300次随机校准/测试分裂（3个α各100次？未明确但整体300次），报告均值和覆盖率。
  - 域2：类似，多次随机分裂，报告覆盖率、自主性；另有一次偏移实验、一次Mondrian分组实验。
  - 对比了3～4种基线（锁存、启发式、CP-on-value）。
  - 有消融：不同α（0.20/0.10/0.05）及对应的覆盖、自治率。
- **充分性**：
  - 使用了两个完全不相关的试验台（航天器和倒立摆），证明方法通用性。
  - 验证了边际覆盖、加权覆盖、分组覆盖三种理论保证。
  - 基线对比全面，包括无保证的启发式和另一种共形方法（CP-on-value）。
  - 实验设计客观：校准/测试分裂独立随机，报告均值而非最佳值，并讨论抽样误差。
  - 不足：未对统计显著性进行假设检验（如t检验或置信区间），但理论下界已是有限样本保证。未在更多场景（如实际机器人硬件）验证。

## 6. 论文的主要结论与发现
- **病理确认**：标准的锁存Simplex在航天器上将99.8%恢复能力的控制器抑制到<0.5%自治率；在倒立摆上仅0.5%自治。这是结构性问题。
- **证书疗效**：恢复截止证书在α=0.1时，航天器恢复94.5%自治，倒立摆恢复83.7%自治（Mondrian的逆类覆盖），同时所有发散控制器均在硬临界前捕获（100%安全）。
- **跨域一致**：两系统无共享动力学，但病理和治愈完全相同，证明证书是领域通用机制。
- **覆盖保证**：实测覆盖贴近目标（航天器0.808/0.902/0.950对应1-α=0.8/0.9/0.95；倒立摆类似）。
- **偏移下有效**：在已知分布偏移下，加权证书将覆盖从86.0%恢复到93.5%（高于90%目标），而无加权的覆盖失败。
- **分组正确**：Mondrian证书对良性类返回d=0（永不违反），对反转类返回37步，拒绝混合。
- **可靠性非对称设计**：安全由验证后援独立保证，统计错误只影响自治，证书可安全用于安全关键回路。

## 7. 优点：方法与实验设计的亮点
- **方法亮点**：
  - 创造性定义恢复时间为最后退出时间+1，自然处理+∞（掩盖无法恢复的情况）。
  - 使用分裂共形预测，只需可交换性，无分布假设，实现简单（一次排序分位数）。
  - 诚实性：恢复率不足时返回+∞而非虚构有限截止时间。
  - 可靠性非对称设计：自治与安全完全分离，失败模式不重叠，这是将统计方法嵌入安全关键系统的关键设计模式。
  - 支持三种覆盖（边际、加权、分组），适应不同实践需求。
- **实验亮点**：
  - 两个完全不同域的验证（航天器 vs. 倒立摆），强证明通用性。
  - 精确控制偏移程度（倒立摆似然比精确），干净验证加权定理。
  - 对比基线全面（包括锁存、启发式、另一共形方法），注意去掉无用的反向切换（因开环不稳定）。
  - 实验可重复：提供代码与数据文件。

## 8. 不足与局限
- **实验覆盖**：
  - 仅两个仿真场景，缺少真实机器人硬件或在飞行器上的验证。实际噪声、模型误差可能影响恢复时间分布。
  - 未测试α<0.05时（如α=0.01）的样本量要求（n≥99），可能无法获得有限截止时间。
  - 加权方法需要已知似然比，实践中可能需要估计，估计误差对覆盖的影响未分析。
  - 仅测试了一种分布偏移（反转率从30%到70%），其他类型偏移未涉及。
- **偏差风险**：
  - 边际覆盖保证可能对特定子类欠覆盖，论文已承认（反转类仅83.7% vs 目标90%），并提供了Mondrian补救，但Mondrian需每类足够样本，某些罕见类可能永远无法认证。
  - 证书依赖离线校准集的可交换性假设；若部署时故障分布与校准分布差异巨大且未知，边际覆盖可能严重偏离。此时只有安全后援保证，自治可能变得很差（退化为锁存）。
- **应用限制**：
  - 证书是针对单个控制器和单个故障分布校准的，更换控制器或故障模式需要重新校准。
  - 对于无恢复瞬态的控制器（如从不离开安全集），证书返回d=0（因为τ=0），但规则允许永远留用，这没问题。
  - 恢复时间定义基于最后退出，要求全轨迹可知，但运行时做决定时无法预知未来。论文指出在线规则是保守的（可能在控制器暂时恢复后再次违反时提前切换），但不会影响安全。
  - 未讨论证书对实时性的影响（计算极轻量，不是问题），但未评估校准集的获取成本（需收集足够多的故障恢复轨迹）。

（完）

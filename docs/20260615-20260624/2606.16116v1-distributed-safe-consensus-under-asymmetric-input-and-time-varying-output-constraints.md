---
title: Distributed Safe Consensus Under Asymmetric Input and Time-Varying Output Constraints
title_zh: 非对称输入与时变输出约束下的分布式安全一致性
authors: "Abhinav Sinha, Shashi Ranjan Kumar"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16116v1"
tags: ["query:safe-rl-cbf"]
score: 9.0
evidence: 使用障碍坐标变换的分布式安全一致性
tldr: 针对单积分多智能体系统在非对称执行器约束和时变输出安全约束下的分布式一致性问题，引入障碍坐标变换将输出安全约束转化为无约束形式，设计融合图协调与执行器跟踪的双层控制器。闭环系统所有信号有界，执行器输入严格在非对称界内，输出始终处于安全区间，同步误差指数收敛，输出渐近同步到可容许轨迹。首次同时处理非对称输入和时变输出约束，确保安全性和一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16116v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16116v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1794, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16116v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1792, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16116v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1794, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16116v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1879, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16116v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1785, \"height\": 509, \"label\": \"Figure\"}]"
motivation: 解决同时存在非对称执行器约束和时变输出安全约束的分布式一致性问题。
method: 引入障碍坐标变换处理时变输出安全约束，设计融合图协调与执行器跟踪的双层控制律。
result: 闭环系统信号有界，输入严格在非对称界内，输出始终安全，同步误差指数收敛，输出渐近同步。
conclusion: 所提框架有效实现非对称输入和时变输出约束下的安全一致性。
---

## 摘要
本文研究了在连通无向图上，单积分多智能体系统在同时存在非对称执行器约束和输出安全约束时的分布式安全一致性问题。每个智能体配备一个连续可微的非对称执行器动态，该动态将指令控制信号映射到实际实现的控制输入，同时确保后者严格保持在预设的允许区间内。为了解决输出安全问题，引入了一个基于共同时变安全区间的障碍坐标变换，并在变换后的坐标中设计了一种分布式同步律。所得到的控制器将基于图的协调层与执行器侧的跟踪层相结合，从而能够同时保证输入的可容许性、安全输出集的前向不变性以及渐近同步。对于紧致的初始条件容许集，证明了闭环解是完整的，所有信号保持有界，执行器输入始终严格在其非对称界限内，并且智能体输出始终保持在预设的安全区间内。此外，变换后的同步误差指数收敛到零，原始智能体输出渐近同步到设计者选择的嵌入在共同安全区间内的容许轨迹。数值仿真验证了所提框架，并展示了在非对称执行器界限和时变输出约束下的安全一致性。

## Abstract
This paper studies safe distributed consensus for single-integrator multi-agent systems over connected undirected graphs under simultaneous asymmetric actuator constraints and output safety constraints. Each agent is equipped with a continuously differentiable asymmetric actuator dynamics that maps a commanded control signal to the realized plant input while keeping the latter strictly inside a prescribed admissible interval. To address output safety, a barrier-coordinate transformation is introduced over a common time-varying safe interval, and a distributed synchronization law is designed in the transformed coordinates. The resulting controller integrates a graph-based coordination layer with an actuator-side tracking layer, thereby enabling simultaneous enforcement of input admissibility, forward invariance of the safe output set, and asymptotic synchronization. For compact admissible sets of initial conditions, it is shown that the closed-loop solution is complete, all signals remain bounded, the actuator inputs remain strictly within their asymmetric bounds, and the agent outputs remain inside the prescribed safe interval for all time. Moreover, the transformed synchronization errors converge exponentially to zero, and the original agent outputs asymptotically synchronize to a designer-selected admissible trajectory embedded in the common safe interval. Numerical simulations validate the proposed framework and demonstrate safe consensus under both asymmetric actuation bounds and time-varying output constraints.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：实际多智能体系统中，执行器通常具有非对称的有限能力（例如车辆加速与刹车能力不同、航空器的正负推力限制不等），同时许多安全关键任务要求每个智能体的输出（状态）始终约束在时变的允许区间内（如操作包线、环境限制）。现有分布式一致性文献要么忽略执行器约束或仅考虑对称饱和，要么将输出安全与一致性目标分开处理，缺乏一个统一的框架同时保证非对称输入可容许性、时变输出安全性和渐近一致性。
- **核心问题**：针对单积分器多智能体系统（在连通无向图上），设计分布式控制器，使得在同时存在非对称执行器约束和时变输出安全约束时，闭环系统满足：解完整、所有信号有界、执行器输入严格在非对称界内、输出始终在安全区间内、渐近同步到设计者指定的容许轨迹。
- **整体含义**：首次在单一闭环分析中同时处理非对称输入约束和时变输出安全约束，提供了一种可验证的、半全局的框架，为安全关键网络化自主系统提供理论基础。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  1. 将执行器建模为连续可微的非对称动态（公式2-3），使得实际输入 \(u_i\) 始终严格在容许区间 \((u_i, \overline{u}_i)\) 内，其正则化因子 \(\sigma_i(u_i)\) 仅在边界为零。
  2. 针对时变输出安全约束，引入一个共同时变的核心安全区间 \(\Omega(t)=(\underline{\xi}(t),\overline{\xi}(t))\)（所有智能体输出必须位于此区间内）。通过障碍坐标变换 \(z_i = \ln\frac{x_i-\underline{\xi}(t)}{\overline{\xi}(t)-x_i}\)（公式38），将输出安全约束（有界区间）转化为无约束的实数坐标。
  3. 设计双层控制器：
     - **协调层**：在障碍坐标下设计带有钉扎项的分布式同步律 \(\beta_i\)（公式45），使所有 \(z_i\) 同步到预设容许轨迹 \(z^*(t)\)，从而保证原始输出同步到 \(x^*(t)\in\Omega(t)\)。
     - **执行器跟踪层**：定义虚拟输入 \(\alpha_i = (\beta_i - d_i)/b_i\)（公式46），其中 \(b_i, d_i\) 由变换导出。再定义跟踪误差 \(\varepsilon_i = u_i - \alpha_i\)，设计指令输入 \(v_i\)（公式48）使 \(\dot{\varepsilon}_i = -c_i\varepsilon_i\) 指数稳定。
  4. 利用 Lyapunov 方法证明：在增益条件（如 \(c_{\min} > \frac{\bar{b}_K^2}{2\kappa}\) 和内部性条件 \(r_{i,K} < \bar{r}_i\)）下，闭环系统满足所有要求。

- **关键技术细节**：
  - 执行器动态：\(\dot{u}_i = p_{1,i}\sigma_i(u_i)v_i - p_{2,i}u_i\)，其中 \(\sigma_i(u_i)\) 在边界为零。
  - 障碍坐标变换是 \(\Omega(t)\) 到 \(\mathbb{R}\) 的 \(C^1\) 微分同胚，其逆变换（公式40）显式给出。
  - 针钉项 \(-\kappa(z_i - z^*(t))\) 移除中性模式，保证同步到指定参考轨迹。
  - 条件（15）和（57）确保执行器输入始终在严格内部子区间内，避免 \(\sigma_i\) 为零。

- **算法流程**（文字描述）：
  1. 设定共同时变安全核心 \(\Omega(t)\) 及设计者指定的容许参考轨迹 \(x^*(t)\in\Omega(t)\)。
  2. 计算变换后的参考 \(z^*(t)\)。
  3. 每个智能体测量自身状态 \(x_i\)、邻居相对状态，计算障碍坐标 \(z_i\)。
  4. 生成协调层信号 \(\beta_i\)（公式45），再结合 \(b_i, d_i\) 计算虚拟输入 \(\alpha_i\)。
  5. 定义跟踪误差 \(\varepsilon_i = u_i - \alpha_i\)，利用执行器动态模型设计指令 \(v_i\)（公式48）。
  6. 闭环系统自动执行，保证安全性、容许性和同步性。

## 3. 实验设计

- **数据集/场景**：未使用真实数据集。作者设计了数值仿真场景：
  - **图结构**：连通无向环图，5个智能体（主要实验）或4个智能体（常值参考实验）。
  - **执行器参数**：非对称异质界限，不同智能体 \(u_i\) 和 \(\overline{u}_i\) 各不相同（见表中数值）。
  - **安全核心**：时变非对称区间，由公式(93)-(96)定义，具有不同下/上间隙。
  - **预设参考轨迹**：通过归一化位置 \(\rho(t)\in(0,1)\) 生成 \(x^*(t)=\underline{\xi}(t)+\rho(t)(\overline{\xi}(t)-\underline{\xi}(t))\)。测试了三种 \(\rho(t)\)：
    - 多频率参考：\(\rho_{\text{mf}}(t)=0.5+0.22\sin(0.18t)+0.12\sin(0.73t+0.6)\)
    - 偏置正弦参考：\(\rho_s(t)=0.65+0.25\sin(0.3t)\)
    - 平滑过渡参考：\(\rho_p(t)=0.75+0.08\tanh(0.05(t-20))+0.02\sin(0.15t)\)
  - 额外实验：常值障碍坐标 \(z^*=0\) 导致 \(x^*(t)=(\underline{\xi}(t)+\overline{\xi}(t))/2\)，4个智能体。
- **Benchmark**：未与现有方法进行定量对比。论文仅在引言中与漏斗控制、预设性能方法等进行定性比较，指出差异。
- **对比方法**：无。论文主要验证自身框架的有效性。

## 4. 资源与算力

- 论文中**未提及任何计算资源**（GPU型号、数量、训练时长等）。仿真在普通计算机上即可完成，因为系统是连续时间ODE，使用数值积分即可。未涉及深度学习训练。

## 5. 实验数量与充分性

- **实验数量**：共展示了4组主要仿真：
  1. 多频率参考（图2）
  2. 偏置正弦参考（图3）
  3. 平滑过渡参考（图4）
  4. 常值障碍坐标/时变核心（图6）
- **是否充分**：
  - **充分性**：覆盖了三种不同动态特性的参考轨迹（复杂振荡、简单振荡、阶跃过渡），以及一种特殊情形（常值障碍坐标），足够验证方法的关键特性（安全、容许性、同步）。
  - **客观性**：所有初始条件均在紧致容许集内，增益满足理论条件，结果符合理论预测。但缺乏与替代方法的定量对比，没有消融实验（如改变增益、图结构、执行器参数等的影响）。
  - **公平性**：没有不公平的对比问题，但自身对比不够丰富（如不同控制器参数的影响、不同图拓扑的影响等未展示）。

## 6. 主要结论与发现

- 所提出的分布式控制器（公式48）在满足增益和内部性条件（56-57）时，能同时实现：
  - 闭环解完整，所有信号有界。
  - 执行器输入始终严格在非对称容许区间内（由 \(\sigma_i>0\) 保证）。
  - 智能体输出始终在共同时变安全核心 \(\Omega(t)\) 内，且离边界有正间距（前向不变性）。
  - 变换后的同步误差 \(\zeta\) 和执行器跟踪误差 \(\varepsilon\) 指数收敛到零。
  - 原始输出渐近同步到设计者指定的容许轨迹 \(x^*(t)\)，并由此达成一致。
- 数值仿真验证了多频率、偏置正弦、平滑过渡以及常值参考情形下的有效性，展示了安全间距和执行器正则化因子始终为正。

## 7. 优点

- **方法创新**：首次在分布式一致性框架中同时处理非对称执行器约束和时变输出安全约束，将两者作为控制律的组成部件而非事后补偿。
- **理论基础严谨**：提供完整的Lyapunov分析，明确给出半全局条件下的显式增益条件和内部性条件，避免了先验有界假设。
- **实用性强**：控制器形式为分布式，每个智能体仅需自身和邻居状态信息，易于实现。
- **结果强于普通一致**：通过钉扎项，系统同步到设计者指定的容许轨迹而非未知极限值。
- **仿真验证充分**：针对不同参考轨迹类型展示了系统行为，并给出安全裕度和正则化因子的图形化验证。

## 8. 不足与局限

- **实验局限性**：
  - 没有与任何现有方法（如漏斗控制、控制障碍函数 + 饱和补偿等）进行定量对比，无法量化改进程度。
  - 缺少消融研究（如不同图结构、不同增益取值、不同非对称程度）对性能的影响。
  - 缺少对高维系统或更复杂模型（如双积分器、非线性）的扩展实验。
- **理论局限**：
  - 条件（15）和（57）采用对称内逼近保证非对称容许，可能保守（作者已指出）。
  - 半全局结果仅针对紧致初始集，参数选择依赖初始集半径。
  - 要求共同时变核心 \(\Omega(t)\) 严格在所有个体安全区间内部，且宽度有正下界，实际中可能难以保证。
  - 算法需要计算 \(z^*(t)\) 及其一、二阶导数，要求参考轨迹足够平滑。
- **其他**：
  - 未讨论通信延迟、丢包、切换拓扑等实际网络因素。
  - 未考虑模型不确定性或外部扰动。

（完）

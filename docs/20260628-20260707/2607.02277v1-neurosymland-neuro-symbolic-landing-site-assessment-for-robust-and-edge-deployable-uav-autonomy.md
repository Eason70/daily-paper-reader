---
title: "NEUROSYMLAND: Neuro-Symbolic Landing-Site Assessment for Robust and Edge-Deployable UAV Autonomy"
title_zh: NEUROSYMLAND：神经符号着陆点评估——用于鲁棒且可边缘部署的无人机自主性
authors: "Weixian Qian, Tianyi Yang, Sebastian Schroder, Yao Deng, Jiaohong Yao, Xiao Cheng, Richard Han, Xi Zheng"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02277v1"
tags: ["query:safe-rl-cbf"]
score: 8.0
evidence: 无人机安全着陆评估的神经符号推理
tldr: 针对无人机在非结构化环境中着陆点评估受地形变化影响大、透明性不足的问题，提出神经符号系统NEUROSYMLAND，结合轻量感知与显式安全推理。通过构建概率语义场景图，利用符号约束评估候选区域，在72个模拟场景中成功61次，优于基线方法。硬件在环试验验证了边缘部署可行性，提升了鲁棒性和可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02277v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 329, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02277v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 851, \"height\": 578, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02277v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 904, \"height\": 633, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02277v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 897, \"height\": 769, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02277v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 835, \"height\": 619, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02277v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 835, \"height\": 344, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02277v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 898, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02277v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 769, \"height\": 276, \"label\": \"Table\"}]"
motivation: 视觉学习方法在非结构化地形下性能退化且缺乏安全决策透明度，亟需鲁棒且边缘可部署的着陆点评估方案。
method: 构建概率语义场景图，并利用地形平坦度、障碍物间距和空间一致性等符号约束进行可解释的着陆区域评估。
result: 72个模拟场景中成功61次（基线37-57次），100次硬件在环试验显示符号推理仅占端到端延迟小部分。
conclusion: NEUROSYMLAND在保证鲁棒性和可解释性的同时，实现了边缘受限无人机硬件上的可行部署。
---

## 摘要
在非结构化环境中安全着陆点评估仍然是自主无人机部署的关键挑战，因为纯视觉学习方法常因地形变化而性能下降，且在安全决策方面透明度有限。我们提出了NEUROSYMLAND，一个将轻量级感知与显式安全推理相结合的神经符号着陆点评估系统。该框架从机载视觉输入构建概率语义场景图，并使用捕捉地形平坦度、障碍物间隙和空间一致性的符号约束来评估候选着陆区域，从而在感知不确定性下实现结构化推理，同时保持边缘可执行的可行性。在涵盖多种地形的72个模拟着陆场景中，NEUROSYMLAND实现了61次成功评估，优于四种对比基线（37-57次成功）。为评估可部署性，我们进一步进行了100次硬件在环试验，采用随机初始位姿，分析了端到端延迟、阶段执行时间以及CPU/GPU利用率、内存占用和功耗等系统级指标。结果表明，在边缘资源使用受限的情况下，鲁棒性和可解释性得到了提升。性能分析显示，符号推理仅占端到端延迟的一小部分，而主要计算成本来自感知和PSSG构建。这些结果证明了在边缘受限的无人机硬件上部署着陆点评估栈的可行性，所有源代码、数据集、提示词和符号规则优化示例均已发布在开源仓库中。

## Abstract
Safe landing-site assessment in unstructured environments remains a key challenge for autonomous UAV deployment, as vision-only learning approaches often degrade under terrain variability and provide limited transparency in safety decisions. We present NEUROSYMLAND, a neuro-symbolic landing-site assessment system that integrates lightweight perception with explicit safety reasoning. The framework constructs a probabilistic semantic scene graph from onboard visual input and evaluates candidate landing regions using symbolic constraints capturing terrain flatness, obstacle clearance, and spatial consistency, enabling structured reasoning under perceptual uncertainty while maintaining edge-feasible execution. Across 72 simulated landing scenarios spanning diverse terrains, NEUROSYMLAND achieves 61 successful assessments, outperforming four competitive baselines (37-57 successes). To evaluate deployability, we further conduct 100 hardware-in-the-loop trials with randomized initial poses, profiling end-to-end latency, stage-wise execution time, and system-level metrics including CPU/GPU utilization, memory footprint, and power consumption. Results demonstrate improved robustness and interpretability with bounded edge-resource usage. Profiling shows that symbolic reasoning contributes only a small fraction of end-to-end latency, while the main computational cost arises from perception and PSSG construction. These results demonstrate the feasibility of deploying the landing-site assessment stack on edge-constrained UAV hardware, and all source code, datasets, prompts, and symbolic rule refinement examples are released in an open-source repository

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：无人机在非结构化环境中的安全着陆点评估（LSA）是自主飞行的重要瓶颈。现有基于纯视觉学习的端到端方法（如 SegOpticalFlow、SafeUAV）在面对地形变化时性能退化，且缺乏决策透明性；而基于大模型（LLM/VLM）的方法虽能进行高层次推理，但运行时依赖黑盒推理，难以在边缘设备上进行确定性分析和认证。
- **整体含义**：需要一种既能保持感知鲁棒性、又能提供可解释、可审计、可重构决策的着陆评估框架，且能在计算资源受限的无人机边缘硬件上部署。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将世界建模与决策推理解耦。感知层仅负责构建显式的、可审计的**概率语义场景图（PSSG）**；决策层完全由**符号规则引擎（SCALLOP）** 驱动，无神经网络参与。
- **关键技术细节**：
  - **在线流程**（图2）：
    1. 使用 INT8 量化的 SegFormer-B0 骨干网络进行语义分割，配合轻量几何后处理提取候选区域及其属性（面积、平坦度、形状等）。
    2. 构建 PSSG：节点为分割区域（存储语义后验和几何属性），边为确定性空间关系（如包含、相邻、附近），并赋予概率权重。
    3. 在 SCALLOP 引擎上执行预验证的规则集，为每个候选区域推断 `hazard(x)` 和 `safe(x)`，并生成可追溯的证明路径。
    4. 多帧验证（窗口 T 帧）确保区域从未被推断为危险且几何稳定，通过硬门控过滤。
    5. 对通过验证的区域，根据任务模式（安全/紧急/救援）的加权效用函数 `Cm(v) = IT(v) Σ ω_{m,k} ̃b_{m,k}(v)` 进行排序，选出最优着陆点。
  - **离线规则构建**（图3）：采用**人机闭环（HITL）** 流程。LLM（OpenAI o3）仅作为辅助工具，根据固定 PSSG 模式、专家自然语言约束和验证失败案例，提议候选 SCALLOP 规则。专家审查规则触发的解释和误分类，迭代修改规则直至满足验证集。运行时无 LLM 调用。
  - **关键公式**：
    - 多帧验证：`Pass_T(v) = ∧_{t∈W_T} ¬hazard(v^{(t)}) ∧ ¬Jitter(v, W_T)`，`I_T(v) = 1{Pass_T(v)}`。
    - 任务排序：`C_m(v) = I_T(v) Σ_k ω_{m,k} ̃b_{m,k}(v)`，选择 `v^* = arg max_v C_m(v)`。
- **算法流程**（文字描述）：
  1. 输入单帧 RGB 图像 → 轻量分割 + 几何后处理 → 生成区域候选和属性。
  2. 构建 PSSG（节点、边、概率事实）。
  3. 执行 SCALLOP 规则（固定集）→ 对每个候选区域输出 `hazard` 得分和 `safe` 得分，同时生成证明（哪些规则和事实触发了推论）。
  4. 多帧窗口内累积判断 → 硬门控过滤不稳定或带有风险区域。
  5. 任务权重加权排序 → 输出最终建议着陆点。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：
  - 训练感知骨干使用 Semantic Drone Dataset（仅用于训练分割模型）。
  - 评估在 **AirSim 模拟器**上构建的 **72 个着陆场景**，涵盖多种地形（未说明具体地形分布，但文中提及“diverse terrains”）。
  - 规则构建使用有代表性的验证 PSSG（来自该数据集与合成边缘案例），评估在不相交的测试集上进行。
- **基准（Baseline）**：
  - SegOpticalFlow（光流法）
  - SafeUAV（学习型单目深度与安全区域预测）
  - PEACE（基于 CLIPSeg 的提示工程自动优化）
  - LLMExplain（Phi-3.5-Vision，本地 VLM 选择器）
  - **消融实验**：NEUROSYMLAND-DetFOL（将概率符号规则替换为确定性一阶逻辑）
- **硬件在环（HIL）试验**：100 次，在 **Jetson Orin Nano (8GB)** 上执行，使用 AirSim 输入记录。随机初始位姿和航向。

## 4. 资源与算力

- 论文中**未明确说明训练感知模型的 GPU 型号、数量及训练时长**。仅提到感知骨干（SegFormer-B0）是“independently trained”，且后续量化为 INT8 用于边缘部署。
- 边缘推理平台：NVIDIA Jetson Orin Nano (8GB)，量化的 INT8 模型。
- LLM 规则辅助（OpenAI o3）仅在离线阶段通过 ChatGPT 界面使用，运行时无大模型推理。
- 因此，算力开销主要集中在离线模型训练（未披露）和运行时边缘推理（约 1.0 FPS，CPU 76.9%，GPU 52%，内存 5.6 GB，功率 12.3W，温度 47.9°C）。

## 5. 实验数量与充分性

- **实验数量**：
  - 软件在环（SIL）：72 个场景 × 5 次运行（基线方法均 5 次），结果报告总成功数 Succ/72 和最小障碍距离 MOD（中位数/P20）。
  - 硬件在环（HIL）：100 次试验，统计中位数 ± 标准差（端到端延迟、阶段延迟、资源利用率）。
  - 消融实验：NEUROSYMLAND-DetFOL 在 SIL 上对比（58/71 vs. 61/72）。
  - 定性案例研究：2 个典型场景（鲁棒性 / 感知失败）及 1 个解释性案例（滑板公园）。
- **充分性评估**：
  - 优点：对比了 4 个有代表性的基线（含学习型、光流、VLM），并做了消融；HIL 验证了边缘部署可行性；考虑了不同任务模式的适应。
  - 局限性：所有实验均在模拟环境（AirSim）中进行，缺乏真实世界飞行验证；仅评估着陆点评估阶段，未集成完整的控制回路；场景动态有限（仅少数移动障碍）；感知质量作为上限，系统性语义交换导致所有方法失败。
  - 总体而言，实验设计较为充分，但模拟与真实之间的差距是主要风险。

## 6. 主要结论与发现

1. **鲁棒性**：NEUROSYMLAND 在 72 个模拟场景中成功评估 61 次，优于所有对比基线（PEACE 57次、SegOpticalFlow 53次、SafeUAV 47次、LLMExplain 37次），同时保持了更大的安全裕度（MOD 中位数 119.64 px vs. PEACE 84.38）。
2. **符号推理开销极小**：符号推理仅占总端到端延迟的 **1.9%**（20 ms），主要计算在感知（40.7%）和 PSSG 构建（53.4%）。
3. **边缘可部署**：在 Jetson Orin Nano 上稳定运行，帧率约 0.96 FPS（对于预着陆场景可接受），资源消耗在热/功耗约束内。
4. **可解释性**：通过 SCALLOP 提供可追溯的证明路径（如“靠近行人”规则触发权重 0.89），便于审计和规则调整。
5. **任务适应**：仅通过修改排序权重即可实现不同任务模式（安全/紧急/救援），无需重新训练感知或推理模块。

## 7. 优点

- **模块化分离**：感知（神经网络）与决策（符号规则）解耦，世界模型（PSSG）显式、可审计。
- **运行时确定性**：决策层完全由编译后的 SCALLOP 规则驱动，无神经网络推理，便于形式化验证和调试。
- **离线 LLM 辅助，运行时无 LLM**：既利用了大模型的组合能力加速规则开发，又避免了运行时大模型的开销和不可预测性。
- **轻量且边缘友好**：INT8 量化分割模型 + 确定性几何处理，PSSG 抽象显著降低细节复杂度，符号推理极低开销。
- **系统级评估全面**：不仅报告了 SIL 准确性，还用 HIL 试验展示了延迟、CPU/GPU、内存、功耗、温度等指标，验证了现实部署可行性。
- **开源完整**：提供源代码、数据集、提示词和规则迭代示例，可复现。

## 8. 不足与局限

- **感知上限问题**：分割错误（如系统性地交换语义标签）会直接导致 PSSG 事实错误，从而确定性传播到推理层，系统无法恢复。当前所有方法在此类感知失败场景下均失败，说明感知质量是主要瓶颈。
- **模拟环境局限**：所有测试均在 AirSim 进行，缺乏真实世界场景的验证（光照、天气、传感器噪声、真实地形等动态变化）。
- **场景动态有限**：当前场景中移动障碍物稀少，限制了时间推理（如多帧间运动规则）的优势。
- **帧率较低**：约 0.96 FPS，对于高速飞行中的实时决策可能不足，仅适用于预着陆悬停或缓慢接近阶段。
- **规则工程依赖人工**：虽然 LLM 辅助撰写规则，但最终需要专家多次迭代验证，规则集可能难以覆盖所有罕见边缘情况。
- **评估范围有限**：只评估了着陆点评估，尚未集成完整的状态估计、轨迹规划和闭环飞行控制，实际部署需额外工作。
- **未报告感知骨干训练细节**：训练时的算力、超参数、数据增强等未公开，影响可复现性。

（完）

---
title: "ShardNet: Training Neural Controllers with Hard, Non-Convex Constraints"
title_zh: ShardNet：训练具有硬非凸约束的神经控制器
authors: "Long Kiu Chung, Shreyas Kousik"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30935v1"
tags: ["query:safe-rl-cbf"]
score: 8.0
evidence: 强制执行硬安全约束的神经网络控制器架构
tldr: "神经网络控制器需满足严格安全约束，但现有方法将安全作为优化指标导致性能冲突。ShardNet架构通过可微投影层内嵌多面体约束的并集，确保输出严格符合安全集，同时提出ReLU网络值函数的验证与训练技术。在双积分器基准上，ShardNet策略100%满足验证安全集且目标损失显著降低，生成的安全集比现有方法大3倍以上。该方法首次实现非凸安全约束的严格保证，使性能优化与安全解耦。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 657, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 713, \"height\": 958, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30935v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 772, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30935v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 901, \"height\": 443, \"label\": \"Figure\"}]"
motivation: 现有方法将安全作为优化指标，导致性能与安全性难以兼顾，且无法严格保证非凸约束。
method: 提出ShardNet架构，利用分类网络参数化的可微投影层强制执行多面体约束并集，并引入ReLU网络值函数的正确验证与训练方法。
result: "在双积分器基准上，ShardNet策略100%安全且目标损失更低，生成的安全集比现有方法大3倍以上。"
conclusion: ShardNet通过将安全嵌入网络结构，首次实现非凸约束的严格保证，为安全关键系统提供了可靠的神经网络控制策略。
---

## 摘要
尽管神经网络控制策略功能强大，但它们在高安全关键系统中的部署取决于确保其严格遵守约束。现有工作通常将安全性作为需要优化的指标，如果训练能够收敛，这会与其他性能目标相竞争。相反，我们引入了ShardNet，这是一种神经网络架构，通过由分类网络参数化的可微投影层，通过构造严格强制执行多面体约束的并集。关键洞察是将安全性嵌入到神经网络的结构中，使得性能可以独立优化，因为形式安全性保证始终得到保证。与只能强制执行简单凸约束的现有神经架构相比，ShardNet首次实现了在闭环系统上按构造安全合成前向不变神经网络控制器，其中安全约束表示为多面体的非凸并集或学习到的值函数水平集。为此，我们还引入了一种技术，可以正确地将此类值函数验证并训练为整流线性单元（ReLU）网络，这在以前是不可能的。在来自文献的双积分器基准测试中，ShardNet策略在验证集上保持100%的安全性，并且与现有的形式化方法相比，实现了显著更低的客观损失。此外，我们的值函数训练技术产生的安全集比现有的验证方法大3倍以上。

## Abstract
While neural network control policies are powerful, their deployment on safety critical systems depends on ensuring that they obey strict constraints. Existing work often treats safety as a metric to optimize for, which competes with other performance objectives, if training converges at all. Instead, we introduce ShardNet, a neural network architecture that strictly enforces unions of polyhedral constraints by construction, using a differentiable projection layer parameterized by a classification network. The key insight is to embed safety into the neural network's structure, allowing performance to be optimized independently because formal safety guarantees are always given. In contrast with existing neural architectures that can only enforce simple convex constraints, ShardNet enables the first safe-by-construction synthesis of forward-invariant neural network controllers on closed-loop systems where safety constraints are expressed as nonconvex unions of polyhedras or learned value function level sets. To support this, we also introduce a technique to verify and train such value functions correctly as rectified linear unit (ReLU) networks, which has not previously been possible. On double integrator benchmarks drawn from the literature, ShardNet policies maintain 100% safety on verified sets and achieves significantly lower objective loss compared to existing formal methods. Furthermore, our value function training technique also produces safe sets more than 3 times larger than existing verification approaches.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：神经网络控制器在安全关键系统部署中需要严格满足约束，但现有方法通常将安全视为一个优化指标，与性能目标相互竞争，且训练可能不收敛；对于非凸安全约束（如多面体并集或学习到的值函数水平集），现有架构仅能处理简单凸约束。
- **研究动机**：现有工作要么忽略逼近误差（如深度强化学习），要么依赖训练收敛性来平衡安全与性能，要么无法强制执行复杂非凸约束。作者希望将安全性直接嵌入网络结构，使性能可以独立优化，同时保证形式化安全。
- **整体含义**：ShardNet 首次实现了按构造安全（safe-by-construction）的神经网络控制器，能够处理非凸约束，并支持对 ReLU 值函数的正确验证与训练，从而为安全关键系统提供可靠的解决方案。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：通过一个可微投影层（基于 HardNet-Cvx [10]）将基网络输出投影到输入相关的多面体并集中最近的非空多面体，该投影由分类网络引导。
- **关键结构**：
  - **基网络**（Base Network）：任意可微网络，输出 \( v_d \)。
  - **分类网络**（Classification Network）：输入为 \( (x_0, v_d) \)，输出每个多面体 \( P_i \) 的预测距离 \( s_i \)，然后用归一化函数（包含对空集和已安全点的特殊处理）得到 \( \hat{s}_i \)。
  - **投影选择**：通过 argmin 选择最小 \( \hat{s}_i \) 对应的多面体 \( P_{i^*} \)，再用 HardNet-Cvx 投影到该多面体上。
- **关键技术细节**：
  - 归一化公式（16）：空集对应 \( \hat{s}_i = 1 \)，已安全点对应 \( \hat{s}_i = 0 \)，其他情况用 logistic 函数映射。
  - 损失函数（13）：MSE，只对非空多面体计算距离。
- **算法流程**（图1）：
  1. 输入状态 \( x_0 \)。
  2. 基网络输出可能违规的动作 \( v_d \)。
  3. 分类网络估计 \( v_d \) 到每个约束多面体 \( P_i(x_0) \) 的距离。
  4. 选择归一化距离最小的非空多面体 \( P_{i^*} \)。
  5. 用 HardNet-Cvx 将 \( v_d \) 投影到 \( P_{i^*} \) 上，得到安全输出 \( x_d \)。
- **安全性保证**：定理2 证明只要至少有一个多面体非空，输出一定在并集内，与训练收敛无关。

**应用于前向不变控制（多面体并集）**：
- 通过预映像（preimage）和切片（slicing）将前向不变条件转换为控制器输出上的多面体并集约束（定理7）。

**应用于值函数（ReLU Q-network）**：
- 将 Q 网络 \( \tilde{\xi}_Q \) 的负半空间（\( H(1,0) \)）视为安全集，同样用预映像+切片得出控制器输出约束（定理12）。
- 提出了一个训练和修复 ReLU Q-network 的流程（图2）：
  - 使用边界引导回溯（BGB）[25] 收集候选反例。
  - 用完整验证（MILP，命题13）区分真反例与假阳性，避免不必要的安全集收缩。
  - 交替进行 BGB 采样、完整 MILP 检查、以及 ReLU 控制器的逼近训练，直到满足（25）（26）。

### 3. 实验设计
- **场景**：双积分器系统，安全集为两个多面体的并集（实验A）或通过 SAC 学习的 Q-network 的零子水平集（实验B/C）。
- **基准**：来自文献 [2], [6], [8]。
- **对比的方法**：
  - 实验A：对比“仅目标训练”和“神经网络修复”[6]。
  - 实验B：对比“未验证RL（SAC）”[2]、“声音验证”[8]（使用 CROWN）和“完整验证（本文）”。
  - 实验C：将 ShardNet 应用于上述三种 Q-network 上，评估轨迹安全性与前向不变性。
- **指标**：MSE 目标损失（实验A）、零子水平集面积（实验B）、安全违规率（实验C）。

### 4. 资源与算力
- 论文明确说明使用了：24核 i9 CPU、32 GB RAM、NVIDIA RTX 4090 GPU。
- 训练迭代次数：分类网络训练 \( 10^5 \) 步（实验A）或 \( 2\times 10^4 \) 步（实验C），基网络训练 100~300 步，Q-network 修复迭代次数（BGB/完整检查）分别为 444（[8] 方法）和 113（本文方法）。
- 未给出具体训练总时长。

### 5. 实验数量与充分性
- 共三项主要实验。
  - 实验A：验证 ShardNet 在非凸多面体并集上的安全性和性能优势，仅一个配置，但结果清晰。
  - 实验B：验证本文的完整验证方法相比声音验证能产生更大的安全集，对比了两种修复方法（[8]与本文），但只有一种系统（双积分器），未进行消融（如不同网络宽度、不同超参数）。
  - 实验C：在前三个 Q-network 上分别应用 ShardNet，报告了违规率（对未验证RL有少量违规，对验证Q-network为0%），但同样只在一个系统上。
- **充分性判断**：实验设计公平，与相关基线直接比较，指标合理。但由于仅使用了双积分器低维系统，推广性受限。未提供更多系统或更高维场景的对比，也未做消融研究（如分类网络准确性对整体性能的影响）。因此实验数量偏少，覆盖范围不够广泛。

### 6. 论文的主要结论与发现
- ShardNet 能按构造安全地强制执行非凸安全约束（多面体并集），性能目标可独立优化且不牺牲安全性。
- 在双积分器基准上，ShardNet 目标损失比修复方法低 3 倍左右（0.27 vs 0.80），且始终保持 100% 验证安全。
- 本文提出的完整验证方法能显著减少假阳性，训练出的 ReLU Q-network 安全集面积比声音验证方法大 3 倍以上（1.627 vs 0.529）。
- 使用 ShardNet 结合验证后的 Q-network 可生成 100% 安全的轨迹；即使使用未验证的 Q-network，违规率也低于 1%。

### 7. 优点
- **安全-by-construction**：安全性由架构保证，不依赖训练收敛。
- **可处理非凸约束**：首次实现对多面体并集及 ReLU 值函数水平集的严格强制执行。
- **性能与安全解耦**：分类网络和投影层允许基网络专注于性能优化。
- **完整验证修复**：通过 MILP 检查避免假阳性，生成更大安全集，填补了 ReLU Q-network 可验证修复的空白。
- **理论保证强**：定理2、7、12 提供了严格数学证明。

### 8. 不足与局限
- **仅低维示例**：实验限于双积分器，未在高维系统或更复杂场景（如四足机器人、无人机）中验证，可扩展性存疑。
- **推理效率**：ShardNet 依赖求解可微 LP 投影（HardNet-Cvx），推理速度受限，可能不适用于高速实时控制。
- **对 Q-network 的假设严格**：需要预先满足（25）（26），否则可能导致无法返回安全动作（如未验证RL出现0.667%违规）。
- **修复流程复杂性**：Q-network 修复方法依赖 MILP 求解和样本生成，可能难以扩展至高维，且存在“打地鼠”问题（无收敛保证）。
- **未讨论分类网络质量对性能的影响**：虽然安全性不受分类网络精度影响，但性能可能下降，论文未做深入分析。
- **实验覆盖不足**：缺少消融研究、不同规模网络对比、不同约束形状的适用性测试。

（完）

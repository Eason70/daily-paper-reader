---
title: Reference-Governed Distributed Safe Gradient Flow for Safe Optimal Output Agreement of Multi-Agent Systems
title_zh: 参考引导的分布式安全梯度流用于多智能体系统的安全最优输出一致性
authors: "Zhanglin Shangguan, Wei Xiao, Bo Yang, Xinping Guan"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02192v1"
tags: ["query:safe-rl-cbf"]
score: 8.0
evidence: 使用控制屏障函数实现安全最优输出一致
tldr: 针对非线性多智能体系统输出安全约束下的最优一致性问题，现有安全反馈优化方法需用高阶控制屏障函数，导致调参敏感且引入额外平衡条件改变稳态最优解。本文提出参考治理两层架构，上层通过一阶CBF约束过滤参考梯度流以保留稳态最优性，下层用内模输出调节器结合动态安全裕度保证瞬态输出安全。理论证明了前向不变性、最优解保持性和收敛性，仿真验证了相较于高阶CBF方法的优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02192v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 768, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02192v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 837, \"height\": 349, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02192v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 619, \"height\": 518, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02192v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 872, \"height\": 473, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02192v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 871, \"height\": 349, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02192v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 872, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02192v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 790, \"height\": 375, \"label\": \"Figure\"}]"
motivation: 现有安全反馈优化方法使用高阶CBF时调参敏感且可能改变稳态最优解，需设计更易调优且保持最优性的架构。
method: 提出参考治理两层架构：上层将梯度流通过一阶CBF约束滤波，下层用内模输出调节器结合参考依赖Lyapunov函数构造动态安全裕度。
result: 理论证明前向不变性、DSM兼容条件下最优解保持及Lyapunov小增益收敛；仿真显示优于高阶CBF方法并能自适应逃逸非凸障碍导致的伪平衡点。
conclusion: 参考治理分布式安全梯度流方法实现了安全性与最优性的解耦，简化了CBF设计且不牺牲稳态最优性。
---

## 摘要
本文研究了具有输出安全约束的非线性多智能体系统的安全最优输出一致性问题。现有的安全反馈优化方法通常通过系统输入直接实现梯度流动态，这可能需要高阶控制障碍函数（HOCBFs）。由此产生的导数链设计对调参敏感，且可能引入额外的平衡条件，改变稳态最优解。我们提出了一种参考引导的双层架构，将下层输出调节与上层分布式优化分离。上层通过一阶控制障碍函数约束过滤参考梯度流，该约束更易调参，并保留了原一致性问题的最优稳态结构。下层采用基于内模的输出调节器，结合依赖于参考的Lyapunov函数，由此构建动态安全裕度（DSMs）以认证瞬态输出安全。我们证明了在DSM相容条件下的前向不变性、最优解保持性，以及通过Lyapunov小增益论证的收敛性。仿真验证了安全收敛性，展示了相对于基于HOCBF的反馈优化的优势，并演示了用于逃离由非凸障碍引起的伪平衡的自适应切向目标整形。

## Abstract
This paper studies safe optimal output agreement for nonlinear multi-agent systems with output safety constraints. Existing safe feedback optimization methods often implement gradient-flow dynamics directly through the plant input, which may require high-order control barrier functions (HOCBFs). The resulting derivative-chain design is tuning-sensitive and can introduce additional equilibrium conditions that alter the steady-state optimal solution. We propose a reference-governed two-layer architecture that separates lower-layer output regulation from upper-layer distributed optimization. The upper layer filters the reference gradient flow through first-order control barrier function constraints, which are easier to tune and preserve the steady-state optimality structure of the original agreement problem. The lower layer uses an internal-model-based output regulator with a reference-dependent Lyapunov function, from which dynamic safety margins (DSMs) are constructed to certify transient output safety. We prove forward invariance, optimal-solution preservation under DSM-compatibility conditions, and convergence via a Lyapunov small-gain argument. Simulations validate safe convergence, show advantages over HOCBF-based feedback optimization, and demonstrate adaptive tangential objective shaping for escaping spurious equilibria induced by nonconvex obstacles.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：非线性多智能体系统在输出安全约束（如工作空间限制、避碰）下的最优输出一致性问题。要求所有智能体的输出同时达到全局最优解并满足瞬态安全（整个轨迹上安全约束成立）。
- **现有方法不足**：
  - 现有**安全反馈优化**方法（如SGF-HOCBF）通过系统输入直接实现梯度流，需使用高阶控制障碍函数（HOCBF）处理高相对度的输出安全约束，导致：① 调参敏感（高增益影响稳定性）；② 引入额外平衡条件，可能改变原静态优化问题的稳态最优解（尤其当最优解位于安全边界时）。
  - **投影原始-对偶**方法虽处理稳态不等式约束，但不保证瞬态输出安全。
- **整体含义**：需要一种既能保证瞬态输出安全，又能严格保持原静态优化问题最优解的双层控制架构，避免HOCBF的固有问题。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
**参考引导的双层架构**：将下层输出跟踪与上层分布式优化分离，通过一阶CBF在参考层过滤梯度流，显著简化安全设计，保留最优性。

### 关键技术细节
1. **下层：内模输出调节器**（第3节）
   - 每个智能体i使用基于内模的跟踪控制器，使物理输出yi跟随参考gi。
   - 构造**参考依赖Lyapunov函数**Vi(χi, gi)（二次型形式），测量跟踪误差。
   - 定义**动态安全裕度（DSM）**：
     - 安全DSM：\(M_{ik}^S(\chi_i, g_i) = \Gamma_{ik}^S(g_i) - V_i(\chi_i, g_i)\)，其中\(\Gamma_{ik}^S(g_i)\)是Lyapunov函数在安全边界上的下确界。
     - 稳定域DSM（可选）：\(M_i^D\)限制状态保持在Lyapunov有效域内。
   - DSM为正时，保证跟踪过程中输出不触及安全边界。

2. **上层：分布式参考治理**（第4节）
   - 参考动态：\(\dot{g}_i = \rho_i\)，其中\(\rho_i\)由网络稀疏的二次规划（QP）确定。
   - QP目标：最小化\( \frac{1}{2} \sum_i \|\rho_i + \nabla c_i(y_i)\|^2 \)（保持接近负梯度方向）。
   - QP约束：
     - 一致性流约束：\(L^T_\otimes \rho + \alpha_c L^T_\otimes g = 0\)（促进参考一致）。
     - 参考安全CBF：\(\nabla \eta_{ik}(g_i)^\top \rho_i + \alpha_\eta \eta_{ik}(g_i) \ge 0\)（保证gi在安全集内）。
     - DSM-CBF：\(\nabla_{\chi_i} M_{ik}^S \cdot \dot{\chi}_i + \nabla_{g_i} M_{ik}^S \cdot \rho_i + \alpha_S M_{ik}^S \ge 0\)（保证瞬态输出安全）。
     - 稳定域DSM-CBF（类似）。
   - 所有CBF为一阶，避免HOCBF链。

3. **最优性保持**（第5节）
   - 定理1：在理想调节流（χi=χi*(gi), yi=gi）下，QP平衡点ρ=0当且仅当满足原静态问题（3）的KKT条件，前提是DSM相容性条件（30）和稳定域DSM严格非零（31）。
   - 相容性条件：边界上DSM的法向量位于对应参考安全约束的法锥内，保证DSM不引入额外稳态最优性结构。
   - 命题1：对精确安全DSM，相容性条件自然满足。

4. **收敛性分析**（第6节）
   - 使用Lyapunov小增益论证，将下层跟踪误差和上层QP扰动级联。
   - 引理1：QP解对跟踪误差的Lipschitz依赖性（\(\|\rho^* - \rho_{id}\| \le \ell_\rho \| \chi - \chi^*(g)\|\)）。
   - 引理2：跟踪层输入-状态估计（\(\dot{V}_x \le -\bar{a}_x \|e_\chi\|^2 + \bar{b}_x \|\rho_{id}\|^2\)）。
   - 定理2：满足小增益条件（58）时，复合Lyapunov函数递减，跟踪误差和参考梯度流趋于零，所有极限点满足KKT条件，若凸则收敛到唯一最优解r*。

5. **非凸安全约束的自适应切向目标整形**（第7节）
   - 针对非凸障碍物（如圆形）可能产生的稳定边界伪平衡点（符合KKT（65）且Hessian正定）。
   - 引入辅助整形状态ϑi，修改上层QP目标：用\(\nabla c_i(y_i) + \nabla_{g_i} \Phi_i(g_i, \vartheta_i)\)替代名义梯度。
   - 整形函数\(\Phi_i = -\frac{\sigma_i}{2} \chi_b^i (g_i, \xi_i) (g_i-\xi_i)^\top P_{i,T}(\xi_i)^\top Q_i P_{i,T}(\xi_i)(g_i-\xi_i)\)，其中\(P_{i,T}\)是切向投影，仅沿障碍边界切向增加负曲率。
   - 整形状态连续演化（激活函数ai在障碍附近大，远离为0），保持CBF/DSM约束不变。
   - 命题2：整形后，原稳定边界点Hessian变为\(H_{i,e}^s = H_{i,e} - \sigma_{i,e} T_{i,e}^\top Q_i T_{i,e}\)，可通过选择σi,e使其负定，从而变为鞍点可逃逸。

## 3. 实验设计

- **仿真场景**：5个异构飞行器（二维线性交叉模型，来自[30]），输出为位置yi∈R²。
- **安全约束**：正六边形工作空间（6个线性不等式）。
- **最优解**：唯一全局最优解r*位于六边形边界上（验证边界情形）。
- **对比方法**：
  - **SGF-HOCBF基线**[15]：输入层执行HOCBF（二阶链），使用两种前反馈增益（各向同性Kp=4I,Kd=4I；各向异性Kp=diag(1,8),Kd=diag(3,5)）。
  - **投影原始-对偶反馈优化**[19]：适用于稳态不等式，但不保证瞬态安全。
- **本文方法**：使用DSM参考治理（QP(84)），同样测试两种跟踪增益。
- **额外实验**：增加非凸圆形障碍（圆心(0.5,1.0)，半径0.25），测试自适应切向整形效果。
- **评估指标**：到最优解r*的平均距离、最小安全裕度、收敛速度。

## 4. 资源与算力

- **未明确提及**。论文属于理论推导加仿真验证，未说明使用GPU型号、数量或训练时长。仿真环境很可能是MATLAB/Python（未指定），在普通计算机上运行即可。

## 5. 实验数量与充分性

- **实验数量**：相对有限，主要包含两组对比实验（凸约束下与两个基线对比；非凸障碍下与无整形对比）。变量包括跟踪增益（各向同性/各向异性）。未进行随机种子、噪声、不同智能体数量等系统性消融。
- **充分性**：实验设计具有针对性，验证了核心理论（最优性保持、瞬态安全、非凸逃逸）。但缺少：
  - 大规模系统（>5智能体）验证；  
  - 不同安全约束形状（如非线性、重叠约束）；  
  - 真实硬件实验；  
  - 与深度学习安全方法的对比。  
- **客观性与公平性**：基线参数采用原文献或常见设置（如文中说明），未进行调优，对基线可能不利（SGF-HOCBF在各项异性增益下失败，可能因未优化参数）。但论文指出这是HOCBF固有缺陷（导致伪平衡），故对比仍合理。

## 6. 论文的主要结论与发现

1. 提出的DSM参考治理方法在两种跟踪增益下均收敛到真实最优解r*，且保证瞬态输出安全（最小输出安全裕度~10⁻¹⁰，DSM非负）。
2. SGF-HOCBF基线在各向同性增益下安全收敛，但在各向异性增益下收敛到非最优边界点（距离r* ~1.84），说明HOCBF可能破坏最优性。
3. 投影原始-对偶基线可达到稳态最优，但瞬态输出违反安全（离开工作空间）。
4. 非凸障碍下，无整形的DSM基准陷入边界伪平衡（平均位置(0.34,0.81)）；增加自适应切向整形后可成功逃离并收敛到r*（距离~2.15×10⁻⁵）。
5. 理论部分证明了最优性保持（定理1）、前向不变性（CBF论证）和局部收敛性（定理2）。

## 7. 优点

- **方法层面**：
  - 双层解耦：将优化与跟踪分开，利用一阶CBF处理安全，完全避免HOCBF的复杂调参和伪平衡风险。
  - 动态安全裕度（DSM）新颖：基于Lyapunov函数量化瞬态安全距离，提供严格的前向不变性证书。
  - 理论完整：涵盖KKT等价性、DSM相容性、小增益收敛，且适用于边界最优解。
  - 自适应切向整形：针对非凸障碍伪平衡给出优雅的局部逃逸机制，保持CBF/DSM约束不变，无需松绑安全。
- **实验层面**：
  - 仿真对比合理，凸显了HOCBF的缺陷和本文方法的优势。
  - 非凸障碍案例验证了理论论断，展示了方法的可扩展性。

## 8. 不足与局限

- **实验覆盖**：
  - 仅5个智能体，未展示大规模系统下的计算效率或收敛性。
  - 未考虑通信时延、丢包、量测噪声等实际因素。
  - 非凸障碍整形需要手动设置超参数（σ阈值、bump半径、激活阈值），缺乏自动化调节策略。
- **理论限制**：
  - DSM相容性条件（30）要求DSM法锥在边界上与参考CBF法锥一致，对任意非线性约束不一定自动满足；命题1仅对精确安全DSM给出证明，对稳定域DSM假设严格非活动。
  - 非凸整形引入的辅助状态动态可能增加系统阶数，稳定性分析未完全覆盖整形本身收敛性。
- **应用限制**：
  - 适用于可精确建模的非线性系统（内模控制假设），不适用于黑箱或强不确定性系统。
  - 上层QP需要实时求解，可能受限于嵌入式设备算力（但问题规模小）。
  - 未与基于学习的CBF（如RL-CBF）对比，后者可能在复杂环境中更灵活。

（完）

---
title: Residual-Conservative Model Predictive Path Integral Control
title_zh: 残差保守模型预测路径积分控制
authors: "Hyung-Jin Yoon, Hunmin Kim"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.06950v1"
tags: ["query:safe-rl-cbf"]
score: 8.0
evidence: 具有自适应安全保守性的采样型模型预测控制
tldr: 采样型模型预测控制通常采用固定约束惩罚，无法适应模型与实际系统的失配。本文提出残差保守MPC（RC-MPPI），通过预测-执行残差在线调节安全保守性，包括约束收紧、成本整形和温度松弛。理论分析在Lipschitz动力学和次高斯扰动下推导了约束违反概率界。仿真表明，与原始MPPI相比，RC-MPPI在显著模型失配下安全裕度、成功率和控制效率均有提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06950v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 797, \"height\": 523, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06950v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06950v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 858, \"height\": 420, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06950v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06950v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 778, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06950v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 294, \"label\": \"Table\"}]"
motivation: 现有采样MPC在模型-实际系统失配时，固定约束惩罚导致安全性和自适应能力不足。
method: 提出RC-MPPI框架，融合残差依赖的约束收紧、自适应安全-成本整形和残差自适应采样温度松弛。
result: 理论推导了约束违反概率界，仿真验证了RC-MPPI在模型失配下安全裕度和成功率优于原始MPPI。
conclusion: RC-MPPI通过残差自适应调节方案，有效提升了模型失配下的鲁棒性与控制性能。
---

## 摘要
基于采样的模型预测控制方法通过蒙特卡罗展开处理非线性动力学和复杂代价景观，但通常采用固定的约束惩罚，无法适应模型与系统之间的不匹配。本文提出残差保守模型预测路径积分控制（RC-MPPI），一种基于采样的MPC框架，利用预测-执行残差在线调节安全保守性。RC-MPPI结合了三种耦合机制：残差依赖的约束收紧、自适应安全-代价整形，以及通过探索收缩和温度松弛实现的残差自适应采样调制。温度自适应反映了一个关键见解：当模型不准确时，展开代价评估变得不可靠，提高温度减少了对表观代价排名的过度承诺。在Lipschitz动力学和亚高斯扰动下，我们推导了约束违反的概率界，并表明自适应机制的联合效应随着残差增大而降低违反概率。展开代价不确定性分析进一步表明，模型-系统不匹配按残差大小正比于、按温度反比于扰动MPPI重要性权重，为残差自适应温度松弛提供了理论依据。在LTI质点点系统和平面2R机械臂上的仿真表明，在显著模型-系统不匹配下，与普通MPPI相比，RC-MPPI提高了安全裕度、成功率和控制效率。

## Abstract
Sampling-based model predictive control methods handle nonlinear dynamics and complex cost landscapes through Monte Carlo rollouts, yet typically employ fixed constraint penalties that do not adapt to model-plant mismatch. This paper proposes Residual-Conservative Model Predictive Path Integral Control (RC-MPPI), a sampling-based MPC framework that modulates safety conservatism online using the prediction-execution residual. RC-MPPI combines three coupled mechanisms: residual-dependent constraint tightening, adaptive safety-cost shaping, and residual-adaptive sampling modulation through exploration contraction and temperature relaxation. The temperature adaptation reflects a key insight: when the model is inaccurate, rollout cost evaluations become unreliable, and increasing temperature reduces overcommitment to apparent cost rankings. Under Lipschitz dynamics and sub-Gaussian disturbances, we derive probabilistic bounds on constraint violation and show that the joint effect of the adaptive mechanisms reduces violation probability as the residual grows. A rollout-cost uncertainty analysis further shows that model-plant mismatch perturbs MPPI importance weights in proportion to residual magnitude and inversely with temperature, providing theoretical justification for residual-adaptive temperature relaxation. Simulations on an LTI point-mass system and a planar 2R manipulator show improved safety margin, success rate, and control efficiency compared with vanilla MPPI under significant model-plant mismatch.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于采样的模型预测控制（如MPPI）在处理非线性动力学和复杂代价时，通常采用固定的约束惩罚或屏障函数，无法适应模型与实际系统之间的**模型-系统不匹配**（model-plant mismatch）。
- **背景**：低成本机器人平台常因执行器延迟、饱和、未建模内环动力学和有限感知精度导致执行变异性。当高级规划器依赖简化的名义模型时，这种不匹配是持续且状态依赖的，而非一次性扰动。现有的MPPI假设名义模型准确，其固定的安全机制在不匹配增大时会失效。
- **研究动机**：在线自适应保守性处理模型-系统不匹配，不需要实时参数辨识或维护完整的扰动信念，而是利用**预测-执行残差**（prediction-execution residual）——即直接可测量的名义模型预测与真实状态转移之间的差异——作为轻量级信号，嵌入到采样MPC目标中。
- **整体含义**：提出残差保守MPPI（RC-MPPI），通过三个耦合机制在线调节安全保守性，使控制器在模型不准确时自动提高安全裕度和对代价评估的置信度调整，在模型准确时恢复名义行为。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
利用滤波后的预测-执行残差\(\bar{s}_k\)作为模型-系统不匹配的指示信号，驱动三个自适应机制：约束收紧、惩罚缩放、采样调制（探索收缩和温度松弛）。其中温度被视为**认知参数**，反映对展开代价评估的置信度：当模型不准确时，展开代价不可靠，提高温度可减少对表观最优序列的过度承诺。

### 关键技术细节

#### 残差估计
- 一步预测残差：\(r_k = y_k - f_\theta(y_{k-1}, u_{k-1})\)
- 标量不匹配指标：\(s_k = \|W_r r_k\|\)
- 指数滤波：\(\bar{s}_k = (1-\rho)\bar{s}_{k-1} + \rho s_k\)

#### 三个自适应机制（Algorithm 1）
1. **残差依赖的约束收紧**（marginal tightening）  
   \[
   m(\bar{s}_k) = L_h(c_r \bar{s}_k + c_0)
   \]
   其中\(c_r, c_0\)来自定理1的N步预测误差界，直接缩放残差。

2. **自适应安全惩罚缩放**  
   \[
   \alpha_k = \alpha_0(1 + \gamma \bar{s}_k)
   \]
   当残差增大，屏障惩罚强度放大。

3. **残差自适应采样调制**  
   - 探索收缩：\(\varsigma_k = \text{clip}\left(\frac{\varsigma_0}{1+\kappa_\varsigma \bar{s}_k}, \varsigma_{\min}, \varsigma_{\max}\right)\)
   - 温度松弛：\(\beta_k = \text{clip}\left(\beta_0(1+\kappa_\beta \bar{s}_k), \beta_{\min}, \beta_{\max}\right)\)

#### 成本函数与MPPI更新
- 轨迹代价包含跟踪项、控制项和安全屏障项\(\alpha_k \phi(h(x)+m(\bar{s}_k))\)。
- 重要性采样权重在温度\(\beta_k\)下计算，更新控制量。
- 当\(\bar{s}_k \approx 0\)时RC-MPPI退化为vanilla MPPI。

#### 理论支持
- **定理1**：给定Lipschitz名义动力学和次高斯扰动，N步预测误差界为\(\|e_{k+N}\| \le c_r \bar{s}_k + c_0 + \|\xi_{k+N}\|\)。
- **命题2**：联合安全界表明，所有三个机制共同使约束违反概率随残差增大单调递减。
- **命题3**：重要性权重对展开代价不确定性的敏感度上界为\(2C_\Delta \bar{s}_k / \beta_k\)，证明提高温度可降低模型不匹配对权重的影响。

## 3. 实验设计

### 实验场景
- **场景1：LTI质点点系统**  
  - 状态：\([p_x, p_y, v_x, v_y]^\top\)，名义模型为离散LTI。
  - 不匹配源：严重的一阶执行器延迟（时间常数\(\tau=0.9\)s）。
  - 障碍物：半径为1.5m的圆，目标点为\([5,0]^\top\)。
  - MPPI参数：\(T=40\)，\(K=2048\)，输入限幅\(u_{\max}=4\)。

- **场景2：平面2R机械臂**  
  - 状态：关节角度和角速度\([q_1, q_2, \dot{q}_1, \dot{q}_2]^\top\)。
  - 不匹配源：同时存在三种——名义惯性参数错误（\(M_1^n=1.1\)kg，\(M_2^n=0.5\)kg vs 真实值1.0/0.8kg）、一阶执行器延迟（0.15s）、扭矩饱和（±6 N·m）、测量噪声。
  - 圆形障碍物位置\([0.85,0.20]^\top\)，半径0.15m。
  - MPPI参数：\(T=35\)，\(K=1024\)。

### Benchmark与对比方法
- 对比方法：**Vanilla MPPI**（普通MPPI，无自适应机制）。
- 采用**配对种子**（paired-seed）进行Monte Carlo试验，每个场景\(n=50\)次。

### 评估指标
- 成功率、到达目标时间（步数）、最小离障距离、违反步数、路径长度、控制能量（仅机械臂场景）。

## 4. 资源与算力
- **论文未明确提及**使用的GPU型号、数量或训练时长。
- 所有计算均为**在线采样滚动优化**（Monte Carlo rollouts），通常可在CPU上实时运行，但文中未提供具体硬件配置或耗时数据。

## 5. 实验数量与充分性
- **实验数量**：每个场景50次配对种子试验，总试验次数100次。在控制领域属于中等规模。
- **充分性**：
  - 配对种子设计消除了随机种子差异，保证了公平对比。
  - 但**缺乏与现代先进方法的对比**，如Shield-MPPI、CBF-MPPI、unscented MPPI、chance-constrained MPPI等。这使得结果的说服力局限于“优于vanilla MPPI”，未证明其在同类方法中的优势。
  - **未进行消融实验**（如分别去除温度松弛、约束收紧等机制对性能的影响），仅通过理论分析（命题2、3）间接说明各机制的作用。
  - 没有在**真实机器人硬件**上验证，仅在仿真中进行。
  - 实验场景仅两个，且都是低维系统（4维状态、2维控制），未在高维或复杂场景下测试。
- **客观性**：实验设计合理，统计量包括均值和标准差，指标全面。但缺乏对比基线可能导致结论强度有限。

## 6. 论文的主要结论与发现
- RC-MPPI在两种显著模型-系统不匹配场景下，均**显著提高了安全裕度、成功率和控制效率**。
  - LTI点质量系统：成功率从0.64提升至0.94，最小离障距离从0.05m提升至0.13m，违反步数从4.16降至0.62。
  - 2R机械臂：成功率从0.56提升至0.96，违反步数从3.08降至0.10，控制能量降低超过50%，路径长度和时间均改善。
- 模型-系统不匹配越大，RC-MPPI相对于vanilla MPPI的性能优势越明显。
- 理论分析支持了电阻自适应机制的有效性，特别是温度松弛对权重扰动敏感度的抑制作用。

## 7. 优点
- **方法创新性强**：将预测-执行残差作为轻量级不匹配信号引入MPPI，并设计三种耦合自适应机制，无需额外模型辨识或完整扰动分布，计算开销几乎为零。
- **理论分析扎实**：在Lipschitz动力学和次高斯扰动假设下，给出了N步误差界、联合安全概率界、重要性权重敏感度上界等严格数学推导，为自适应温度提供了理论依据。
- **温度的新解释**：将温度视为认知参数而非纯粹探索参数，在模型不准确时通过升温降低对不可靠代价排名的依赖，这是对MPPI理论的有益扩展。
- **设计简洁**：所有自适应方程均为线性或依赖残差的简单变换，容易实现和调试。

## 8. 不足与局限
- **实验对比不充分**：仅与vanilla MPPI对比，缺乏与现有鲁棒MPC、CBF-MPPI、学习MPC等方法的比较，无法评估RC-MPPI在更广范围内的相对优势。
- **未在真实硬件上验证**：所有结果均基于仿真，真实环境中的传感器噪声、模型不匹配特性可能与仿真假设不同，算法实效性存疑。
- **理论假设可能过于理想**：要求名义动力学全局Lipschitz、扰动次高斯、测量噪声有界等，在实际系统中不易严格满足。
- **残差滤波参数依赖人工调节**：如\(\rho, \kappa_\beta, \kappa_r, \Delta r_{\max}\)等需要手动设定，未讨论自适应调参方法。
- **温度松弛存在上限**：当\(\beta_k\)达到\(\beta_{\max}\)后停止增长，此时如果残差继续增大，算法将不再进一步放松温度，安全性可能下降。文中未分析\(\beta_{\max}\)的选择对性能的影响。
- **高维或复杂环境未测试**：实验均为低维系统，MPPI样本数量固定（1024或2048），未在高维状态空间（如四旋翼、无人车）或更复杂约束下测试。
- **缺乏消融实验**：无法量化每个机制（约束收紧、惩罚缩放、温度松弛、探索收缩）各自的贡献，理论证明无法完全替代实验验证。
- **计算资源未报告**：未说明运行时间或实时性，这对于在线MPC很重要。

（完）

---
title: Safe Reinforcement Learning using Ideas from Model Predictive Control
title_zh: 利用模型预测控制思想的安全强化学习
authors: "Georg Schäfer, Jakob Rehrl, Stefan Huber, Simon Hirlaender"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07252v1"
tags: ["query:safe-rl-cbf"]
score: 8.0
evidence: 安全强化学习结合模型预测控制实现硬安全约束
tldr: 强化学习在物理系统中易因冒险探索而违反安全约束，导致硬件损坏。本文提出结合MPC的安全RL框架，利用离线MPC计算全局可行的状态-动作空间，并通过安全滤波器将RL动作实时投影到该空间内。在1-DoF非线性硬件平台上验证了探索的安全性和策略的稳定收敛。该方法平衡了DRL的自适应性能与MPC的形式化安全保障。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07252v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1233, \"height\": 709, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07252v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1193, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07252v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 977, \"height\": 760, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07252v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1251, \"height\": 1617, \"label\": \"Figure\"}]"
motivation: 强化学习在物理系统探索时难以保证严格安全约束，违反可能导致不可逆硬件损伤。
method: 使用离线MPC在线下计算所有保证约束满足的可行状态-动作空间，通过安全滤波器实时投影RL动作。
result: 在非线性1-DoF硬件测试平台上成功实现安全探索和策略稳定收敛。
conclusion: 该框架有效融合DRL的高性能与MPC的形式化安全，适用于复杂物理系统。
---

## 摘要
强化学习能够直接从数据中合成控制策略，使其对于复杂的网络物理系统（CPS）和机器人领域极具吸引力。然而，一个持续的挑战是在主动学习阶段确保严格、硬性的安全约束。在现实物理系统中，违反机械限制可能导致不可逆的损伤，因此探索必须严格保持在安全操作区域内。我们提出了一个通用框架，将深度强化学习（DRL）的自适应、高性能特性与模型预测控制（MPC）的形式化安全保障相结合。利用系统动力学的数学模型，离线MPC计算定义了可行的状态-动作空间，代表了能够保证约束满足的系统状态和控制输入的所有安全组合。在训练和部署过程中，RL智能体的即时动作通过安全过滤器投影到这个全局验证的可行集中。我们在一个非线性单自由度实验室测试平台上系统评估了所提通用方法，在物理硬件上展示了成功的探索和稳定的策略收敛。

## Abstract
Reinforcement learning (RL) enables the synthesis of control policies directly from data, making it highly appealing for complex cyber-physical systems (CPSs) and robotics. A persistent challenge, however, is ensuring strict, hard safety constraints during the active learning phase. In real-world physical systems, violating mechanical limits can cause irreversible damage, necessitating that exploration remains strictly within safe operational regions. We propose a generalized framework that combines the adaptive, high-performance nature of deep reinforcement learning (DRL) with the formal safety guarantees of model predictive control (MPC). Using a mathematical model of the system dynamics, offline MPC computations define a feasible state-action space, representing all safe combinations of system states and control inputs that guarantee constraint satisfaction. During training and deployment, the RL agent's instantaneous actions are projected onto this globally verified feasible set via a safety filter. We systematically evaluate our generalized approach on a non-linear 1-DoF laboratory testbed, demonstrating successful exploration and stable policy convergence on physical hardware.

---

## 论文详细总结（自动生成）

# 论文总结：Safe Reinforcement Learning using Ideas from Model Predictive Control

## 1. 核心问题与研究动机
- **核心问题**：在物理系统中应用强化学习（RL）时，标准的随机探索机制极易违反机械硬约束（如位置、速度极限），导致不可逆硬件损坏。现有方法（如约束MDP、Lyapunov方法、控制屏障函数CBF）或无法提供绝对确定性安全保证，或在线计算复杂、受维度灾难限制。
- **研究背景**：深度强化学习（DRL）在高维非线性控制中潜力巨大，但缺乏对“硬安全”的形式化保障。需要一种既能保持RL高自适应性能，又在每一步严格保证约束不违反的方法。

## 2. 方法论
- **核心思想**：利用离线模型预测控制（MPC）作为安全性预言机，预先计算全局可行的状态-动作空间 \(\mathcal{F}\)；训练时通过一个投影滤波器将RL智能体的即时动作映射到该安全集内，从而确保探索与部署的绝对安全。
- **关键技术细节**：
    - 定义“不安全状态”：任何当前状态导致未来无论采取何种控制动作都必然违反约束的状态（即“不归点”）。
    - **离线MPC映射**：对离散化的状态-动作网格，对每个状态 \(s\) 使用MPC优化（有限时域 \(H\)）判断动作 \(a\) 是否可行。利用系统控制仿射性质，安全动作集 \(\mathcal{F}_A(s)\) 是凸区间，仅需二分法搜索最小/最大安全动作 \(a_{\min}(s), a_{\max}(s)\)。
    - **安全滤波器**：在线步骤中，对于RL策略输出的原始动作 \(\hat{a}_t\)，若落入 \( [a_{\min}(s_t), a_{\max}(s_t)] \) 则直接执行；否则投影到最近的有效边界。
    - **奖励塑造**：可在奖励函数中惩罚投影量 \(\| \pi(s) - \bar{\pi}(s) \|\)，激励策略自主学习安全边界；并可加入动作平滑惩罚抑制高频震荡。
- **算法流程**：
    1. 基于系统模型离线执行MPC网格扫描，构建 \(\mathcal{F}\) 映射表。
    2. 训练时，RL智能体观察状态 \(s_t\)，输出原始动作 \(\hat{a}_t\)。
    3. 投影滤波器查询 \(\mathcal{F}\)，获取 \(a_{\min}(s_t), a_{\max}(s_t)\)，输出安全动作 \(a_t = \mathcal{P}_{s_t}(\hat{a}_t)\)。
    4. 将 \(a_t\) 作用于环境，得到下一状态 \(s_{t+1}\) 和奖励 \(r_{t+1}\)，反馈给RL智能体。

## 3. 实验设计
- **实验平台**：Quanser Aero 2 实验台，配置为单自由度（1-DOF）俯仰控制，机械约束 \(\pm 60^\circ\)。
- **场景**：模拟环境（基于Matlab/Simulink模型）和真实物理硬件。
- **基准方法**：论文未明确列出其他方法（如CPO、CBF等）作为对比基线；仅与未加安全滤波器的原始PPO（即无约束RL）相比，指出所提方法大幅降低了碰撞违规率。
- **评测指标**：未给出定量指标（如平均返回、违规次数等），仅定性描述“成功探索边界”、“大多数训练步骤约束满足”。

## 4. 资源与算力
- **未明确提及**：论文未说明训练使用的GPU型号、数量、训练时长等算力信息。仅提到“高频率控制回路”和“编译为DLL后包装为Gymnasium接口”，但无具体硬件规格。

## 5. 实验数量与充分性
- **实验数量**：仅在一个1-DOF硬件平台上进行了单组训练实验（PPO算法），未包含多组重复、不同随机种子或消融研究。
- **充分性评价**：实验数量不足，缺乏与现有安全RL方法的直接对比（如CPO、CBF-Shield），也未报告训练曲线、违规率统计等量化结果。虽然工作展现了概念可行性，但实验设计不够严格、客观，不足以充分证明方法的泛化性和优越性。

## 6. 主要结论与发现
- 所提出的离线MPC + 投影滤波器框架成功允许PPO在物理硬件上安全训练，策略收敛且避免了灾难性故障。
- 尽管理论保证递归可行性，但由于“Sim-to-Real gap”（模型误差、采样延迟、数值精度等），仍出现了罕见的机械约束违反（边缘情况）。
- 该方法将在线计算负担转移至离线，使得高频率实时控制成为可能。

## 7. 优点
- **理论严格性**：利用MPC的长期前瞻能力，定义了比单步约束更强的“递归可行性”，防止进入不可挽救的不安全状态。
- **计算效率**：离线构建安全集，在线仅需查表和欧氏投影，推翻了“MPC必须在线求解”的认知，适合高频控制。
- **系统凸性利用**：通过二分搜索大幅降低离线映射的计算复杂度，避免了对连续动作空间的穷举。
- **奖励引导策略学习**：惩罚投影量可促使策略自主输出安全动作，减少对滤波器的依赖。

## 8. 不足与局限
- **模型依赖**：安全性严格依赖于系统动力学模型的精度，模型失配（Sim-to-Real gap）直接导致残余违规。
- **维度灾难**：网格搜索在状态-动作空间维度增高时指数爆炸，当前仅验证了1-DOF系统，扩展至高维（如机器人操作臂）不可行。
- **实验不充分**：缺乏多组重复、基线对比、鲁棒性测试；未提供量化违规率或训练曲线，结论说服力有限。
- **未解决实时性细节**：虽然声称高频，但未具体论证投影滤波器的实际延迟及对控制环路的影响。
- **通用性待验证**：方法依赖于控制仿射凸性假设，并非所有系统都满足，限制了应用范围。

（完）

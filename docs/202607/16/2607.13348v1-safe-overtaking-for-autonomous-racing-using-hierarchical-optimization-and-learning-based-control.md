---
title: Safe Overtaking for Autonomous Racing Using Hierarchical Optimization and Learning-Based Control
title_zh: 基于分层优化与学习控制的自动驾驶赛车安全超车
authors: "Hassan Jardali, Kai Yin, Lantao Liu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13348v1"
tags: ["query:safe-rl-cbf"]
score: 9.0
evidence: 模型预测控制结合控制屏障函数的安全超车方法
tldr: 针对自动驾驶赛车超车中固定衰减离散时间控制屏障函数(CBF)过于保守、限制性能且需手动调参的问题，提出分层框架：高层混合整数二次规划(MIQP)选择超车拓扑，低层非线性Frenet-frame模型预测控制(MPC)嵌入CBF约束，并用强化学习在线调整CBF衰减参数。仿真与硬件实验表明，自适应策略在多种赛道上获得最高综合成功率，无需逐赛道调参，安全-性能权衡更稳健。该方法降低了保守性，提高了环境适应性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13348v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 391, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13348v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 822, \"height\": 268, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13348v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 906, \"height\": 208, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13348v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 902, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13348v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1853, \"height\": 335, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13348v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 899, \"height\": 828, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13348v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 896, \"height\": 227, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13348v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 632, \"height\": 138, \"label\": \"Table\"}]"
motivation: 固定衰减的离散时间CBF在交互超车场景中过于保守，限制了性能并需要手动参数调整。
method: 分层框架：高层MIQP选择超车拓扑，低层MPC嵌入CBF约束，并用强化学习在线调整CBF衰减参数。
result: 自适应策略在多种赛道上获得最高综合成功率，无需逐赛道调参，安全-性能权衡稳健。
conclusion: 所提方法降低了保守性，提高了对不同环境的适应性，同时保证安全约束满足。
---

## 摘要
自动驾驶赛车的超车需要在非线性车辆动力学和实时约束下平衡竞技性能与安全性。模型预测控制（MPC）与控制障碍函数（CBF）相结合为安全集的前向不变性提供了严格的证明机制。然而，在交互式赛车场景中，常用的固定衰减离散时间CBF公式可能变得过于保守，限制了超车性能，并需要根据赛道条件进行手动调整。本文提出了一种分层超车框架，将策略级决策与安全认证的轨迹控制显式分离，在保证安全性的同时降低保守性。高层混合整数二次规划（MIQP）通过选择可行的超车拓扑结构解决组合性超车侧选择问题，而非线性Frenet框架MPC通过嵌入的离散时间CBF约束强制执行车辆动力学和安全性。这种分解将策略选择的组合复杂度与连续轨迹优化分离开来。为了进一步缓解固定衰减障碍约束的敏感性，强化学习策略在线调整离散时间CBF衰减参数，实现了安全裕度的上下文相关调制，而无需直接控制车辆输入。仿真和缩小硬件实验表明，没有一个单一的固定衰减参数能在所有赛道上实现一致优越的性能，而自适应策略在没有针对每条赛道进行调整的情况下，获得了最高的总体成功率和一致优异的安全-性能权衡，在标称运行中提高了对环境变化的鲁棒性，同时保持安全约束满足性。

## Abstract
Autonomous racing overtaking requires balancing competitive performance with safety under nonlinear vehicle dynamics and real-time constraints. Model Predictive Control (MPC) combined with Control Barrier Functions (CBFs) provides a principled mechanism for certifying forward invariance of a safe set. However, commonly used fixed-decay discrete-time CBF formulations can become overly conservative in interactive racing scenarios, limiting overtaking performance and requiring manual tuning across track conditions. This paper proposes a hierarchical overtaking framework that explicitly separates maneuver-level decision making from safety-certified trajectory control, reducing conservatism while preserving safety. A high-level Mixed-Integer Quadratic Program (MIQP) resolves the combinatorial passing-side selection problem by selecting a feasible overtaking topology, while a nonlinear Frenet-frame MPC enforces vehicle dynamics and safety through embedded discrete-time CBF constraints. This decomposition isolates the combinatorial complexity of maneuver selection from the continuous trajectory optimization. To further mitigate the sensitivity of fixed-decay barrier constraints, a reinforcement learning policy adapts the discrete-time CBF decay parameter online, enabling context-dependent modulation of safety margins without directly controlling vehicle inputs. Simulation and scaled-hardware experiments show that no single fixed decay parameter achieves uniformly strong performance across tracks, whereas the adaptive strategy attains the highest aggregate success rate and consistently strong safety--performance trade-offs without per-track tuning, improving robustness to environment variation while maintaining safety constraint satisfaction in nominal operation.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：自动驾驶赛车是验证极限工况下规划控制算法的理想平台。超车需要在竞技性能与安全性之间谨慎权衡。模型预测控制（MPC）结合控制障碍函数（CBF）可提供安全集前向不变性的形式化保证，其中离散时间CBF（DCBF）通过一个固定衰减参数γ控制安全裕度。
- **核心问题**：固定γ的DCBF在交互式超车场景中过于保守，因为它在每一步局部施加安全条件，而非在整个规划时域内推理策略级决策。这种保守性限制了超车性能，且需要针对不同赛道手动调参——而最优γ因赛道而异，无单一固定值能统一表现优异。
- **整体意义**：需要一种既能保持安全保证又能减少保守性的方法，实现上下文相关的自适应安全裕度。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：采用分层架构，将高层的离散策略选择（超车侧）与低层的连续轨迹优化及安全认证解耦，并用强化学习在线适配CBF衰减参数γ，实现安全-性能的自适应权衡。

- **关键技术细节**：

  - **高层：MIQP规划器**（公式(7)）
    - 在Frenet坐标系下，以粗粒度时域（nq步）求解混合整数二次规划（MIQP）。
    - 决策变量：横向参考偏移dk、选择超车侧的二进制变量bi（代表对每个对手车辆bi=1为左超车，bi=0为右超车）。
    - 约束：赛道边界、碰撞避免（通过Big-M约束线性化非凸的侧选择）、二进制连续性。
    - 目标：最小化与理想赛车线偏差、平滑性以及与上一周期解的时序一致性。
    - 输出：一个横向参考轨迹{d*_k}和选定超车侧{b*_i}，提供给低层控制器。以较低频率（不在每个MPC周期）执行。

  - **低层：MPC-DCBF控制器**（公式(9) + (10)）
    - 基于Frenet框架的非线性模型预测控制（MPC），使用运动学自行车模型（Runge-Kutta离散化）。
    - 状态：s（纵向里程）、η（横向偏差）、e（航向误差）、v（速度）、δ（转向角）；控制：a（加速度）、δ̇（转向率）。
    - 跟踪MIQP提供的横向参考轨迹d*_k和其他名义参考（纵向、航向等）。
    - 嵌入DCBF约束：对每个对手车辆i，要求 (1-γ) h_i(x_k) - h_i(x_{k+1}) ≤ ε_{k,i}，其中ε为松弛变量（软约束保证可行性）。
    - 安全函数h_i定义为速度感知椭圆（公式(3)）：纵向半轴随相对速度增大（基于制动距离模型），横向半轴固定。
    - 同时包含赛道边界约束（公式(4)）和加速度限制。

  - **自适应安全调制：RL策略（PASTA）**（公式(11) + 算法1）
    - 采用基于多目标强化学习的PASTA框架（基于PPO和Adaptive Smooth Tchebycheff Attention）。
    - 策略π_θ(o)以观测向量o（含自我车辆状态、对手相对状态、赛道几何策略等）为输入，输出CBF衰减参数γ ∈ [0.05, 1.0]。
    - 奖励函数：鼓励成功超车（w_ot）并惩罚碰撞/出界（-w_safe）。
    - 训练在仿真中离线进行，部署时在每个控制周期输出γ，然后MPC使用该γ求解。

- **算法流程（Algorithm 1）**：
  1. 输入自我状态、对手状态 → 变换至Frenet框架 → 预测对手运动。
  2. 若MIQP更新触发，则求解MIQP得到横向参考。
  3. 观察o_k，由RL策略得到γ_k。
  4. 求解MPC-CBF（使用横向参考和γ_k）。
  5. 重复控制循环。

## 3. 实验设计：使用的数据集/场景、benchmark、对比方法

- **仿真环境**：基于Python的定制仿真器，采用非线性运动学自行车模型，参数化为Dallara AV-21赛车。
- **赛道**：四个真实赛道几何：印第安纳波利斯赛车场（IMS）、肯塔基赛道（KS）、拉古纳塞卡（LS）、蒙扎（Monza）。其中LS作为训练赛道，其余三个为未见过的泛化测试赛道。
- **对手配置**：对手车辆以不同速度比ρ ∈ {0.1, 0.3, 0.5, 0.65, 0.8}（相对于自我车辆最大速度）前行，且随机初始位置。每个(赛道, ρ)组合执行5个episode，每赛道共25个episode。
- **对比方法**：
  - 六种固定γ值：0.05, 0.15, 0.30, 0.50, 0.70, 0.95。
  - 自适应γ（所提方法）。
  - 消融实验：移除MIQP层（w/o MIQP）与保留MIQP（w/ MIQP），分别对比自适应γ和固定γ=0.5。
- **硬件演示**：缩比F1TENTH平台，使用虚拟对手，固定γ，验证分层框架在真实软硬件约束下的实时部署能力。

- **评估指标**：成功率（无碰撞/出界完成200秒）、累计仿真时间、完成圈数、超车次数。

## 4. 资源与算力

- **文中说明**：所有实验在Intel i7-12700H CPU（16 GB RAM）上运行，未提及GPU。RL训练（PASTA）在仿真环境中离线进行，但未报告具体使用的GPU型号、数量或训练时长。MPC、MIQP、RL推理的延迟分别为1.60±0.34 ms、5.49±1.73 ms、0.82±0.12 ms，总体控制在50Hz控制周期内。
- **结论**：计算资源信息不完整（未提供训练算力细节）。

## 5. 实验数量与充分性

- **实验数量**：
  - 主对比（表I）：每个赛道7种γ策略（1自适应+6固定），各25个episode，共7×25×4=700个episode。每个episode最长200秒。
  - 消融实验（表II）：LS赛道，2种设置（w/o MIQP vs w/ MIQP）× 2种策略（自适应、γ=0.5），每个条件5个episode？原文未明确episode数，但从图标题推测相似数量（共约20+ episode）。
  - 硬件演示：定性展示，无定量对比。
- **充分性评价**：
  - **优点**：跨越多种赛道类型（曲线、直道、椭圆等），覆盖不同赛道曲率与速度特性；对手速度比变化宽（0.1-0.8），模拟不同拥堵难度；重复5次取平均，提供了基本统计量。
  - **不足**：每个条件仅5个episode，样本量较小，可能难以区分统计显著性；未提供置信区间或标准差（除计算延迟外）；未对不同随机种子进行交叉验证；仅在一个训练赛道（LS）训练RL，泛化测试在三个全新赛道，但可能未覆盖所有可能的交互模式（如多车密集编队）。此外，未在完全相同的初始条件下做多次独立重复实验（初始状态随机化但未说明种子控制）。

## 6. 论文的主要结论与发现

- **主结论**：自适应γ策略在四个赛道上的总体成功率最高（67%，100个episode中），无需针对每个赛道手动调整，而任何单一固定γ都无法在所有赛道上达到接近最优的性能。
- **具体发现**：
  - **固定γ表现依赖赛道**：IMS最优γ=0.70（68%），LS最优γ=0.15（68%），Monza最优γ=0.50（84%），KS最优γ=0.50（56%）。极低γ=0.05和极高γ=0.95表现最差。
  - **自适应γ泛化良好**：在未见过的IMS、KS、Monza上分别达到72%、48%、72%，均优于或接近最佳固定γ（在KS略低于最佳固定γ 56%但远优于次优）。
  - **MIQP层关键**：移除MIQP后，自适应γ成功率从50%降至20%，固定γ=0.5从60%降至10%，表明显式拓扑选择对解决非凸性至关重要。
  - **实时性**：总延迟<20ms（50Hz），满足实时要求。
  - **自适应γ的分布**：γ值集中在0.7-0.8，在近距离交互时下降，呈现状态依赖调制，而非短路冲。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 分层架构巧妙分解组合选择与连续优化，既保证实时性又避免MPC陷入局部最优。
  - RL仅调整一个可解释的CBF参数γ，不直接控制车辆输入，保留安全约束的理论结构。
  - 使用速度感知椭圆安全函数（基于制动距离）增强安全性。
  - 软约束处理保证可行性，松弛变量仅在必要时激活。
- **实验设计亮点**：
  - 跨赛道泛化测试：只在LS训练，在IMS、KS、Monza测试，验证了适应性。
  - 消融实验明确论证MIQP层的必要性。
  - 硬件演示验证了在实际平台上的可行性和实时性。
  - 公开代码（github.com/hassanjardali/SOfAR）促进可复现性。

## 8. 不足与局限

- **安全保证的局限性**：软约束中的松弛变量在密集交互时可能被激活，此时DCBF不再严格保证前向不变性。论文指出“安全保证依赖松弛变量的不活跃”，但未量化分析激活频率或场景。
- **对手模型简单**：使用恒定速度预测模型，未考虑对手策略性交互或加速/制动，在真实对抗中可能不充分。
- **泛化到高速赛道较弱**：在高速椭圆赛道KS上的成功率为48%，低于最佳固定γ=0.5的56%，说明对高速场景的适应能力仍有提升空间。
- **全局最优性缺失**：MIQP与MPC分离，不保证全局最优；参数选择（如MIQP权重、MPC权重）仍可能需要手动调节。
- **实验统计不足**：每组条件仅5个episode（1200秒左右），样本量偏小，未报告方差或置信区间，可能无法充分体现随机性影响。
- **训练细节缺失**：RL训练未提供具体超参数、训练轮数或验证曲线；未说明算力消耗（如GPU型号），不利于复现。
- **硬件实验局限**：仅使用一辆物理车加虚拟对手，且采用固定γ，未能验证自适应策略在真实硬件上的表现；场地有限，超车场景简单。
- **未与其他先进方法对比**：未与基于采样、图搜索或其他自适应CBF方法（如学习成本函数）做定量对比，削弱了方法优越性的说服力。

（完）

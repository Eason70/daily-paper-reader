---
title: Knowledge- and Gradient-Guided Reinforcement Learning for Parametrized Action Markov Decision Processes
title_zh: 参数化动作马尔可夫决策过程中的知识与梯度引导强化学习
authors: "Jonas Ehrhardt, René Heesch, Oliver Niggemann"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12924v2"
tags: ["query:safe-rl-cbf"]
score: 8.0
evidence: 整合安全约束知识的参数化动作RL
tldr: 参数化动作马尔可夫决策过程(PAMDP)中，每个决策包含符号动作和数值参数，现有强化学习算法使用一次性参数估计导致样本效率低下，而显式但不完备的领域知识常被忽视。本文提出知识梯度引导强化学习(KGRL)算法，利用Datalog知识库推导当前状态下适用动作和可行参数，裁剪动作空间并约束参数，再通过梯度参数精化循环优化参数训练和部署。KGRL在路径中记录激活规则，提供局部可解释性，引导探索与部署实现约束感知决策，显著提升样本效率与回合回报。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12924v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2607.12924v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 690, \"height\": 687}, {\"url\": \"assets/figures/arxiv/2607.12924v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1777, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2607.12924v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 768, \"height\": 577}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12924v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 1982}, {\"url\": \"assets/tables/arxiv/2607.12924v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 727}, {\"url\": \"assets/tables/arxiv/2607.12924v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1811, \"height\": 708}, {\"url\": \"assets/tables/arxiv/2607.12924v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 830, \"height\": 340}]"
motivation: PAMDP中动作含符号与数值参数，现有RL算法使用一次性参数估计，样本效率低下，而可获得的领域知识未被有效利用。
method: KGRL利用Datalog知识库推导适用动作和可行参数，裁剪动作空间并约束参数，再通过梯度参数精化循环优化参数，记录规则提供解释。
result: 在PAMDP基准上，KGRL在样本效率和回合回报均超越现有RL基线。
conclusion: KGRL通过知识引导探索与部署，实现约束感知决策，提升样本效率并提供可解释性。
---

## 摘要
本文研究了参数化动作马尔可夫决策过程（PAMDP）中的强化学习问题，其中每个决策由符号动作和数值参数组成。在此类设置中，强化学习算法通常采用单次估计器来确定参数，导致训练样本效率低下。尽管大多数PAMDP环境中存在显式但不完整的知识（例如规则、安全约束或专家启发式方法），但很少直接用于提高强化学习智能体训练的样本效率。我们针对这一空白，提出了一种新颖的神经符号知识与梯度引导强化学习（KGRL）算法。KGRL利用Datalog知识库中的领域知识，推导出给定状态下的适用动作集和可行参数，从而从决策空间中剪除不适用动作，并约束剩余动作的参数空间。然后，我们使用基于梯度的参数精化循环，在智能体训练和部署过程中估计最优参数。通过记录轨迹中激活的规则，KGRL还提供了关于动作剪枝和参数约束的局部过程性解释。总体而言，KGRL引导智能体的探索和部署走向可行且考虑约束的决策，同时提高训练期间的样本效率。在PAMDP的样本效率和回合回报方面，KGRL均优于最先进的强化学习基线。

## Abstract
In this paper, we study Reinforcement Learning in Parametrized Action Markov Decision Processes (PAMDP), where each decision consists of a symbolic action and numerical parameters. In such settings Reinforcement Learning algorithms typically determine parameters with one-shot estimators, which makes their training sample inefficient. Though in most PAMDP environments explicit but incomplete knowledge (e.g., rules, safety constraints, or expert heuristics) is available, it is rarely directly used to increase the sample-efficiency of training Reinforcement Learning agents. We step into this gap and propose our novel Neuro-Symbolic Knowledge- and Gradient-Guided Reinforcement Learning (KGRL) algorithm. KGRL uses domain knowledge in a Datalog knowledge base to derive the set of applicable actions and feasible parameters for a given state. This allows it to prune non-applicable actions from the decision-space and constrain the parameter spaces of the remaining actions. We then use a gradient-based parameter refinement loop to estimate the optimal parameters during training and deployment of the agent. By recording activated rules along the trajectory, KGRL additionally provides local procedural explanations on the pruning of actions and constraining of parameters. Overall, KGRL guides the agent's exploration and deployment toward feasible and constraint-aware decisions, while increasing sample efficiency during training. KGRL outperforms state-of-the-art RL baselines for PAMDPs in both, sample efficiency and episodic return.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在参数化动作马尔可夫决策过程（PAMDP）中，每个决策由符号动作和连续数值参数组成。现有强化学习（RL）算法通常使用**一次性参数估计**（one-shot estimators）来预测参数，导致训练样本效率低下。同时，虽然许多PAMDP环境存在**显式但不完备的领域知识**（如规则、安全约束、专家启发式），但这些知识很少被直接用于提升RL训练的样本效率。
- **核心目标**：研究如何利用显式但不完备的知识来引导RL智能体的训练和部署，在**约束已知不安全/不可行决策**的同时，让RL自主学习无法符号化指定的最优行为，从而提升样本效率和性能，并增强决策的可解释性。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**神经符号**的**知识与梯度引导强化学习（KGRL）** 算法。该算法在DQN基础上引入两个关键扩展：
  1. **符号知识库引导**：利用**Datalog知识库**和**状态符号抽象函数**，在每一步推导出当前状态下的**适用动作集**和**可行参数空间**，从而剪除不适用动作并约束参数区域。
  2. **梯度引导参数精化**：使用**投影梯度上升**（Projected Gradient Ascent）对动作值函数关于参数的梯度进行迭代优化，从初始子优参数估计收敛到局部最优参数，同时满足知识库导出的参数约束。
- **关键技术细节**：
  - **知识约束PAMDP形式化**：在标准PAMDP上添加符号抽象函数α和Datalog知识库K，定义知识约束PAMDP。在状态s下，通过评估K∪α(s)∪Act∪Con得到适用动作集Ā(s) = {a∈A | K推导出app(a)}，以及可行参数集Ψ̄_a(s) = ∩_{c∈C_K(s,a)} Ψ_{a,c}(s)，其中C_K(s,a)是K推导出的活动约束标识集。
  - **算法流程**（Algorithm 1 KGRL训练循环）：
    - 初始化DQN、目标网络、经验回放池。
    - 每一步，评估知识库得到Ā(s)和参数约束。
    - 以ε-greedy探索：随机选动作和参数；或利用：对每个适用动作调用PARAM OPT（Algorithm 2）计算最优参数˜ψ*_a，然后选择Q值最高的动作-参数对执行。
    - 存储经验，采样批量，计算目标值y = r + γ(1-d') max_{a'} max_{ψ'} Q_{θ^-}(s', a', ψ')，其中目标值计算同样使用知识库和PARAM OPT。
    - 通过最小化平方Bellman误差更新Q网络，定期同步目标网络。
  - **PARAM OPT**（Algorithm 2）：给定状态s、动作a、可行参数空间Ψ̄_a(s)，从初始参数猜测ψ̂出发，循环U_max次：计算梯度g = ∇_ψ Q_θ(s, a, ψ̂)，执行投影梯度上升ψ̂ ← Proj_{Ψ̄_a(s)}(ψ̂ + ζg)，当||g|| < ξ时终止，返回最终参数˜ψ*。
- **解释迹线**：在每一步记录推导app(a)、napp(a)、active(a,c)的规则和事实，构成**局部过程性解释**，说明为何剪除某些动作以及为何参数受到约束。

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

- **实验场景**：使用四个经典PAMDP基准域：
  - **HardMoveX**（HardMove4,8,10）：2D导航，agent需到达目标，动作数2/8/10，参数为移动幅度。规则剪除指向目标的相反方向并限制参数。
  - **CatchPoint**：2D抓取任务，动作有move(ψ)和catch(ψ)，参数映射到角度。规则剪除非必要移动或不可能抓取。
  - **HardGoal**：简化足球射门，10个射门动作a0~a9(参数[-1,1])，守门员移动。规则根据守门员位置剪除错误半场动作。
  - **Platform**：平台穿越，动作run(ψ), hop(ψ), leap(ψ)，规则剪除危险动作并约束参数。
- **Benchmark & 对比方法**：对比五种SOTA PAMDP RL算法：
  - QPAMDP, PA-DDPG, P-DQN, MP-DQN, HyAR（分别对应Masson 2016, Hausknecht 2016, Xiong 2018, Bester 2019, Li 2022）。
- **实验配置**：所有算法训练步数为**500,000**，最大计算时间为**12小时**，**8个不同随机种子**，每1,000步在**16个不同实例**上评估一次。报告AULC（Area Under Learning Curve）和学习曲线（平均回合回报±标准差）。

### 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量和具体训练时长。仅致谢中提到使用了HPC集群HSUper，但未提供硬件细节。因此，本文的算力资源信息不完整，无法进行精确比较。

### 5. 实验数量与充分性

- **实验数量**：
  - 主实验：在**6个领域变体**（HardMove4/8/10, CatchPoint, HardGoal, Platform）上对比5种基线，共30组对比。
  - 消融实验：对KGRL进行**8种消融**（训练/评估时分别或同时去除知识库、去除梯度指导，以及两者都去除），每个消融配置同样在6个域上重复8种子，共48×8=384组实验（估计）。
- **充分性**：
  - **充分**：遵循Vranješ等人（2024）的实证ML研究设计原则，报告均值±标准差，使用AULC和学习曲线两个指标，方差显示一致性。
  - **客观公平**：所有方法在相同预算和评估协议下比较，消融实验系统性地分离了知识库和梯度指导的贡献。
- **局限性**：实验仅在模拟基准域上进行，未包含真实世界或更大规模的任务；未提供计算成本对比（如时间、参数规模）。

### 6. 论文的主要结论与发现

- **性能提升**：KGRL在所有域上取得**最高平均AULC**，超过所有基线。学习曲线显示KGRL从一开始就有更高回报，且收敛更快、方差更低。
- **知识库作用**：知识库**主要影响部署阶段**（去除知识库后性能显著下降），但在训练阶段的影响较小甚至在某些域负面。知识库的利用率（K-utilization）高，尤其在HardMove域上动作剪枝率约50%，显著简化决策空间。
- **梯度指导作用**：梯度参数精化**在部署时明显提升性能**，但在训练时引入额外噪声，导致训练阶段性能下降（消融实验表明训练时去除梯度指导反而更好）。
- **可解释性**：通过记录规则激活，KGRL能提供**局部程序性解释迹线**，说明为何动作被剪除或参数被约束，这是其他PAMDP算法不具备的。
- **鲁棒性**：KGRL在不同种子和域上表现出较低的方差，说明其训练更稳定。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：
  - 首次将符号知识库（Datalog）集成到PAMDP的RL中，处理参数化动作空间。
  - 神经符号结合：知识保障安全、剪枝动作空间；梯度优化提供高效参数精化。
  - 提供**解释迹线**，增强可解释性和安全性，适用于安全关键应用。
- **实验设计亮点**：
  - 对知识库和梯度指导分别进行**训练/部署阶段的消融**，清晰揭示各自贡献的时机。
  - 使用AULC和学习曲线两种指标，全面评估样本效率与最终性能。
  - 遵循可复现性准则，开源代码（GitHub），有利于后续工作。

### 8. 不足与局限

- **实验覆盖有限**：仅限四个模拟域（均为2D连续控制），未在更复杂或真实任务（如机器人、工业控制）上验证，泛化性待评估。
- **知识库与抽象函数手工设计**：依赖专家手工编写规则和阈值抽象函数，可能不适用于动态变化或复杂的真实环境；学习它们会导致风险（错误规则剪除最优动作）。
- **梯度优化的局限性**：PARAM OPT在非凸Q函数上不能保证全局最优；训练时使用可能有害，需要课程学习等策略缓解。
- **可解释性层面**：解释迹线仅限于**局部过程性**解释，缺乏全局聚合和统计总结。
- **计算成本未量化**：未报告KGRL与基线的时间/FLOPs对比，虽然PARAM OPT可能增加部署计算开销。
- **对错误知识敏感**：错误的规则或抽象函数会导致永久性剪枝错误，影响最终性能。
- **一个域中知识利用不足**：Platform域的动作剪枝率为0，说明该域规则仅约束参数，知识基贡献有限。

（完）

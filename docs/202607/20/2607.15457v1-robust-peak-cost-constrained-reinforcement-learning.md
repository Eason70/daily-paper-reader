---
title: Robust Peak-cost Constrained Reinforcement Learning
title_zh: 鲁棒峰值成本约束强化学习
authors: "Shilpa Mukhopadhyay, Sourav Ganguly, Santosh Mohan Rajkumar, Honghao Wei, Debdipta Goswami, Arnob Ghosh"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15457v1"
tags: ["query:safe-rl-cbf"]
score: 8.0
evidence: 针对安全关键应用的鲁棒峰值代价约束强化学习
tldr: 针对安全关键应用中单次大违规可能造成灾难的问题，本文研究鲁棒峰值成本约束强化学习。与标准CMDP不同，峰值成本约束MDP可能不存在零对偶间隙，为此提出替代优化框架与基于积分概率度量的鲁棒值估计方法。理论证明在适当超参数下，替代解能实现相同鲁棒奖励且约束违反不超过epsilon。实验表明该方法在动力学扰动下有效保证安全并保持良好奖励性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15457v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 834, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15457v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15457v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 800, \"height\": 441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15457v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 456, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15457v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1816, \"height\": 1910, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15457v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 869, \"height\": 816, \"label\": \"Table\"}]"
motivation: 标准CMDP基于期望累积成本无法捕捉单次大违规的灾难性后果，且现有方法对偶性质不清。
method: 提出替代优化框架与基于积分概率度量的鲁棒值估计方法，解决松弛对偶间隙与动力学模型不匹配。
result: 替代解在适当超参数下达到原始问题相同鲁棒奖励，约束违反不超过epsilon。
conclusion: 该方法在动力学扰动下有效平衡安全与奖励性能。
---

## 摘要
我们研究了鲁棒峰值成本约束强化学习（RP-CRL），其目标是在控制轨迹中遇到的最大成本的同时最大化期望奖励。该设置受到安全关键应用的启发，在这些应用中，单个大的违规可能是灾难性的，因此无法被基于期望累积成本的标准CMDP框架充分捕捉。现有的可达性约束RL方法采用基于拉格朗日的方法，然而峰值成本约束MDP的潜在对偶性质仍不清楚。我们表明，与标准CMDP不同，峰值成本约束MDP可能不允许零对偶间隙。我们进一步考虑了一种鲁棒公式化，以解决转换动态中的模拟器-现实世界不匹配问题。为了解决这个问题，我们开发了一种替代优化框架和一种基于积分概率度量的鲁棒价值估计方法。我们证明，通过适当的超参数选择，替代解决方案在最多违反约束epsilon的情况下，实现了与原始问题相同的鲁棒奖励值。实验表明，所提出的方法在动态扰动下有效保证了安全性，同时保持了强大的奖励性能。

## Abstract
We study robust peak-cost constrained reinforcement learning (RP-CRL), where the objective is to maximize expected reward while controlling the maximum cost encountered along a trajectory. This setting is motivated by safety-critical applications in which a single large violation can be catastrophic and therefore cannot be adequately captured by the standard CMDP framework based on expected cumulative cost. Existing reachability-constrained RL methods adopt Lagrangian-based approaches, yet the underlying duality properties of peak-cost constrained MDPs remain unclear. We show that, unlike standard CMDPs, peak-cost constrained MDPs may not admit zero duality gap. We further consider a robust formulation to address simulator-to-real-world mismatch in the transition dynamics. To solve this problem, we develop a surrogate optimization framework and a robust value estimation method based on integral probability metrics. We prove that, with appropriate hyperparameter choices, the surrogate solution attains the same robust reward value as the original problem while violating the constraint by at most epsilon. Experiments show that the proposed method effectively enforces safety under dynamics perturbations while retaining strong reward performance.

---

## 论文详细总结（自动生成）

好的，遵照您的要求，以下是对给定论文的详细中文总结。

### 一、论文的核心问题与整体含义

- **研究动机**：标准的约束马尔可夫决策过程（CMDP）框架通过限制**期望的累积成本**来保障安全。然而，在许多安全关键应用中（如自动驾驶、机器人），单次巨大的、瞬时的安全违规（例如，一次猛烈碰撞）比多次小违规的总和更具灾难性。CMDP框架无法捕捉这种“峰值成本”的风险。
- **核心问题**：
    1.  **理论缺陷**：针对峰值成本约束的强化学习（如Reachability-constrained RL, RCRL）通常采用拉格朗日方法，但缺乏其对偶性质的严格证明。标准CMDP的零对偶间隙性质在峰值成本约束下是否成立尚不明确。
    2.  **鲁棒性缺失**：现有的峰值成本约束方法未考虑**模拟器到真实环境（sim-to-real）的动态不匹配**。一个在模拟器中满足峰值约束的策略，在真实环境中可能因动力学扰动而严重违规。
- **整体含义**：本工作旨在理论上阐明峰值成本约束MDP的对偶性质，并提出一个**鲁棒的**峰值成本约束RL框架（RP-CRL），以在存在环境动态不确定性的情况下，同时保证任务奖励的最大化和峰值成本的严格约束。

### 二、论文提出的方法论

- **核心思想**：
    1.  **揭示理论性质**：首先证明峰值成本约束MDP**不一定具有零对偶间隙**，这解释了为何现有的拉格朗日方法可能无法找到全局最优解。
    2.  **替代优化框架**：为解决非凸和非零对偶间隙问题，提出一个替代优化问题。该问题将原始的多目标优化（最大化奖励 + 满足峰值约束）转化为一个单目标，能够根据当前策略的可行性自动在优化奖励和优化约束之间切换。
    3.  **鲁棒值估计**：引入基于**积分概率度量**的鲁棒值函数估计方法，以处理动力学不确定性。通过在鲁棒贝尔曼算子中加入一个正则项，可以有效地估计在最坏情况下的奖励和价值函数。

- **关键技术细节**：
    1.  **对偶间隙证明**：通过一个双状态、双动作的MDP反例，严格证明峰值成本约束问题存在严格大于零的对偶间隙。
    2.  **替代问题公式化**：定义替代优化目标为 `min_π max{ (max_P Jπ,r / β) , (max_{P,s} Vπ,c_peak(s) - b) }`。其中`β`是一个超参数。根据命题1，当`β`足够大时，该替代问题的最优解能使原始问题的约束违反在`ε`之内，且奖励不差于原始最优解。
    3.  **鲁棒贝尔曼算子**：基于IPM，推导出鲁棒价值函数的更新方式。对于线性函数逼近，奖励和峰值成本的价值函数都通过加入与权重`w`范数成正比的惩罚项来实现鲁棒性。
        - 奖励TD误差：`δ_{t,r} = r_t + γV_r(s_{t+1}) - V_r(s_t) + ρ_r ||w_{r,2:d}||_2`
        - 峰值成本TD误差：`δ_{t,c} = (1-γ)c_t + γ max(c_t, V_{c,peak}(s_{t+1})) - V_{c,peak}(s_t) + ρ_c ||w_{c,2:d}||_2`
    4.  **算法流程（RP-CRL）**：
        - **采样**：使用当前策略在环境中收集轨迹。
        - **目标选择**：计算平均奖励项`A = mean(V_r/β)`和峰值成本约束违反项`B = max(V_{c,peak} - b)`。
        - **角色切换**：选择`A`和`B`中的较大者作为优化方向。
            - 若`B`更大（约束被违反），则更新策略以减少峰值成本。
            - 若`A`更大（约束满足），则更新策略以最大化奖励。
        - **更新**：使用PPO风格的裁剪目标，基于上述选择的方向对应的优势函数更新Actor；相应地更新奖励Critic和峰值成本Critic，其损失函数基于鲁棒TD误差的GAE计算。

### 三、实验设计

- **实验场景**：
    1.  **修改版CartPole**：在标准CartPole基础上加入成本函数，惩罚小车超出安全位置（|x| > 1.0）的偏差，并在超出终止界限时给予高额成本。目标是在保持峰值成本低于基线（b=2.0）的同时最大化回报。
    2.  **MuJoCo套件**：在Ant, HalfCheetah, Humanoid, Swimmer四个标准连续控制任务上评估。成本定义不同（如最大绝对动作扭矩、能量消耗）。
- **基准方法**：
    1.  **Primal-Dual (PD)**：文献[5]中用于峰值成本约束的原始对偶方法。
    2.  **Surrogate Objective (NP)**：在**非扰动**环境下训练的、使用本文提出的替代目标（公式6）的方法。
- **对比方法与设置**：
    - **RP-CRL**：在**扰动**环境下训练的、使用替代目标的方法。
    - **扰动设置**：在CartPole中，训练时对重力g加入`N(0, 0.5)`的噪声。在MuJoCo中，对不同环境加入重力或粘滞系数的扰动。
    - **推理测试**：使用更强的扰动（如`N(0, 2.0)`的噪声）来测试策略的鲁棒性。
- **评估指标**：累计回报（Returns）和轨迹最大峰值成本（Peak-Cost）。

### 四、资源与算力

论文中**未明确提及**具体使用的GPU型号、数量、单次训练时长等算力信息。只提到Actor和Critic网络使用学习率3e-4，且所有网络为三层前馈神经网络。

### 五、实验数量与充分性

- **实验组数**：
    1.  CartPole环境上，与PD和NP方法在**训练**和**推理**阶段进行了对比。
    2.  CartPole上，对两种不同的成本函数（C1, C2）进行了消融研究。
    3.  CartPole上，对两种不同强度的环境扰动（σ=0.5, 2.0）进行了消融研究。
    4.  MuJoCo套件上，在4个不同环境上进行了训练和推理阶段的对比实验。
- **充分性评价**：实验设计较为全面。
    - **对比公平**：与主流基线（PD）和无鲁棒版本（NP）对比，能清晰地显示贡献。
    - **覆盖率广**：从简单的CartPole到复杂的MuJoCo套件，提升了结论的普适性。
    - **消融充分**：针对成本函数和扰动水平的变化进行了测试，验证了方法的鲁棒性和泛化能力。
    - **客观性**：实验结果图清晰地展示了均值与方差，结果分析客观。
    - **综合评价**：实验设计比较充分，有力地支撑了“RP-CRL在保证峰值约束和最优奖励方面优于现有方法”的核心结论。

### 六、论文的主要结论与发现

1.  **理论发现**：峰值成本约束MDP可能不存在零对偶间隙，这是首次严格证明其与标准CMDP的理论差异。
2.  **方法有效性**：提出的替代优化框架能够有效绕过非零对偶间隙问题，通过适当的超参数`β`选择，可以找到近似最优且满足约束的鲁棒策略。
3.  **鲁棒性**：在动力学环境扰动下，基于IPM的鲁棒值估计方法使得RP-CRL训练的模型，即便在更强的干扰下，也能持续满足峰值成本约束并获得更高的奖励，显著优于在非扰动环境下训练的策略。
4.  **通用性**：该方法在不同成本函数、不同扰动水平和多个MuJoCo环境下均表现良好，证明了其通用性和稳健性。

### 七、优点

1.  **理论贡献突出**：首次严格证明了峰值成本约束MDP不满足零对偶间隙，为理解该问题的本质提供了理论基石，并解释了现有方法的局限性。
2.  **方法创新点明确**：将替代优化框架和基于IPM的鲁棒值估计相结合，设计了一个完整的求解方案。这种“理论驱动+鲁棒性考量”的思路非常清晰。
3.  **实验设计系统**：实验验证了理论发现（PD方法性能不足），并全面展示了在不同情况下的鲁棒性提升，证据链完整。

### 八、不足与局限

1.  **缺少收敛性分析**：论文承认“对提出方法的收敛性保证的特征化留待未来工作”，这是一个重要的局限。用户无法得知RP-CRL算法的迭代收敛速度或理论最优性保证。
2.  **扰动类型**：实验中仅考虑了重力和粘滞系数的随机扰动，未讨论其他类型的动态不确定性（如模型参数错误、结构突变等），鲁棒性的泛化边界有待进一步探索。
3.  **超参数敏感性**：算法性能严重依赖于超参数`β`的选择。虽然理论给出了一个大致的范围（β ≥ Cmax/ε），但在实际应用中如何高效地调优`β`仍是一个挑战。
4.  **实验规模**：实验环境（CartPole, MuJoCo）规模相对有限。对于更复杂的、具有高维状态空间和长时域任务的安全关键系统（如自动驾驶、无人机），算法的扩展性和有效性尚需验证。

（完）

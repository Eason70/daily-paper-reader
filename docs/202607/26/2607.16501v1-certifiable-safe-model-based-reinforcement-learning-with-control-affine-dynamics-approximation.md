---
title: Certifiable Safe Model-Based Reinforcement Learning with Control-Affine Dynamics Approximation
title_zh: 基于控制仿射动力学近似的可认证安全模型强化学习
authors: "Hao Zhou, Yanze Zhang, Cameron Reid, Wenhao Luo"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16501v1"
tags: ["query:safe-rl-cbf"]
score: 9.0
evidence: 可认证安全模型基强化学习结合控制理论安全保证
tldr: 安全模型基强化学习旨在平衡安全探索与任务效率，但现有方法常忽略残余模型不确定性的控制仿射结构，导致过于保守或无效保证。本文提出新框架，使用控制仿射随机傅立叶特征（ARFF）高效建模动力学，并采用自适应共形预测（ACP）量化由模型不确定性引起的安全约束不确定性，进而结合控制障碍函数（CBF）合成可认证安全策略。在推车杆和3D四旋翼仿真中，该方法成功实现了安全探索与高效控制。该工作为数据驱动的安全强化学习提供了兼具计算效率和严格安全性保证的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16501v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 652, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16501v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1797, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16501v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1789, \"height\": 512, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16501v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1798, \"height\": 344, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16501v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 878, \"height\": 1017, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16501v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 904, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16501v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 710, \"height\": 127, \"label\": \"Table\"}]"
motivation: 现有安全强化学习方法常忽略残余模型不确定性的控制仿射结构，导致过于保守或无效的安全保证。
method: 使用ARFF学习控制仿射动力学，ACP量化安全约束不确定性，并结合CBF合成可认证安全策略。
result: 在推车杆和3D四旋翼仿真中验证了框架的有效性与安全性保证。
conclusion: 实现了数据驱动的可认证安全强化学习，同时兼顾计算效率与严格安全性保证。
---

## 摘要
安全模型强化学习常将控制理论分析与强化学习相结合，使机器人能够安全地探索（部分）未知的系统动力学，同时导出控制动作以提高任务效率。控制性能和安全性保证通常依赖于部分建模的名义系统动力学的先验知识以及补偿残余模型不确定性的数据驱动模型。然而，现有方法往往忽略了残余模型不确定性的结构（例如，控制仿射分量），这可能导致在基于安全的学习控制器下过于保守的机器人行为或无效的安全保证。本文提出了一种安全强化学习框架，该框架使用控制屏障函数学习控制仿射动力学，并带有可认证的数据驱动安全策略。具体地，我们首先使用控制仿射随机傅里叶特征对机器人动力学进行建模，使其呈控制仿射形式，这提供了随数据集大小缩放的计算效率，并减少了模型基强化学习的潜在模型偏差。然后，应用一种使用自适应共形预测的无模型、高效的不确定性量化方法来量化由学习到的控制仿射动力学引起的安全约束中的不确定性。这允许通过CBF实现原则性且高效的控制器综合，从而实现数据驱动的安全保证。在小车-倒立摆和3D四旋翼平台上的仿真结果证明了所提出框架的有效性。

## Abstract
Safe model-based reinforcement learning (RL) often bridges control-theoretic analysis and RL for robots to safely explore (partially) unknown system dynamics while deriving control actions for task efficiency. The control performance and safety assurance typically rely on prior knowledge of partially modeled nominal system dynamics and the data-driven models that compensate for residual model uncertainties. However, existing methods often overlook the structure of residual model uncertainties (e.g., components affine in control), which could lead to overly conservative robot behaviors or invalid safety guarantees under the safe learning-based controllers. This paper proposes a safe reinforcement learning framework that learns control-affine dynamics with a certifiable data-driven safe policy using control barrier functions (CBF). Specifically, we first use Control-Affine Random Fourier Features (ARFF) to model robot dynamics in a control-affine form, which offers computational efficiency that scales with dataset size and reduces potential model bias for model-based reinforcement learning. Then, a model-free, efficient uncertainty quantification method using adaptive conformal prediction (ACP) is applied to quantify the uncertainty in the safety constraint arising from the learned control-affine dynamics. This allows for data-driven safety assurance amenable to principled and efficient controller synthesis with CBF. Simulation results on the cartpole and the 3D quadrotor platforms demonstrate the effectiveness of the proposed framework.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

安全模型强化学习（Safe MBRL）旨在让机器人在探索未知动力学的同时保证安全，通常结合控制理论（如控制障碍函数CBF）与数据驱动模型。现有方法存在两个关键问题：

- **忽视残余动力学不确定性的控制仿射结构**：许多工作将未知动力学 `d(x,u)` 假设为仅依赖于状态 `x` 而非控制输入 `u`，忽略了其可能包含控制仿射分量 `˜f(x) + ˜g(x)u`，这导致模型偏差，进而使CBF约束无效或过于保守。
- **不确定性估计不准确或计算昂贵**：高斯过程（GP）虽能提供方差，但计算复杂度随数据增长；随机傅里叶特征（RFF）近似GP会低估不确定性，导致安全保证不可靠；传统共形预测（CP）要求数据可交换性，在线学习场景下常被违反。

论文旨在提出一种**可认证安全**的模型强化学习框架，同时解决模型结构偏差与不确定性校准问题，实现高效、安全的在线学习与控制。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 使用**控制仿射随机傅里叶特征（ARFF）** 显式学习控制仿射形式的未知动力学，保留控制仿射结构，便于CBF优化。
- 采用**自适应共形预测（ACP）** 动态校准预测不确定性，将机会约束转化为确定性的不确定性感知安全约束，实现可认证安全保证。
- 结合**乐观探索（Thompson采样）** 与**模型预测路径积分（MPPI）** 规划器，平衡探索与利用。

### 关键技术细节

1. **控制仿射动力学学习**：
   - 模型：`d(x_k, u_k) = ω^T ϕ(x_k, u_k)`，其中 `ϕ(x_k, u_k) = [φ_1(x_k), …, φ_{m+1}(x_k)] [u_k^T, 1]^T`，`φ_i` 为RFF基函数。
   - 权值 `ω` 通过岭回归解析求解（式14-15），计算复杂度不随数据规模增长。
   - 预测时控制输入 `u` 被显式解耦，使优化问题保持凸性。

2. **基于ACP的不确定性量化**：
   - 定义非一致性分数 `S_k = |G^*(x_k,u_k) - G(x_k,u_k)|`，其中 `G` 预测安全屏障值，`G^*` 为真值。
   - ACP动态更新分位数 `α_{k+1} = α_k + δ(α_c - e_k)`，生成预测区间 `[G - S^{(r)}, G + S^{(r)}]`。
   - **定理1**：为满足 `P(G^* ≥ 0) ≥ 1-α`，充分条件是 `-S^{(r)} + G(x_k,u_k) ≥ 0`，从而将机会约束转化为确定性的下级约束。

3. **安全控制综合**：
   - MPPI生成参考控制输入 `u^{ref}`。
   - 求解二次规划（式17）得到安全控制 `u_k^{safe}`，约束包括：CBF条件 + ACP不确定性偏移项 `S^{(r)}` + 控制限幅。
   - 由于动力学呈控制仿射，优化问题可转为凸优化，用CVXPY高效求解。

4. **算法流程（Algorithm 1: SafeCARL）**：
   - 每回合：采样Thompson模型权重 → MPPI规划 → ACP-CBF安全筛选 → 执行 → 收集数据 → 更新岭回归参数及ACP分位数。

## 3. 实验设计：数据集/场景、benchmark、对比方法

### 场景与任务
1. **Cartpole（倒立摆）**：利用Gymnasium环境，控制目标是保持直立（`x_goal = [0,0,180°,0]`），通过注入不准确的标称参数（质量、长度）模拟未知动力学。
2. **3D Quadrotor Stabilization（四旋翼稳定）**：动力学基于微分平坦性，标称质量偏差和环境风（`sin p`）作为未知动力学。目标：到达三维目标位置 `[1.5,2.0,3.0]`。
3. **3D Quadrotor Tracking（轨迹跟踪）**：跟踪“8”字形轨迹，安全约束重新定义。

### 对比方法
- **MPPI-GT**：使用真实动力学（上界参考）。
- **LC3 (MPPI-RFF)**：采用标准RFF（非控制仿射）学习动力学，无CBF。
- **MPPI-ARFF**：本文方法去掉安全约束（仅ARFF+MPPI）。
- **MPPI-ARFF-CBF**：本文方法去掉ACP不确定性校准（仅ARFF+CBF）。
- **SafeCARL（本文方法）**：完整ARFF+ACP+CBF。

### 评估指标
- **回报（Reward）**：与最优策略的差异，反映学习效率。
- **安全率（Safe%）**：在100次独立实验中 `h(x) > 0` 的比例。
- **最小h值**：记录所有回合中 `h(x)` 的最小值。
- **计算时间**：求解优化问题（式17）的平均耗时。

## 4. 资源与算力

论文未明确说明使用的GPU型号、数量或训练时长。在计算时间评估中，提到运行于 **AMD Ryzen 7960X 24核处理器**，求解式17的平均时间：Cartpole上0.38ms，UAV上0.44ms，相比RFF方法约快8倍。但未提及RL训练总时间或多轮实验的硬件配置细节。

## 5. 实验数量与充分性

- **重复次数**：Cartpole和UAV稳定任务均进行了 **100次独立实验** 以统计安全率，统计量充足。
- **消融实验**：系统性地对比了“无安全约束”、“仅CBF”、“CBF+ACP”三个层次，验证了每个模块的必要性。
- **任务多样性**：覆盖了2D和3D系统、稳定与跟踪两种控制目标，场景较全面。
- **公平性**：所有对比方法使用相同的超参数（特征数P=100，MPPI参数等），控制变量。
- **客观性**：安全率的定义明确（`h(x) > 0`），并报告均值和标准差，结果清晰可复现。

不足：未在真实机器人上验证，仅仿真；未提供奖励曲线或安全指标的详细统计检验（如显著性分析）。

## 6. 论文的主要结论与发现

1. **ARFF可有效学习控制仿射未知动力学**：相比标准RFF（LC3），ARFF在Cartpole和UAV上均取得更高回报，且收敛更快（约10个回合），表明显式保留控制仿射结构可减少模型偏差。
2. **ACP校准显著提升安全保证**：无ACP的CBF约束（MPPI-ARFF-CBF）在Cartpole上安全率仅19%，UAV上0%；加入ACP后达到100%和98%，接近预设失效概率α=0.02。
3. **计算效率优越**：优化问题为凸，求解时间比RFF方法快约8倍，适合在线部署。
4. **良好泛化能力**：UAV稳定实验中，从不同起始位置出发均能安全到达目标；跟踪任务中，SafeCARL轨迹比LC3更精准且保持安全。

## 7. 优点

- **方法创新性**：首次在安全MBRL中结合ARFF与ACP，解决了模型结构性偏差与不确定性校准两大痛点，理论分析（定理1）提供了可认证安全保证。
- **计算高效**：岭回归解析解 + 凸优化，避免了GP或非线性规划的昂贵计算，利于实时控制。
- **不确定性量化无模型**：ACP不依赖于具体预测模型的结构，仅需非一致性分数，通用性强。
- **实验充分**：多任务、多对比、多指标、多次重复，结论稳健。
- **工程友好**：算法流程清晰（Algorithm 1），代码可复现性好（引用CVXPY等开源工具）。

## 8. 不足与局限

- **仅仿真验证**：未在真实机器人（如四旋翼实物）上测试，实际环境中的传感器噪声、执行器延迟、模型失配等挑战未被覆盖。
- **实验覆盖有限**：仅测试了Cartpole和3D四旋翼两种系统，未涉及更复杂的高维系统（如机械臂、足式机器人）或存在障碍物的避碰场景。
- **安全集设计假设**：安全集 `C` 需预先定义（如椭圆区域），且CBF参数γ需手动设定；若安全集不精确或动态变化，方法可能失效。
- **ACP参数敏感性**：学习率δ和失效概率α需用户指定，未做敏感性分析；在线环境中α可能随分布偏移而变化，本文默认固定值。
- **理论保证的局限性**：定理1给出的是充分条件（充分而非必要），可能导致保守性；此外，ACP的边际覆盖保证仅在假设数据满足弱依赖条件下成立，论文未讨论严格验证。
- **未对比更先进的基线**：如基于GP的高斯过程CBF（Wang et al. 2018）、基于DRL的安全方法（PPO-Safety）等，仅与LC3和简单消融对比。
- **计算资源描述不足**：未提及RL训练总耗时、GPU使用情况，影响可复现性评估。

（完）

---
title: Optimal Safety Control using High-Order Control Barrier Functions
title_zh: 使用高阶控制屏障函数的最优安全控制
authors: "Neng Li, Zuodong Pan, Jiaxing Wang, Weiguo Xia, Wei Ren"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17032v1"
tags: ["query:safe-rl-cbf"]
score: 10.0
evidence: 使用高阶控制屏障函数的最优安全控制
tldr: 针对非线性系统最优安全控制，本文提出两种新型高阶控制屏障函数（HOCBF），实现显式安全控制器设计。结合向量Lyapunov函数方法，提出高阶控制Lyapunov函数（HOCLF）并解决与HOCBF的兼容性，确保镇定与安全双重目标。通过四旋翼导航算例验证了所提方法的有效性。该研究为复杂系统安全关键控制提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17032v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1669, \"height\": 549, \"label\": \"Figure\"}]"
motivation: 现有零化HOCBF设计复杂，需构造新型HOCBF以简化安全控制器。
method: 推导两种显式新型HOCBF，结合向量Lyapunov函数提出HOCLF，并处理其兼容性。
result: 成功实现非线性系统镇定与安全的联合最优控制，四旋翼导航验证效果。
conclusion: 所提HOCBF与HOCLF框架可有效保证系统安全性与稳定性。
---

## 摘要
本文通过提出新型高阶控制屏障函数（HOCBFs），研究了非线性控制系统的最优安全控制问题。与归零型高阶控制屏障函数不同，我们推导了两种新型HOCBFs，并以显式方式设计了安全控制器。接下来，我们采用向量Lyapunov函数方法，为镇定控制问题提出了一种新型高阶控制Lyapunov函数（HOCLF）。讨论了所提HOCBFs与现有HOCBFs之间的关系。随后，解决了所提出的HOCLF与HOCBF的兼容性问题，以确保同时实现镇定和安全控制目标，从而建立了最优控制器。最后，通过四旋翼飞行器导航问题的数值示例，验证了所得结果的有效性。

## Abstract
This paper investigates the optimal safety control problem of nonlinear control systems by proposing novel high-order control barrier functions (HOCBFs). Different from zeroing HOCBFs, two novel HOCBFs are derived and the safety controllers are designed in an explicit way. Next, we implement vector Lyapunov function approach to propose a novel high-order control Lyapunov function (HOCLF) for the stabilization control problem. The relations between the proposed and existing HOCBFs are discussed. Afterwards, the compatibility of the proposed HOCLF and HOCBF is addressed to guarantee the stabilization and safety control objectives simultaneously, and thus the optimal controller is established. Finally, a numerical example from the navigation problem of quadrotors is presented to illustrate the efficacy of the derived results.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 核心问题与整体含义（研究动机与背景）
- **研究动机**：实际非线性控制系统（如四旋翼飞行器、机器人）通常具有高阶动力学（二阶及以上）。传统的控制 Lyapunov 函数 (CLF) 与控制屏障函数 (CBF) 在处理高阶系统时，因 Lie 导数为零而失效。现有基于归零型高阶 CBF (zeroing HOCBF) 的方法存在保守性（安全集被缩小）且控制器设计复杂。
- **核心问题**：如何为高阶非线性系统设计兼具镇定与安全的最优控制器，同时避免传统方法的保守性并实现显式控制律。
- **整体含义**：该研究提出一种新的高阶控制屏障函数（HOCBF）与高阶控制 Lyapunov 函数（HOCLF）框架，能够直接处理高阶系统，扩大可用的安全集，并通过显式公式简化控制器设计，为安全关键控制（如无人机导航）提供了理论支撑。

#### 2. 方法论：核心思想、关键技术细节、算法流程
- **核心思想**：
  - 采用向量 Lyapunov 函数方法，将高阶导数耦合在一个向量不等式内，从而构造出新的高阶 CLF (HOCLF) 与两种高阶 CBF (HOCBF-I 和 HOCBF-II)。
  - 其中 HOCBF-I 通过“倒数型”递推关系 (式 12) 保证安全集（较小）；HOCBF-II 基于向量不等式直接保证原始安全集（更大），扩展了适用范围。
  - 通过将镇定控制器 (式 7) 与安全约束整合为凸优化问题 (式 20)，并利用 KKT 条件得到显式的最优控制律 (式 21,24)。
- **关键技术细节**：
  - **HOCLF 定义**：要求导数满足向量不等式 \( \dot{\mathbf{V}}(x) \preceq \Lambda \mathbf{V}(x) \)，其中 \(\Lambda\) 是 Hurwitz 矩阵，用于保证指数稳定性。
  - **HOCBF-I**：基于递归函数 \( \phi_i \) 定义，要求 \( \inf_u \{ L_f \phi_{r-1} + L_g \phi_{r-1} u \} \le \eta_r(1/\phi_{r-1}) \)。
  - **HOCBF-II**：使用向量形式 \( \dot{\mathbf{B}}(x) \preceq \Theta^\top \mathbf{B}(x) \)，直接对原始屏障函数 B(x) 的多次导数施加约束，得到更大的安全集。
  - **显式控制器**：镇定控制器 \( u_s \) 由式 (7) 给出（基于 Sontag 公式的推广）；安全修正项 \( \bar{u}^* \) 由式 (24) 给出，当约束激活时采用投影修正，否则保持镇定控制器。
- **算法流程**：
  1. 选取 HOCLF \( V \) 与 HOCBF \( B \) 候选函数。
  2. 验证其满足定义条件（如相对阶、Hurwitz 矩阵、初始状态正性）。
  3. 构造显式镇定控制器 \( u_s \) (式 7)。
  4. 计算状态相关的 \( \tilde{c}(x) \) 与 \( d(x) \)。
  5. 若 \( \tilde{c}(x) \le 0 \)，则最优控制器 \( u^* = u_s \)；否则 \( u^* = u_s - \frac{\tilde{c}}{|d|^2} d^\top \)。
  6. 应用 \( u^* \) 到系统，保证安全集正向不变且局部渐近稳定。

#### 3. 实验设计
- **系统模型**：四旋翼飞行器动力学模型 (式 29)，含位置、速度、姿态与角速度子系统，采用姿态内环+位置外环的设计，仅需设计位置控制器。
- **任务场景**：安全导航，避开一个球形障碍物，从起始点到达目标点 \( p_d \)。
- **安全集定义**：\( S = \{ p \in \mathbb{R}^3 : \|p - p_o\|^2 \ge r^2 \} \)，障碍物半径 2.35 m，安全余量 0.15 m。
- **方法对比**：
  - **Case 1**：本文提出的 HOCLF + HOCBF-I。
  - **Case 2**：本文提出的 HOCLF + HOCBF-II。
  - **Case 3**：现有的归零型 HOCBF [15]（Xiao & Belta, 2021）。
- **参数设置**：HOCLF 中 \( \lambda = [-10, -11]^\top \)；HOCBF-I 中 \( \eta_0(s)=2s, \eta_1(s)=10s \)；HOCBF-II 中 \( [\Theta_{21},\Theta_{22}] = [10, -10] \)；对照方法中 \( \gamma_1(s)=10s \)。
- **评价指标**：收敛时间、控制能量积分 \( U_i = \int_0^8 |u_i(t)| dt \)、计算时间（有界控制时）。
- **无提及 benchmark 数据集**，采用自行定义的导航场景。

#### 4. 资源与算力
- **硬件**：AMD Ryzen 9 5900HX (3.30 GHz) CPU，16 GB RAM，无独立 GPU。
- **软件**：MATLAB R2023a。
- **训练/仿真时长**：未报告具体时长，仅给出每个时间步的计算时间（6.11ms~6.87ms）。未提及大规模训练或 GPU 使用。

#### 5. 实验数量与充分性
- **实验数量**：仅一组场景（四旋翼导航），但设置了三种方法对比（Cases 1-3），相当于三个子实验。
- **充分性评价**：
  - **优点**：覆盖了两种新方法与现有方法的对比，呈现了位置轨迹、屏障函数值和控制输入时域图，并定量分析了控制能量与计算时间。
  - **不足**：仅测试单一场景（单个球形障碍、固定的起始/目标点），缺乏不同障碍复杂度、不同初始条件、多障碍环境或传感器噪声下的测试；未进行消融实验（如不同 HOCLF 参数、不同 Hurwitz 矩阵选择的影响）；未与更多基线方法（如基于 NLP 的求解器、基于 MPC 的安全控制）对比；未讨论控制有界情况下的数值优化精度。

#### 6. 主要结论与发现
- 所提 HOCLF+HOCBF-I 和 HOCLF+HOCBF-II 均能成功实现障碍物规避与位置镇定。
- Case 1 与 Case 3 收敛较快（约 4 秒），但 Case 2 收敛较慢（约 6.5 秒）。
- Case 2 的控制能量最小（\(U_2=383.18\)），而 Case 1 和 Case 3 分别为 400.92 和 399.28，说明 HOCBF-II 具有能量优势。
- 在有界控制约束时，Case 1 计算时间最短（6.11ms），Case 2 最长（6.87ms），但仍满足实时性要求。
- 提出了 HOCBF-镇定集的概念，并证明了其存在性，从而保证了局部渐近稳定。

#### 7. 优点
- **理论创新**：提出了两种显式的 HOCBF（倒数型与向量型），扩大了安全集范围，解决了归零型 HOCBF 的保守性。
- **控制器显式化**：基于 Sontag 公式和 KKT 条件，给出了无需求解在线二次规划的最优控制律（除非控制受约束），降低了计算复杂度。
- **兼容性分析**：通过 HOCBF-镇定集的概念，统一处理安全和镇定的冲突，弥补了现有高阶控制函数兼容性研究的空白。
- **实验验证充分**：通过四旋翼导航实例直观展示了三种方法的效果，并定量比较了能量消耗与计算时间。

#### 8. 不足与局限
- **实验覆盖不足**：仅使用单一场景、单次仿真，未进行蒙特卡洛统计或随机扰动测试，结果可能存在过拟合风险。
- **应用限制**：
  - 要求 HOCLF 与 HOCBF 候选函数的相对阶数匹配，且 Hurwitz 矩阵需满足特定不等式，设计有一定难度。
  - 初始状态需满足向量正性条件（如 \( \sum \omega_1^\top \pi_i B_i(x_0)>0 \)），限制了可用的初始状态范围。
  - 未考虑传感器噪声、模型不确定性、外部扰动等鲁棒性要求。
  - 控制器显式公式在控制输入受限时仍需求解 QP，计算时间有所增加。
- **理论局限**：仅研究了连续时间确定性问题，未扩展到分布式、随机系统、时滞系统等更复杂场景。
- **公平性**：对比方法 [15] 的参数（如 \( \gamma_1(s)=10s \)）可能未经过优化，导致对比可能不够公平；Case 2 的收敛较慢，未说明是否可以通过调节 HOCBF-II 参数来加速。

（完）

---
title: Robust Adaptive Backup Control Barrier Functions
title_zh: 鲁棒自适应备份控制障碍函数
authors: "Ersin Daş, David E. J. van Wijk, Tamas G. Molnar, Aaron D. Ames, Joel W. Burdick"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20842v1"
tags: ["query:safe-rl-cbf"]
score: 9.0
evidence: 鲁棒自适应备份控制屏障函数用于参数不确定下的安全控制
tldr: 针对非线性控制仿射系统存在参数不确定性的安全问题，提出鲁棒自适应备份控制障碍函数。该方法通过认证自适应估计器在线估计未知参数并给出分量级误差界，利用估计模型计算备份流并以此收紧安全条件；对于执行矩阵不确定性，采用对偶重新表述使得安全滤波器可转化为二次规划。理论证明满足所提约束的控制器能在参数不确定性与输入约束下保证系统安全。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20842v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 861, \"height\": 730, \"label\": \"Figure\"}]"
motivation: 备份控制障碍函数依赖模型预测，参数不确定性会导致预测轨迹失准，进而无法保证系统安全。
method: 使用认证估计器提供参数自适应律与误差界，基于估计模型收紧备份流安全条件；引入对偶重写以处理执行矩阵不确定性。
result: 设计出二次规划安全滤波器，并证明其约束满足时系统在参数不确定与输入约束下保持安全。
conclusion: 所提方法有效处理模型参数不确定性，扩展了备份控制障碍函数在不确定非线性系统中的适用范围。
---

## 摘要
我们提出了一种鲁棒自适应备份控制障碍函数的概念，用于在漂移动力学和驱动矩阵中均存在参数不确定性的非线性控制仿射系统。备份控制障碍函数通过预测系统在预先认证的安全控制器下的轨迹来保证安全性。然而，这些预测依赖于模型，当系统包含未知参数时可能不准确。为解决此问题，我们使用逐元素认证的自适应估计器来估计未知参数，该估计器提供参数自适应律和分量级估计误差界。我们利用估计模型计算备份流，并使用这些认证边界收紧安全条件。所得安全条件考虑了预测流对参数估计误差的敏感性。此外，为处理驱动矩阵的不确定性，我们采用基于对偶性的重构，从而能够使用计算高效的基于二次规划的安全滤波器。我们证明，满足所提出的鲁棒自适应备份控制障碍函数约束的控制器能在参数不确定性和输入约束下保证安全性。

## Abstract
We propose a notion of robust adaptive backup control barrier functions for nonlinear control affine systems with parametric uncertainty in both the drift dynamics and actuation matrix. Backup control barrier functions guarantee safety by predicting the system's trajectory under a pre-certified safe controller. However, these predictions rely on the model and can be inaccurate when the system contains unknown parameters. To address this issue, we estimate the unknown parameters using element-wise certified adaptive estimators that provide a parameter adaptation law and component-wise estimation error bounds. We compute the backup flow using the estimated model and tighten the safety conditions using these certified bounds. The resulting safety conditions account for the sensitivity of the predicted flow to parameter estimation errors. Moreover, to handle uncertainty in the actuation matrix, we use a duality-based reformulation that enables the use of a computationally efficient quadratic-program-based safety filter. We prove that controllers satisfying the proposed robust adaptive backup control barrier function constraints guarantee safety under parametric uncertainty and input constraints.

---

## 论文详细总结（自动生成）

# 论文总结：鲁棒自适应备份控制障碍函数

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：备份控制障碍函数（bCBF）通过预测系统在预先认证的安全控制器下的轨迹来保证安全性，该方法依赖精确模型。当系统存在参数不确定性（包括漂移动力学和执行矩阵），模型预测的备份轨迹与真实轨迹可能严重偏离，导致安全保证失效。
- **关键挑战**：参数不确定性不仅出现在漂移项，还出现在执行矩阵（即控制输入方向）。后者使得安全约束中不确定性直接与决策变量（控制输入）耦合，传统的鲁棒处理方法更复杂。
- **论文目标**：提出一种**鲁棒自适应备份控制障碍函数**框架，能够在参数不确定性下，利用在线自适应估计器估计未知参数，并利用估计误差的认证界来收紧安全条件，从而保证输入受限非线性系统的安全性。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 使用**逐元素认证自适应估计器**（如DREM）在线估计未知参数，并输出分量级的估计误差界。
- 基于当前参数估计值向前传播**估计的备份流**，并利用估计误差界推导**真实备份流与估计备份流之间的偏差**（通过Gronwall不等式）。
- 用该偏差构造**收紧项**，建立原始安全函数的保守下界，从而定义**内逼近的隐式安全集**。
- 针对执行矩阵不确定性导致安全约束中包含未知参数乘以输入的问题，采用**对偶重构**将鲁棒约束转化为一组等价的线性不等式，从而保持整个安全滤波器为凸二次规划（QP）。

### 关键技术细节
1. **系统建模**：假设不确定性为参数仿射形式：`Δf(x)=F(x)θ_f`，`Δg(x)u= Σ G_j(x)u θ_{g,j}`，合并为`ϕ(x,u)θ`。
2. **估计器类（Definition 1）**：要求估计器提供自适应律`˙θ̂ = q`，并且满足`|θ_i - θ̂_i(t)| ≤ ρ_i(t)`，其中`ρ_i(t)`是已知的认证界。
3. **备份流误差界（Lemma 2）**：利用Lipschitz常数和Gronwall不等式，导出`‖φ_b(τ,x) - φ̂_b(τ,x,t)‖ ≤ δ_φ(τ,x,t)`，其中`δ_φ`由积分形式给出，依赖认证界`ρ_i(t)`。
4. **收紧安全条件**：定义`¯h = h(φ̂_b) - ϵ`，`¯h_b = h_b(φ̂_b(T)) - ϵ_b`，其中`ϵ = L_h δ_φ`等。则约束`˙¯h ≥ -α(¯h)`能保证真实系统安全。
5. **对偶重构（公式(31)-(34)）**：将包含未知误差`˜θ`的约束`a + c^T ˜θ ≥ 0`转化为存在对偶变量`μ`满足`a + b^T μ ≥ 0`且`μ^T A = c^T, μ ≤ 0`，其中`A`和`b`由误差集定义。此转换使约束保持为关于`u`和`μ`的线性（仿射）式，从而构成QP。
6. **安全滤波器（公式(35)）**：最小化`||u - k_p(x)||^2`，受限于输入界`U`以及所有离散时间点的对偶约束。

## 3. 实验设计

- **场景**：平面四旋翼（planar quadrotor）的载荷插入任务。四旋翼需避开墙面（水平位置下限）、保持最小高度和俯仰角界限。
- **数据与参数**：未知参数向量`θ ∈ R^6`（阻尼、重力偏差、质量倒数偏差、转动惯量倒数偏差、推力偏心）。真实参数使四旋翼比标称模型重47%。
- **基准方法**：
  - 主控制器`k_p`（级联位置-姿态控制器，无安全考虑）
  - 名义bCBF（使用标称模型`θ=0`，无鲁棒化）
- **对比内容**：轨迹在`p_x-p_z`平面上的安全性、控制输入是否满足约束、参数估计值随时间变化。

## 4. 资源与算力

- 论文**未明确提及**使用的GPU型号、数量、训练时长等计算资源。
- 仅说明仿真代码和参数在GitHub仓库中。作为控制理论验证性实验，通常使用CPU完成短时间仿真，算力需求较低。

## 5. 实验数量与充分性

- **实验数量**：主要展示一个代表性仿真场景（见图1）。未进行多组随机种子重复、不同参数配置或消融实验。
- **充分性评估**：在控制理论论文中，单案例可视化验证常见，但缺乏统计性结果和更复杂场景测试。充分性一般。不过论文提供了严格的数学证明，实验仅用于定性演示，因此可接受。
- **公平性**：对比的两种基线（主控制器、名义bCBF）均选择了合理且典型的对手，对比结果清晰。

## 6. 主要结论与发现

- 所提出的**鲁棒自适应bCBF**能够在参数不确定性和输入约束下**始终保证四旋翼处于安全集内**。
- 相比之下，主控制器和名义bCBF均导致安全违规（名义bCBF由于模型失配干预过晚）。
- DREM估计器提供的认证界随时间**收缩**，使收紧项逐渐减小，内逼近安全集逐渐接近理想隐式安全集。
- 安全滤波器始终满足输入界限（推力、力矩）。

## 7. 优点

- **方法创新性强**：首个将自适应估计的**分量级误差界**与备份CBF结合的工作，处理漂移和执行器双重不确定性。
- **理论保证完整**：证明了满足所提约束的控制器能保持安全集前向不变，且不需要持续激励条件。
- **计算轻量**：通过对偶重写保持QP形式，可利用快速求解器在线求解。
- **无模型假设保守**：估计误差界随数据积累自动减小，实现保守性渐近改善。

## 8. 不足与局限

- **实验覆盖有限**：仅测试平面四旋翼一个低维示例，未验证高维系统（如六旋翼、机械臂）或更复杂约束（如障碍物规避）。
- **流误差界的保守性**：利用Lipschitz常数和Gronwall不等式得到的`δ_φ`可能较松弛，尤其在系统发散或快速变化时。论文承认可借助收缩性、一类Lipschitz性等改进。
- **依赖预设计鲁棒备份集**：需要手动构造鲁棒备份控制器和备份集（如Proposition 1），对用户要求较高，自动化设计不足。
- **QP可行性问题**：初始参数框过宽或备份视界过长可能导致`bCI(t)`为空，需切换至备份控制器（论文提出切换策略但未验证平滑性）。
- **未讨论测量噪声**：假设状态完全可测，未考虑输出反馈情况。

（完）

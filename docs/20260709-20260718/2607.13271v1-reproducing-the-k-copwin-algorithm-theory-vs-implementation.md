---
title: "Reproducing the k-copwin Algorithm: Theory vs. Implementation"
title_zh: 重现k-copwin算法：理论与实现
authors: "Meagan Mann, Christian Muise, Erin Meger"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.13271v1"
tags: ["query:multi-agent"]
score: 9.0
evidence: 重现警察与强盗追逃博弈的k-copwin算法
tldr: 警察与强盗博弈是图论中经典的追逃问题，核心在于确定图是否由k个警察即可获胜（k-copwin）。本文复现了Petr等人提出的k-copwin算法，并用Python进行了具体实现。通过详细阐述算法步骤和关键细节，验证了理论正确性。该工作为算法的实际应用和部署提供了可靠的参考基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13271v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 894, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13271v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 912, \"height\": 512, \"label\": \"Figure\"}]"
motivation: 现有k-copwin算法缺乏公开实现，需复现以验证理论并促进实际应用。
method: 使用Python严格遵循Petr等人的算法设计，处理博弈状态转移和胜负判定逻辑。
result: 成功复现算法，实现能正确判定给定图是否为k-copwin，复现结果与理论一致。
conclusion: 该实现澄清了算法细节，为图论博弈的实证研究提供了可复用的工具。
---

## 摘要
警察与强盗是一个被广泛研究的追逃游戏，为图论与理论计算提供了深刻见解。其核心问题在于确定抓捕强盗所需的最少警察数量，即警察数。我们专注于复现Petr、Portier和Versteegen在2022年提出的一种算法，该算法能高效判定一个图是否为$k$-警察必胜图。本文介绍了该$k$-警察必胜算法的Python实现。工作中，我们详细阐述了实现过程，澄清了算法的关键方面，并讨论了其对未来实际部署的意义。

## Abstract
Cops and Robbers is a well-studied pursuit-evasion game that provides insights into graph theory and theoretical computing. A central question is determining the minimum number of cops required to capture the robber, known as the cop number. We focus on reproducing an algorithm proposed by Petr, Portier, and Versteegen in 2022, which efficiently determines whether a graph is $k$-copwin. This paper presents a Python implementation of the $k$-copwin algorithm. In this work, we present our implementation in detail, clarify key aspects of the algorithm, and discuss its implications for future practical deployments.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：警察与强盗（Cops and Robbers）是图论中经典的追逃博弈。确定一个图是否可由 \(k\) 个警察保证抓获强盗（即 \(k\)-copwin）是核心问题。Petr、Portier 和 Versteegen 在2022年提出了一个比传统算法更高效的算法（复杂度 \(O(k n^{k+2})\)），但原论文仅给出伪代码，未提供可执行实现。
- **整体含义**：本文旨在复现该算法并给出完整的 Python 实现，通过实际操作揭示理论描述与实现之间的差异（如索引偏差、数据结构的初始化歧义），验证理论正确性，并为后续研究提供可复用的基准工具。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：将博弈建模为状态空间上的反向广度优先搜索（reverse BFS）。每个状态 \(s = (p_0, p_1, \dots, p_k, t)\) 记录强盗位置 \(p_0\)、 \(k\) 个警察的位置 \(p_1 \dots p_k\) 以及当前轮到谁移动（\(t\) 表示最后一个移动的玩家，\(t = k\) 表示强盗刚移动，\(t < k\) 表示第 \(t+1\) 号警察刚移动）。
- **关键技术细节**：
  - 数据结构：`COPS_WIN` 布尔数组标记已判定为警察必胜的状态；`COUNTER` 记录每个强盗回合状态中强盗剩余的安全移动选项数（初始值为 \(1 + \deg_G(p_0)\)）；`QUEUE` 用于BFS传播。
  - 初始化：遍历所有 \(n^{k+1}\) 个位置组合和 \(k+1\) 个回合索引，初始化`COUNTER`：仅在 \(t = k\)（强盗回合）时设置计数器，其他回合设为0。注意原论文伪代码误将 \(t=0\) 作为强盗回合，本文修正为 \(t=k\)。
  - 标记终端胜利状态：任何强盗与警察同顶点的状态直接加入QUEUE并标记为copwin。
  - 反向传播：
    - 若 \(t \neq 0\)（警察刚移动），所有前驱状态直接标记为copwin并加入队列。
    - 若 \(t = 0\)（强盗刚移动，即当前是警察回合），对每个前驱状态递减其`COUNTER`；当计数归零时，该前驱标记为copwin并入队。
  - 最终检查：是否存在一组警察初始位置，使得对所有强盗初始位置的状态均为copwin。
- **算法流程**：状态空间生成 → 初始化数据结构 → 入队终端胜利状态 → while循环处理QUEUE（依据turn类型执行直接标记或计数器递减） → 全局检查返回结果。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：所有2至9个顶点的连通图，共273,192个图，来自Anu数据库的graph6格式。
- **基准与对比方法**：
  - 2011年算法：基于Bonato和Nowakowski的《The Game of Cops and Robbers on Graphs》描述的算法，使用GitHub上已有的Python实现（Jabbath/Cop-Number）。
  - 2022年算法：本文实现的Petr等人的算法。
- **评估指标**：正确性（两算法判定的警察数是否一致）和平均运行时间。

### 4. 资源与算力

- 论文未明确说明使用的具体硬件资源（GPU型号、数量、训练时长等）。所有实验在CPU上运行，未提及GPU使用。

### 5. 实验数量与充分性

- **实验数量**：完整测试了所有2-9个顶点的连通图（1, 2, 7, 19, 69, 499, 8109, 264484个图分别对应n=2~9）。仅对比了两种算法在正确性和平均运行时间上的差异，未进行消融实验或超参数调优。
- **充分性分析**：
  - 正确性验证充分：所有图两算法结果完全一致，证实了复现算法的正确性。
  - 运行时间对比充分：展示了平均运行时随顶点数增长的曲线，并给出图数量作为背景。但仅测试了n≤9的图（因已知10个顶点以上才可能出现需要3个警察的图），未测试更大规模图的可扩展性。
  - 未进行不同k值的独立测试（所有图仅需k=1或2），也未分析空间消耗。

### 6. 论文的主要结论与发现

- 成功复现了Petr等人的k-copwin算法，并发现了原论文伪代码中的一个关键索引问题（将t=0误用作强盗回合，实际应为t=k），修正后算法逻辑正确。
- 实验证实复现的2022算法平均运行时间普遍低于2011年算法，显示出理论上的效率提升在实际中可体现。
- 强调了实现对于验证理论、澄清细节的重要性，指出理论描述与实现之间存在的“语义鸿沟”需要通过实际编码来弥合。

### 7. 优点：方法或实验设计上的亮点

- **全面覆盖小规模图**：使用所有2-9个顶点的连通图进行测试，结果具有统计显著性。
- **明确的正确性验证**：通过与已有的2011算法实现进行交叉验证，确保结果一致。
- **算法改进**：对初始化步骤进行了优化（仅对可能为终端胜利的位置组合进行遍历），减少了常数因子。
- **代码开源**：附完整Python代码（附录），便于其他研究者复现和扩展。

### 8. 不足与局限

- **实验规模有限**：仅测试至9个顶点，未涉及需要更多警察的图（如10个顶点以上），无法评估算法在更大k或更大n时的性能。
- **缺少消融实验**：未单独评估各优化技巧（如初始化优化）对运行时间的贡献。
- **未进行内存开销分析**：算法状态空间为 \(O(k n^{k+1})\)，对于较大n或k可能内存爆炸，文中未讨论。
- **对比基线单一**：仅对比2011年算法，未与其他更近期的k-copwin判定算法（如Clarke-MacGillivray算法等）进行比较。
- **硬件未报告**：未披露实验运行的机器配置，影响可重复性。
- **理论分析简化**：由于n≤9时cop number仅为1或2，实际验证仅区分copwin与非copwin，未能检验算法对更高k值的支持。

（完）

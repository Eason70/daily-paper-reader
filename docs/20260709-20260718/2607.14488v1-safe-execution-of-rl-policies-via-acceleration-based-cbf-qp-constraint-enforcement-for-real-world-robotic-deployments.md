---
title: Safe Execution of RL Policies Via Acceleration-Based CBF-QP Constraint Enforcement for Real-World Robotic Deployments
title_zh: 基于加速度的CBF-QP约束执行的RL策略安全部署：面向真实世界机器人应用
authors: "Bastien Muraccioli, Alice Cariou, Pierre-Alexandre Leziart, Mathieu Celerier, Arnaud Demont, Gentiane Venture, Mehdi Benallegue"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14488v1"
tags: ["query:safe-rl-cbf"]
score: 10.0
evidence: 基于加速度的CBF-QP安全滤波器用于强化学习策略
tldr: "强化学习策略在真实机器人部署中因缺乏安全保证而易在安全边界附近失败。本文提出Acc-CBF-QP，一种基于加速度的二次规划安全滤波器，利用控制障碍函数在运行时约束任意RL策略满足关节位置、速度、扭矩和碰撞约束，无需修改训练。通过引入TorqueTask和Forward DynamicsTask两种任务平衡安全与性能。在Kinova Gen3机械臂上完全消除约束违反，在Unitree H1人形机器人上违规次数降低92%，同时保持任务性能。该方法即插即用，代码开源，显著提升RL策略硬件部署安全性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14488v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 717, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14488v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 661, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14488v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 665, \"height\": 451, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14488v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 902, \"height\": 536, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14488v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 897, \"height\": 249, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14488v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 277, \"label\": \"Table\"}]"
motivation: 现有RL策略缺乏可部署的安全保证，约束违反导致硬件失败，亟需不修改策略的运行时安全滤波器。
method: 提出Acc-CBF-QP，构建加速度级CBF-QP统一优化框架，强制关节限位和碰撞约束，并设计TorqueTask和Forward DynamicsTask调控偏差。
result: "在Kinova机械臂完全消除违规，在H1人形上违规率降低92%，激进速度下生存时间延长，标称任务性能保持。"
conclusion: 基于加速度的CBF-QP安全滤波器可有效提升RL策略的真实硬件部署安全性和稳定性，代码开源供复用。
---

## 摘要
强化学习在解决复杂机器人控制问题方面展现了卓越的能力，但其缺乏安全保证严重限制了在硬件上的部署。特别是，当腿式机器人和机械臂通常在安全临界边界附近运行时，分布外状态可能导致部署失败。为此，我们提出了Acc-CBF-QP，一种基于加速度的二次规划安全滤波器，利用控制障碍函数在运行时将任何RL策略约束到安全集内，而无需修改训练过程。该方法适用于无约束和Safe-RL策略，并在统一的优化框架内强制执行关节位置、速度、扭矩和碰撞约束。一个关键贡献是RL+QP任务的公式化，当约束可能被违反时，这些任务会调节与RL指令的偏差。我们引入了最小化扭矩偏差的扭矩任务和最小化诱导加速度偏差的前向动力学任务，从而对安全-性能权衡提供原则性控制。在7自由度Kinova Gen3机械臂和19自由度Unitree H1人形机器人上的实验（包括仿真和硬件）显示，约束违反显著减少。在实际H1硬件上，单独的Safe-RL策略产生10.04次违反/秒，而通过Acc-CBF-QP增强后，违反次数减少了92%，降至0.80次/秒。在Kinova Gen3上，Acc-CBF-QP完全消除了违反。在无违反情况下，RL目标的标称任务性能得以保持。在H1上执行激进的速度指令时，Acc-CBF-QP通过防止约束引起的停机来改善执行，从而延长生存时间。完整流程已开源。

## Abstract
Reinforcement Learning (RL) has demonstrated remarkable capabilities for solving complex robotic control problems, but its lack of safety guarantees severely limits deployment on hardware. In particular, as legged robots and manipulators often operate near safety-critical boundaries, out-of-distribution states can lead to failure upon deployment. To address this, we introduce Acc-CBF-QP, an acceleration-based Quadratic Program (QP) safety filter using Control Barrier Functions (CBFs) that constrains any RL policy onto a safe set at runtime without modifying training. The method applies to unconstrained and Safe-RL policies, and enforces joint position, velocity, torque, and collision constraints within a unified optimization framework. A key contribution is the formulation of RL+QP tasks that regulate deviation from the RL command when constraints would otherwise be violated. We introduce a TorqueTask, minimizing torque deviation, and a Forward Dynamics Task, minimizing induced acceleration deviation, thus providing principled control over safety-performance trade-offs. Experiments on a 7-DoF Kinova Gen3 manipulator and a 19-DoF Unitree H1 humanoid, both in simulation and on hardware, highlight substantial reductions in constraint violations. On the real H1 hardware, a Safe-RL policy alone yielded 10.04 violations/s, which were reduced by 92% to 0.80 violations/s when augmented with Acc-CBF-QP. On the Kinova Gen3, Acc-CBF-QP fully eliminated violations. Nominal task performance of the RL objective is preserved in violation-free regimes. Under aggressive velocity commands on H1, Acc-CBF-QP improves execution by preventing constraint-induced shutdowns, yielding longer survival times. The full pipeline is open-source.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：强化学习（RL）策略在机器人控制中表现优异，但其**缺乏安全保证**，导致在真实硬件部署时，当遇到分布外（OoD）状态或接近安全边界时，容易发生约束违反（如超过关节限位、碰撞、扭矩超限），进而引发硬件损坏或安全事故。
- **研究动机**：现有安全方法主要分两类：(1) **训练时安全RL**（如CMDP、CaT）仅在期望上提供统计保证，分布偏移下保证退化，且修改约束需要重训练；(2) **运行时安全滤波器**（如CBF-QP）在理论上可提供前向不变性保证，但多为仿真验证或未直接对接RL策略输出。亟需一种**即插即用、不修改训练、能在真实机器人上运行的运行时安全层**。
- **整体含义**：本文提出的Acc-CBF-QP填补了RL与优化安全控制之间的鸿沟，使任何预训练RL策略（无约束或有约束）都能在硬件部署时严格满足物理约束，同时保持任务性能。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在加速度域构建一个**二次规划（QP）安全滤波器**，利用**控制障碍函数（CBF）** 在运行时将RL策略的扭矩指令投影到安全集内。滤波器以加速率为决策变量，约束包括关节位置、速度、扭矩限位和自碰撞，所有约束均表示为加速度的仿射不等式。RL策略的指令通过两种任务公式化为QP的代价函数，从而在约束违反时**最小化对RL原目标（扭矩或加速度）的偏离**，实现安全-性能的权衡。
- **关键技术细节**：
  - **机器人动力学与观测器**：使用广义动量观测器（GMO）估计外部扰动 \(\hat{\tau}_{\text{ext}}\)，用于补偿模型误差；对浮动基座机器人，利用Tilt Observer估计基座速度，并利用接触力假设计算锚点速度。
  - **CBF约束构建**：
    - 速度限位：使用相对度1的CBF条件 \(\dot{h}_{\text{vel}} + \lambda h_{\text{vel}} \geq 0\)。
    - 位置限位和碰撞：相对度2，采用指数CBF (ECBF)：\(\ddot{h} + \alpha_1 \dot{h} + \alpha_2 h \geq 0\)，参数化为 \(\alpha_1+\alpha_2=\lambda\)，\(\alpha_1\alpha_2=\lambda^2/(4\zeta^2)\)。
    - 扭矩限位：通过逆动力学将QP解出的加速度转为扭矩，并与上下限比较。
  - **两种QP任务**：
    - **TorqueTask**：最小化QP扭矩与RL扭矩之差，代价为 \(\|M\ddot{q} - (\tau_{\text{RL}} - D(q,\dot{q}))\|^2\)。
    - **Forward Dynamics Task (FDTask)**：最小化QP加速度与RL指令对应的前向动力学加速度之差，代价为 \(\|\ddot{q} - M^{-1}(\tau_{\text{RL}} - D(q,\dot{q}))\|^2\)。
  - **鲁棒性分析**：在扰动有界假设下，给出输入-状态安全保证（ISSf），即轨迹保持在安全集的 \(O(\bar{\delta}/\lambda)\) 邻域内。
  - **算法流程**：每个控制周期：获取状态 → 运行RL策略得到扭矩指令 → 通过QP求解满足CBF约束的最优加速度 → 逆动力学得到实际扭矩（对机械臂直接扭矩控制；对人形通过逆PD转换为位置指令）。

### 3. 实验设计：数据集/场景、基准、对比方法

- **平台与场景**：
  - **Kinova Gen3 7-DoF机械臂**：使用公开PPO策略（端到端到达任务），无安全约束。场景包括：
    - 正常条件（两个靶点间循环，动态/准静态相位）。
    - 分布外场景（改变初始配置导致关节限位违反）。
  - **Unitree H1 19-DoF人形机器人**：使用基于CaT的Safe-RL行走策略（仅控制腿部）。场景包括：
    - 真实硬件和仿真中的约束违反率评估（零速度指令下的漂移行走）。
    - 仿真中激进速度指令（平坦地面，减小碰撞裕度）的生存时间测试。
    - 仿真中不平地形（随机旋转方块）的鲁棒性测试。
- **基准（baseline）**：
  - 对机械臂：仅RL（无安全滤波器）。
  - 对人形：
    - RL-NST（无安全终止条件，仅终止于跌倒或超时，作为上界）。
    - RL（带安全终止条件）。
    - RL-Sat（扭矩饱和，作为硬件可行基线）。
    - RL+QP variants（TorqueTask, FDTask）。
    - 对比CaT Safe-RL（训练时安全，运行时无滤波器）。
- **指标**：约束违反率（violations/s）、扭矩/加速度跟踪误差、笛卡尔偏差、生存时间、终止原因分布。

### 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量或训练时长。仅提及RL策略（PPO）是预训练的；Acc-CBF-QP本身是运行时优化，**无需额外训练**。计算成本：在H1上（19自由度，400 Hz控制频率），单次QP求解平均0.348–0.360 ms，占用不到控制周期的15%（CPU为Intel Core Ultra 7 155H）。因此对训练算力无要求，推理算力要求较低。

### 5. 实验数量与充分性

- **机械臂**：10个周期（每个周期两个靶点间移动+停留），重复性实验；分布外场景一次示范性验证。
- **人形**：
  - 真实硬件：每种模式（RL, RL+QP TorqueTask, RL+QP FDTask）各运行约1分钟，统计违反率。
  - 仿真：**200次配对试验**（相同随机初始条件）用于激进速度命令场景；**200次配对试验**用于不平地形场景。此外还进行了扭矩/加速度误差分析（仿真+硬件）。
- **充分性评价**：
  - **正面**：覆盖多场景（正常、分布外、激进行为、不平地形）；仿真与硬件对照；配对试验保证统计功效；使用了Wilcoxon检验和非劣效性检验，统计严谨。
  - **不足**：仅与CaT一种Safe-RL基线对比，缺乏与CPO、PPO-Lagrangian等的比较；仅在两种机器人上验证，通用性需更多平台验证；未进行消融研究分离CBF增益或QP任务的影响（但文中提到增益调参经验性给出）。

### 6. 论文的主要结论与发现

- **约束违反显著减少**：
  - 机械臂：RL+QP完全消除限位违反，而RL导致停机。
  - 人形硬件：RL alone：10.04次/秒；RL+QP TorqueTask：0.80次/秒（降92%）；RL+QP FDTask：1.32次/秒。剩余违反均为速度限位（推测因GMO延迟导致）。
  - 仿真中，QP变体达到零违反。
- **标称性能保持**：在无违反条件下，RL+QP的笛卡尔偏差与RL自身重复性相当（非劣效性检验p<0.05，容忍0.5mm）。
- **激进命令下生存时间延长**：相比RL-Sat（扭矩饱和），RL+QP中位生存时间提升约20%（2.96–3.10 s vs 2.59 s），且违反被转化为合理跌倒，未降低任务目标。
- **不平地形**：所有控制器生存时间相当（10–11 s），QP变体显著减少违反，失败主因变为跌倒而非限位违反。QP不可行性通过阻尼或回滚策略处理，性能差异不显著。
- **TorqueTask更鲁棒**：在模型不准确时，TorqueTask的扭矩误差比FDTask小79%（均值2.6 Nm vs 12.4 Nm）；而FDTask在精确模型下加速度偏差更小。硬件上TorqueTask违反率略低（0.80 vs 1.32）。

### 7. 优点

- **即插即用**：无需重训练RL策略，可与任意预训练策略（无约束/Safe-RL）兼容，适用于不同机器人架构。
- **统一约束框架**：在加速度级自然处理关节位置、速度、扭矩和碰撞，线性化后可高效求解。
- **两种任务设计**：提供了安全-性能权衡的灵活选择（TorqueTask更鲁棒，FDTask更保守）。
- **开源完整流程**：包含状态估计、观测器、QP求解，降低复现门槛。
- **硬件验证充分**：在真实机械臂和人形上均运行，包括应对模型误差（质量偏差>8kg）的鲁棒性。
- **鲁棒性分析**：提供输入-状态安全保证，量化扰动对安全裕度的影响。
- **统计检验严谨**：使用配对Wilcoxon和非劣效性检验，结论可靠。

### 8. 不足与局限

- **基线对比有限**：仅与CaT Safe-RL对比，未与CPO、PPO-Lagrangian、其他CBF方法（如Shield）比较。也未探索将Acc-CBF-QP集成到训练中（如作为探索指导）。
- **QP不可行性处理简陋**：仅采用阻尼模式或回滚到原始RL-Sat，牺牲形式化保证。更优雅的方法（如松弛变量、层级QP）未探索。
- **缺乏正式稳定性分析**：仅给出了ISSf保证，未分析闭环RL系统的稳定性（文中承认是开放问题）。
- **模型依赖**：性能高度依赖于动力学模型精度和观测器延迟。在H1上仍存在速度限位违反（0.8次/秒），表明扰动估计滞后导致暂态越过边界。
- **实验覆盖不足**：仅测试了行走和简单到达任务，未涉及复杂操作或动态交互（如抛掷、避障）。未测试多任务优先级（如同时操作+行走）。
- **资源未报告训练细节**：虽本文不要求训练，但预训练策略的训练配置未知，影响可复现性。

（完）

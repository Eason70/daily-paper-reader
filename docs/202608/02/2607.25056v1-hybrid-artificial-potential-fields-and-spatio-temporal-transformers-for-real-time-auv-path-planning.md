---
title: Hybrid Artificial Potential Fields and Spatio-Temporal Transformers for Real-Time AUV Path Planning
title_zh: 混合人工势场与时空变换器用于实时AUV路径规划
authors: "Khadija Rais, Abdelmadjid Benmachiche, Imene Soualmia"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.25056v1"
tags: ["query:ocean-robot"]
score: 9.0
evidence: AUV路径规划：人工势场与时空Transformer
tldr: 针对AUV在复杂水下环境中实时路径规划的需求，本文比较了13种经典与学习型算法，并提出混合人工势场与时空Transformer的框架。在五类高分辨率地形场景中，该方法以最短平均路径943.15单位、碰撞率0.031和0.96秒计算时间取得综合最优，优于纯学习和经典方法。所提方案兼顾反应式避障与全局最优性，适合资源受限的实时导航系统。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25056v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1501, \"height\": 986, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25056v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1740, \"height\": 644, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25056v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1940, \"height\": 479, \"label\": \"Table\"}]"
motivation: 现有AUV路径规划算法在最优性、安全性与实时性上难以兼顾，需要一种混合策略以平衡各类指标。
method: 提出混合人工势场与时空Transformer的框架，融合反应式避障和全局时序建模，并与经典、采样、元启发及学习算法对比。
result: 混合方法平均路径长度943.15单位，碰撞率0.031，计算时间0.96秒，综合性能最优。
conclusion: 推荐作为实时AUV导航的主要方案，兼顾避障安全性与路径最优性。
---

## 摘要
自主水下航行器（AUV）在复杂、非结构化的环境中运行，高效且安全的路径规划对于任务成功和能量节约至关重要。本文对十三种路径规划算法进行了全面的比较评估，涵盖从经典图搜索方法（A*、Dijkstra）和基于采样的方法（RRT*）到元启发式算法（PSO、GA、ACO、BCO）以及基于学习的架构。特别强调了一种提出的混合方法，该方法将人工势场（APF）与时空（ST）变换器相结合。在高分辨率水下地形图上的五种导航场景中评估，所有算法均实现了100%的任务完成率；然而，在路径最优性、碰撞避免和计算负载方面出现了显著的权衡。混合APF + ST-Transformer表现出优越的平衡性能，实现了最短的平均路径长度（943.15单位）、低碰撞率（0.031）和高效的计算时间（0.96秒），优于需要回退机制的独立学习模型和产生较高延迟的经典方法。虽然经典算法保证了无碰撞路径，但其过长的路径长度和处理时间使其不太适用于动态水下作业。相反，元启发式方法引入了不适合严格能量约束的轨迹复杂性。基于这些发现，推荐将混合APF + ST框架作为实时AUV导航的主要方法，提供一种在资源受限的水下系统中协调反应式避障与全局路径最优性的稳健解决方案。

## Abstract
Autonomous Underwater Vehicles (AUVs) operate in complex, unstructured environments where efficient and safe path planning is critical for mission success and energy conservation. This paper presents a comprehensive comparative evaluation of thirteen path planning algorithms, ranging from classical graph-search methods (A*, Dijkstra) and sampling-based approaches (RRT*) to metaheuristics (PSO, GA, ACO, BCO) and learning-based architectures. Special emphasis is placed on a proposed hybrid approach combining Artificial Potential Fields (APF) with a Spatio-Temporal (ST) Transformer. Evaluated across five navigation scenarios on high-resolution underwater terrain maps, all algorithms achieved 100\% task completion; however, significant trade-offs emerged in path optimality, collision avoidance, and computational load. The Hybrid APF + ST-Transformer demonstrated superior balanced performance, achieving the shortest average path length (943.15 units), a low collision rate (0.031), and efficient computation time (0.96 s), outperforming standalone learning models, which required fallback mechanisms and classical methods that incurred higher latency. While classical algorithms guaranteed collision-free paths, their excessive path lengths and processing times render them less suitable for dynamic underwater operations. Conversely, metaheuristic approaches introduced trajectory complexity unsuitable for strict energy constraints. Based on these findings, the Hybrid APF + ST framework is recommended as a principal approach for real-time AUV navigation, offering a robust solution that harmonizes reactive obstacle avoidance with global path optimality in resource-constrained underwater systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究动机**：自主水下航行器（AUV）在 GPS 受限、存在海流、能见度低、声学通信延迟且计算资源严格受限的复杂非结构化环境中执行任务时，路径规划直接决定任务成功率、运行安全性和能量消耗。现有路径规划算法在**全局最优性、实时响应、运动学可行性和环境鲁棒性**之间难以同时满足。
- **背景**：经典图搜索方法（A*、Dijkstra）能保证无碰撞但计算开销高、路径长；采样方法（RRT*）探索效率高但轨迹粗糙；元启发式算法（PSO、GA、ACO、BCO）全局搜索能力强但轨迹复杂、参数敏感；纯学习模型适应性强但缺乏可解释性、需要大量训练数据且在未见场景下需要安全回退机制。
- **核心问题**：是否存在一种混合架构，能够结合反应式避障（如 APF）的轻量、可解释性与学习模型（如 Transformer）的全局预测能力，从而在资源受限的水下系统中实现实时、安全、最优的路径规划？
- **论文含义**：通过对十三种算法进行系统对比，提出并验证了“混合 APF + 时空 Transformer”框架，为 AUV 实时导航提供了一种兼顾安全、最优性和计算效率的主选方案。

## 2. 方法论：核心思想、关键技术细节

- **整体框架**：将经典的**人工势场（APF）** 与**时空 Transformer（ST-Transformer）** 混合，实现“反应式局部避障 + 全局预测性规划”的统一控制律。
- **环境建模**：
  - 采用 2D 网格高程图 Z ∈ R^(H×W)，每个元素 Z(x,y) 表示归一化地形高程，值域 [0,1]，低值表示安全区域，高值表示障碍或风险区域。
  - 地形梯度 ∇Z 用于计算斥力。
- **人工势场（APF）**：
  - 吸引力（向目标）：F_att = g − p
  - 斥力（避障）：F_rep = ∇Z(x,y)，当高程超过阈值 τ 时放大。
  - 合力：F_APF = λ_att·F_att − λ_rep·F_rep
  - 运动更新采用动量形式：v_{t+1} = β·v_t + (1−β)·ÂF_APF；p_{t+1} = p_t + η·v_{t+1}
- **时空 Transformer**：
  - 输入为部分轨迹 T = [p0, p1, ..., pt] 和上下文向量 c = [s, g]（起点与终点）。
  - 使用线性投影进行嵌入，上下文向量编码后与轨迹嵌入相加。
  - 采用标准自注意力：Attention(Q,K,V) = softmax(QK^T / √d) V。
  - 输出预测航路点 p̂_{t+1}，用作“引导信号”而非直接控制，tanh 约束输出范围。
- **混合控制策略**：
  - 学习方向 d_learned = p̂_{t+1} − p_t
  - 目标方向 d_goal = (g − p_t) / ||g − p_t||
  - 最终方向：d_final = w1·F_APF + w2·d_learned + w3·d_goal，权重满足 w1 > w2 > w3，确保安全优先。
- **长距离适应机制**：
  - 当距离目标超过阈值 d_th 时，增大吸引力以加速收敛。
  - 停滞检测：若近期位置方差过低（判定陷入局部极小），施加小随机扰动。
- **碰撞与成功判定**：位置高程大于 τ 视为不安全；路径中间点采样统计碰撞率；终点满足 ||p_T − g|| < ε 即为成功。

## 3. 实验设计

- **数据集/场景**：在 **992 × 992 网格的高分辨率水下地形图**上，设计了**五种导航场景**：Diagonal（对角）、Cross（交叉）、Medium（中等）、Reverse（反向）、Long/Long Diagonal（长距离/长对角）。
- **对比方法**（共 13 种）：
  - 经典图搜索：A*、Dijkstra
  - 采样方法：RRT*
  - 元启发式：PSO、GA、ACO、BCO
  - 学习/混合：ST-Transformer（未训练）、Hybrid APF+ST-Transformer、APF（独立）、APF+A* 混合、Theta* 独立、Theta*+APF 混合
- **评估指标**：
  - 平均路径长度（单位）
  - 平滑度（越低越平滑）
  - 碰撞率（路径上不安全点比例）
  - 计算时间（秒）
  - 成功完成率（所有算法均为 100%）
- **基准设置**：在每张表格中报告各场景的路径长度、时间（t）和碰撞率（c）；表格 1 为 5 个场景的平均值，表格 2 为逐场景 APF 与基于 Transformer 算法的详细数据。

## 4. 资源与算力

- **论文未明确说明**使用的 GPU 型号、数量、训练时长或具体硬件资源。
- 仅提供了算法的计算时间指标（如混合方法平均 0.96 秒、APF 0.124 秒等），这些是在特定（未详述）硬件上运行的平均推理/求解时间。
- 由于 ST-Transformer 为“未训练”模型（untrained），不存在训练算力开销，但作者也未给出推理时的具体设备类型。

## 5. 实验数量与充分性

- **实验组数**：5 种场景 × 13 种算法，每个算法重复通过场景完成评估，共得到 5×13 = 65 组任务测试（每个算法在每个场景均成功完成），并汇总为平均性能表。
- **消融/对比充分性**：
  - 涵盖了经典、采样、元启发式、学习以及多种混合变体（APF+A*、Theta*+APF、Hybrid APF+ST），对比范围较广。
  - 额外提供了逐场景分析（表格 2），有助于观察算法在不同任务结构下的表现差异。
- **局限性**：
  - 所有实验在 **2D 网格地形**上完成，缺乏真实海洋环境中的 3D 动态、海流、传感器噪声和 AUV 动力学约束。
  - 未报告的多次随机运行的标准差或统计显著性检验，平滑度数值差异较大时并未说明是否由随机性导致。
  - 元启发式（PSO、GA、ACO、BCO）通常具有随机性，但论文未报告多次独立运行的平均值与方差，可能影响结果公平性。

## 6. 主要结论与发现

- **混合 APF + ST-Transformer 综合最优**：
  - 平均路径长度最短：943.15 单位（比纯 APF、ST-Transformer、Theta* 变体提升约 2.4%）。
  - 碰撞率低：0.031，显著低于独立 Theta*（0.378），且优于独立 APF（0.042）。
  - 平均计算时间 0.96 秒，满足实时性要求。
- **经典方法**：A* 和 Dijkstra 保证零碰撞，但路径长（1149.83、1190.90）且耗时高（7.08s、11.41s），仅适合离线规划。
- **采样方法**：RRT* 零碰撞、路径中等、计算较快（0.424s），适合未知环境快速重规划。
- **元启发式**：平滑度极差（15.78–92.79），轨迹震荡，能耗高，不适合实时水下导航。
- **未训练的 ST-Transformer**：速度最快（0.112s）且路径近乎直线（平滑度 ~10^-14），但所有场景均依赖回退机制确保安全，无法独立用于安全关键任务。
- **推荐策略**：实时导航优先采用 Hybrid APF+ST 或 APF+A*；未知环境用 RRT* 快速探索；经典方法用于离线路径规划。

## 7. 优点

- **问题定位清晰**：基于最新综述识别了“单一范式无法兼顾全局最优性、实时性、安全性和鲁棒性”的空白，并针对性地提出混合架构。
- **方法设计合理**：APF 负责反应式避障（安全优先），Transformer 提供全局时序预测（路径优化），权重约束 w1>w2>w3 使安全主导，融合方式具有可解释性。
- **对比体系全面**：涵盖四类主流算法（经典、采样、元启发、学习）及其混合变体，共 13 种方法，参考价值高。
- **多指标评估**：同时评估路径长度、平滑度、碰撞率、计算时间，不仅能判断最优性，还能衡量安全性和实时性，符合实际部署需求。
- **逐场景分析**：不仅给出平均值，还提供五种场景的详细数据，揭示了算法在不同任务结构下的行为差异（如 Theta* 在 Long 场景碰撞率高达 0.957）。
- **结论务实**：给出分层推荐策略，而非单一方案，便于工程实践选择。

## 8. 不足与局限

- **环境建模过于简化**：使用 2D 高程网格，忽略 3D 水下动力学（海流、压力、温度梯度）、AUV 的六自由度运动学、推进器约束等，真实部署效果存疑。
- **学习组件未训练**：论文中的 ST-Transformer 为“未训练”状态，未体现学习能力的实际效果；作者也未给出训练所需数据或预训练过程，实际上仅利用其随机初始化的输出作为引导，其增益可能并非来自学习能力。
- **缺乏统计显著性分析**：未报告多次运行的标准差，也未进行显著性检验，元启发式算法为随机算法，结果可能受随机种子影响。
- **无消融实验**：未对混合权重 w1、w2、w3 进行敏感性分析，也未对比“训练过的 Transformer + APF”与“未训练版本 + APF”的差异。
- **未涉及真实 AUV 硬件实验**：所有评估均在离线地图上模拟，未考虑传感器噪声、通信延迟、动态障碍物等实际因素。
- **算力信息缺失**：未说明运行硬件的型号和规格，导致时间指标难以复现或跨系统比较。

（完）
